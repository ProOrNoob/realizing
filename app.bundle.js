// Sutta Archive - app.bundle.js v63
// Built: Wed 06/17/2026 17:38:40.17
// DO NOT EDIT - generated by build.bat

window.SUTRA_INDEX = [
  {
    type: "nikaya",
    key: "DN",
    labelVi: "Digha Nikaya - Trường Bộ Kinh",
    labelEn: "Dīgha Nikāya - Long Discourses",
   children: [
  { type: "sutta", id: "dn01", code: "DN 1", titlePali: "Brahmajāla Sutta", titleVi: "Kinh Phạm Võng", titleEn: "The Supreme Net" },
  { type: "sutta", id: "dn02", code: "DN 2", titlePali: "Sāmaññaphala Sutta", titleVi: "Kinh Sa Môn Quả", titleEn: "The Fruits of the Contemplative Life" },
  { type: "sutta", id: "dn03", code: "DN 3", titlePali: "Ambaṭṭha Sutta", titleVi: "Kinh Ambattha", titleEn: "About Ambaṭṭha" },
  { type: "sutta", id: "dn04", code: "DN 4", titlePali: "Soṇadaṇḍa Sutta", titleVi: "Kinh Sonadanda", titleEn: "With Soṇadaṇḍa" },
  { type: "sutta", id: "dn05", code: "DN 5", titlePali: "Kūṭadanta Sutta", titleVi: "Kinh Kutadanta", titleEn: "With Kūṭadanta" },
  { type: "sutta", id: "dn06", code: "DN 6", titlePali: "Mahāli Sutta", titleVi: "Kinh Mahāli", titleEn: "With Mahāli" },
  { type: "sutta", id: "dn07", code: "DN 7", titlePali: "Jāliya Sutta", titleVi: "Kinh Jāliya", titleEn: "With Jāliya" },
  { type: "sutta", id: "dn08", code: "DN 8", titlePali: "Mahāsīhanāda Sutta", titleVi: "Kinh Sư Tử Hống với Kassapa", titleEn: "The Great Lion’s Roar" },
  { type: "sutta", id: "dn09", code: "DN 9", titlePali: "Poṭṭhapāda Sutta", titleVi: "Kinh Potthapada", titleEn: "With Poṭṭhapāda" },
  { type: "sutta", id: "dn10", code: "DN 10", titlePali: "Subha Sutta", titleVi: "Kinh Subha", titleEn: "To Subha" },
  { type: "sutta", id: "dn11", code: "DN 11", titlePali: "Kevaṭṭa Sutta", titleVi: "Kinh Kevatta", titleEn: "With Kevaṭṭa" },
  { type: "sutta", id: "dn12", code: "DN 12", titlePali: "Lohicca Sutta", titleVi: "Kinh Lohicca", titleEn: "To Lohicca" },
  { type: "sutta", id: "dn13", code: "DN 13", titlePali: "Tevijja Sutta", titleVi: "Kinh Tam Minh", titleEn: "The Threefold Knowledge" },
  { type: "sutta", id: "dn14", code: "DN 14", titlePali: "Mahāpadāna Sutta", titleVi: "Kinh Đại Bổn", titleEn: "The Great Discourse on Past Buddhas" },
  { type: "sutta", id: "dn15", code: "DN 15", titlePali: "Mahānidāna Sutta", titleVi: "Kinh Đại Nhân Duyên", titleEn: "The Great Discourse on Causation" },
  { type: "sutta", id: "dn16", code: "DN 16", titlePali: "Mahāparinibbāna Sutta", titleVi: "Kinh Đại Bát Niết Bàn", titleEn: "The Great Passing Away" },
  { type: "sutta", id: "dn17", code: "DN 17", titlePali: "Mahāsudassana Sutta", titleVi: "Kinh Đại Thiện Kiến", titleEn: "The Great King of Glory" },
  { type: "sutta", id: "dn18", code: "DN 18", titlePali: "Janavasabha Sutta", titleVi: "Kinh Mahāgovinda", titleEn: "With Janavasabha" },
  { type: "sutta", id: "dn19", code: "DN 19", titlePali: "Mahāgovinda Sutta", titleVi: "Kinh Đại Govinda", titleEn: "The Great Steward" },
  { type: "sutta", id: "dn20", code: "DN 20", titlePali: "Mahāsamaya Sutta", titleVi: "Kinh Đại Hội", titleEn: "The Great Assembly" },
  { type: "sutta", id: "dn21", code: "DN 21", titlePali: "Sakkapañha Sutta", titleVi: "Kinh Đế Thích Vấn Đạo", titleEn: "Sakka’s Questions" },
  { type: "sutta", id: "dn22", code: "DN 22", titlePali: "Mahāsatipaṭṭhāna Sutta", titleVi: "Kinh Đại Niệm Xứ", titleEn: "The Great Foundations of Mindfulness" },
  { type: "sutta", id: "dn23", code: "DN 23", titlePali: "Pāyāsi Sutta", titleVi: "Kinh Pāyāsi", titleEn: "With Pāyāsi" },
  { type: "sutta", id: "dn24", code: "DN 24", titlePali: "Pāṭika Sutta", titleVi: "Kinh Pāṭika", titleEn: "About Pāṭika" },
  { type: "sutta", id: "dn25", code: "DN 25", titlePali: "Udumbarikasīhanāda Sutta", titleVi: "Kinh Udumbarika", titleEn: "The Lion’s Roar at Udumbarikā" },
  { type: "sutta", id: "dn26", code: "DN 26", titlePali: "Cakkavattisīhanāda Sutta", titleVi: "Kinh Chuyển Luân Thánh Vương", titleEn: "The Wheel-Turning Monarch’s Lion’s Roar" },
  { type: "sutta", id: "dn27", code: "DN 27", titlePali: "Aggañña Sutta", titleVi: "Kinh Khởi Thế", titleEn: "On Origins" },
  { type: "sutta", id: "dn28", code: "DN 28", titlePali: "Sampasādanīya Sutta", titleVi: "Kinh Tịnh Tín", titleEn: "Inspiring Confidence" },
  { type: "sutta", id: "dn29", code: "DN 29", titlePali: "Pāsādika Sutta", titleVi: "Kinh Khả Hỷ", titleEn: "Inspiring Approval" },
  { type: "sutta", id: "dn30", code: "DN 30", titlePali: "Lakkhaṇa Sutta", titleVi: "Kinh Tướng", titleEn: "The Marks of a Great Man" },
  { type: "sutta", id: "dn31", code: "DN 31", titlePali: "Siṅgālovāda Sutta", titleVi: "Kinh Giáo Thọ Thiện Sanh", titleEn: "Advice to Sigala" },
  { type: "sutta", id: "dn32", code: "DN 32", titlePali: "Āṭānāṭiya Sutta", titleVi: "Kinh Hộ Trì", titleEn: "The Discourse on Atanatiya Protection" },
  { type: "sutta", id: "dn33", code: "DN 33", titlePali: "Saṅgīti Sutta", titleVi: "Kinh Kết Tập", titleEn: "The Recitation" },
  { type: "sutta", id: "dn34", code: "DN 34", titlePali: "Dasuttara Sutta", titleVi: "Kinh Thập Thượng", titleEn: "Up to Ten" }
]
  },
  {
    type: "nikaya",
    key: "MN",
    labelVi: "Majjhima Nikāya - Trung Bộ Kinh",
    labelEn: "Majjhima Nikāya - Middle-Length",
    children:
    [
  { type: "sutta", id: "mn01", code: "MN 1", titlePali: "Mūlapariyāya Sutta", titleVi: "Kinh Căn Bản Pháp Môn", titleEn: "The Root of All Things" },
  { type: "sutta", id: "mn02", code: "MN 2", titlePali: "Sabbāsava Sutta", titleVi: "Kinh Nhất Thiết Lậu Hoặc", titleEn: "All the Taints" },
  { type: "sutta", id: "mn03", code: "MN 3", titlePali: "Dhammadāyāda Sutta", titleVi: "Kinh Thừa Tự Pháp", titleEn: "Heirs in Dhamma" },
  { type: "sutta", id: "mn04", code: "MN 4", titlePali: "Bhayabherava Sutta", titleVi: "Kinh Sợ Hãi và Khiếp Đảm", titleEn: "Fear and Dread" },
  { type: "sutta", id: "mn05", code: "MN 5", titlePali: "Anaṅgaṇa Sutta", titleVi: "Kinh Không Uế Nhiễm", titleEn: "Without Corruption" },
  { type: "sutta", id: "mn06", code: "MN 6", titlePali: "Ākaṅkheyya Sutta", titleVi: "Kinh Ước Nguyện", titleEn: "If One Should Wish" },
  { type: "sutta", id: "mn07", code: "MN 7", titlePali: "Vatthūpama Sutta", titleVi: "Kinh Ví Dụ Tấm Vải", titleEn: "The Simile of the Cloth" },
  { type: "sutta", id: "mn08", code: "MN 8", titlePali: "Sallekha Sutta", titleVi: "Kinh Tẩy Trừ", titleEn: "Effacement" },
  { type: "sutta", id: "mn09", code: "MN 9", titlePali: "Sammādiṭṭhi Sutta", titleVi: "Kinh Chánh Tri Kiến", titleEn: "Right View" },
  { type: "sutta", id: "mn10", code: "MN 10", titlePali: "Satipaṭṭhāna Sutta", titleVi: "Kinh Niệm Xứ", titleEn: "The Foundations of Mindfulness" },
  { type: "sutta", id: "mn11", code: "MN 11", titlePali: "Cūḷasīhanāda Sutta", titleVi: "Kinh Tiểu Sư Tử Hống", titleEn: "The Shorter Discourse on the Lion’s Roar" },
  { type: "sutta", id: "mn12", code: "MN 12", titlePali: "Mahāsīhanāda Sutta", titleVi: "Kinh Đại Sư Tử Hống", titleEn: "The Greater Discourse on the Lion’s Roar" },
  { type: "sutta", id: "mn13", code: "MN 13", titlePali: "Mahādukkhakkhandha Sutta", titleVi: "Kinh Đại Khổ Uẩn", titleEn: "The Greater Mass of Suffering" },
  { type: "sutta", id: "mn14", code: "MN 14", titlePali: "Cūḷadukkhakkhandha Sutta", titleVi: "Kinh Tiểu Khổ Uẩn", titleEn: "The Lesser Mass of Suffering" },
  { type: "sutta", id: "mn15", code: "MN 15", titlePali: "Anumāna Sutta", titleVi: "Kinh Suy Tư", titleEn: "Inference" },
  { type: "sutta", id: "mn16", code: "MN 16", titlePali: "Cetokhila Sutta", titleVi: "Kinh Tâm Hoang Vu", titleEn: "The Wilderness of the Mind" },
  { type: "sutta", id: "mn17", code: "MN 17", titlePali: "Vanapattha Sutta", titleVi: "Kinh Rừng Rậm", titleEn: "Jungle Thickets" },
  { type: "sutta", id: "mn18", code: "MN 18", titlePali: "Madhupiṇḍika Sutta", titleVi: "Kinh Bánh Mật", titleEn: "The Honeyball" },
  { type: "sutta", id: "mn19", code: "MN 19", titlePali: "Dvedhāvitakka Sutta", titleVi: "Kinh Hai Loại Tư Duy", titleEn: "Two Kinds of Thought" },
  { type: "sutta", id: "mn20", code: "MN 20", titlePali: "Vitakkasaṇṭhāna Sutta", titleVi: "Kinh Đối Trị Tư Duy", titleEn: "The Removal of Distracting Thoughts" },
  { type: "sutta", id: "mn21", code: "MN 21", titlePali: "Kakacūpama Sutta", titleVi: "Kinh Ví Dụ Cái Cưa", titleEn: "The Simile of the Saw" },
  { type: "sutta", id: "mn22", code: "MN 22", titlePali: "Alagaddūpama Sutta", titleVi: "Kinh Ví Dụ Con Rắn", titleEn: "The Simile of the Snake" },
  { type: "sutta", id: "mn23", code: "MN 23", titlePali: "Vammika Sutta", titleVi: "Kinh Tổ Mối", titleEn: "The Ant-hill" },
  { type: "sutta", id: "mn24", code: "MN 24", titlePali: "Rathavinīta Sutta", titleVi: "Kinh Xe Trạm", titleEn: "Relay Chariots" },
  { type: "sutta", id: "mn25", code: "MN 25", titlePali: "Nivāpa Sutta", titleVi: "Kinh Mồi Nhử", titleEn: "The Bait" },
  { type: "sutta", id: "mn26", code: "MN 26", titlePali: "Ariyapariyesanā Sutta", titleVi: "Kinh Thánh Cầu", titleEn: "The Noble Search" },
  { type: "sutta", id: "mn27", code: "MN 27", titlePali: "Cūḷahatthipadopama Sutta", titleVi: "Kinh Dấu Chân Voi Nhỏ", titleEn: "The Shorter Elephant Footprint Simile" },
  { type: "sutta", id: "mn28", code: "MN 28", titlePali: "Mahāhatthipadopama Sutta", titleVi: "Kinh Dấu Chân Voi Lớn", titleEn: "The Greater Elephant Footprint Simile" },
  { type: "sutta", id: "mn29", code: "MN 29", titlePali: "Mahāsāropama Sutta", titleVi: "Kinh Ví Dụ Lõi Cây Lớn", titleEn: "The Greater Discourse on the Simile of the Heartwood" },
  { type: "sutta", id: "mn30", code: "MN 30", titlePali: "Cūḷasāropama Sutta", titleVi: "Kinh Ví Dụ Lõi Cây Nhỏ", titleEn: "The Shorter Discourse on the Heartwood" },
  { type: "sutta", id: "mn31", code: "MN 31", titlePali: "Cūḷagosiṅga Sutta", titleVi: "Kinh Rừng Gosiṅga Nhỏ", titleEn: "The Shorter Gosiṅga Forest Discourse" },
  { type: "sutta", id: "mn32", code: "MN 32", titlePali: "Mahāgosiṅga Sutta", titleVi: "Kinh Rừng Gosiṅga Lớn", titleEn: "The Greater Gosiṅga Forest Discourse" },
  { type: "sutta", id: "mn33", code: "MN 33", titlePali: "Mahāgopālaka Sutta", titleVi: "Kinh Người Chăn Bò Lớn", titleEn: "The Greater Cowherd Discourse" },
  { type: "sutta", id: "mn34", code: "MN 34", titlePali: "Cūḷagopālaka Sutta", titleVi: "Kinh Người Chăn Bò Nhỏ", titleEn: "The Shorter Cowherd Discourse" },
  { type: "sutta", id: "mn35", code: "MN 35", titlePali: "Cūḷasaccaka Sutta", titleVi: "Kinh Saccaka Nhỏ", titleEn: "The Shorter Discourse to Saccaka" },
  { type: "sutta", id: "mn36", code: "MN 36", titlePali: "Mahāsaccaka Sutta", titleVi: "Kinh Saccaka Lớn", titleEn: "The Greater Discourse to Saccaka" },
  { type: "sutta", id: "mn37", code: "MN 37", titlePali: "Cūḷataṇhāsaṅkhaya Sutta", titleVi: "Kinh Diệt Ái Nhỏ", titleEn: "The Shorter Discourse on the Destruction of Craving" },
  { type: "sutta", id: "mn38", code: "MN 38", titlePali: "Mahātaṇhāsaṅkhaya Sutta", titleVi: "Kinh Diệt Ái Lớn", titleEn: "The Greater Discourse on the Destruction of Craving" },
  { type: "sutta", id: "mn39", code: "MN 39", titlePali: "Mahāassapura Sutta", titleVi: "Kinh Đại Assapura", titleEn: "The Greater Discourse at Assapura" },
  { type: "sutta", id: "mn40", code: "MN 40", titlePali: "Cūḷaassapura Sutta", titleVi: "Kinh Tiểu Assapura", titleEn: "The Shorter Discourse at Assapura" },
  { type: "sutta", id: "mn41", code: "MN 41", titlePali: "Sāleyyaka Sutta", titleVi: "Kinh Sāleyyaka", titleEn: "The People of Sālā" },
  { type: "sutta", id: "mn42", code: "MN 42", titlePali: "Verañjaka Sutta", titleVi: "Kinh Verañja", titleEn: "At Verañjā" },
  { type: "sutta", id: "mn43", code: "MN 43", titlePali: "Mahāvedalla Sutta", titleVi: "Kinh Đại Vấn Đáp", titleEn: "The Greater Set of Questions and Answers" },
  { type: "sutta", id: "mn44", code: "MN 44", titlePali: "Cūḷavedalla Sutta", titleVi: "Kinh Tiểu Vấn Đáp", titleEn: "The Shorter Set of Questions and Answers" },
  { type: "sutta", id: "mn45", code: "MN 45", titlePali: "Cūḷadhammasamādāna Sutta", titleVi: "Kinh Tiểu Thọ Trì Pháp", titleEn: "The Shorter Discourse on Taking Up Practices" },
  { type: "sutta", id: "mn46", code: "MN 46", titlePali: "Mahādhammasamādāna Sutta", titleVi: "Kinh Đại Thọ Trì Pháp", titleEn: "The Greater Discourse on Taking Up Practices" },
  { type: "sutta", id: "mn47", code: "MN 47", titlePali: "Vīmaṁsaka Sutta", titleVi: "Kinh Tư Sát", titleEn: "The Inquirer" },
  { type: "sutta", id: "mn48", code: "MN 48", titlePali: "Kosambiya Sutta", titleVi: "Kinh Kosambi", titleEn: "The Kosambiyans" },
  { type: "sutta", id: "mn49", code: "MN 49", titlePali: "Brahmanimantanika Sutta", titleVi: "Kinh Phạm Thiên Thỉnh Phật", titleEn: "The Invitation of the Brahmā" },
  { type: "sutta", id: "mn50", code: "MN 50", titlePali: "Māratajjaniya Sutta", titleVi: "Kinh Quở Trách Ác Ma", titleEn: "The Rebuke to Māra" },
  { type: "sutta", id: "mn51", code: "MN 51", titlePali: "Kandaraka Sutta", titleVi: "Kinh Kandaraka", titleEn: "With Kandaraka" },
  { type: "sutta", id: "mn52", code: "MN 52", titlePali: "Aṭṭhakanāgara Sutta", titleVi: "Kinh Aṭṭhakanāgara", titleEn: "The Man from Aṭṭhakanagara" },
  { type: "sutta", id: "mn53", code: "MN 53", titlePali: "Sekha Sutta", titleVi: "Kinh Hữu Học", titleEn: "The Disciple in Higher Training" },
  { type: "sutta", id: "mn54", code: "MN 54", titlePali: "Potaliya Sutta", titleVi: "Kinh Potaliya", titleEn: "To Potaliya" },
  { type: "sutta", id: "mn55", code: "MN 55", titlePali: "Jīvaka Sutta", titleVi: "Kinh Jīvaka", titleEn: "To Jīvaka" },
  { type: "sutta", id: "mn56", code: "MN 56", titlePali: "Upāli Sutta", titleVi: "Kinh Upāli", titleEn: "To Upāli" },
  { type: "sutta", id: "mn57", code: "MN 57", titlePali: "Kukkuravatika Sutta", titleVi: "Kinh Hạnh Chó", titleEn: "The Dog-Duty Ascetic" },
  { type: "sutta", id: "mn58", code: "MN 58", titlePali: "Abhayarājakumāra Sutta", titleVi: "Kinh Thái Tử Abhaya", titleEn: "To Prince Abhaya" },
  { type: "sutta", id: "mn59", code: "MN 59", titlePali: "Bahuvedanīya Sutta", titleVi: "Kinh Nhiều Cảm Thọ", titleEn: "The Many Kinds of Feeling" },
  { type: "sutta", id: "mn60", code: "MN 60", titlePali: "Apaṇṇaka Sutta", titleVi: "Kinh Không Thể Sai Lầm", titleEn: "The Incontrovertible Teaching" },
  { type: "sutta", id: "mn61", code: "MN 61", titlePali: "Ambalaṭṭhikā Rāhulovāda Sutta", titleVi: "Kinh Giáo Giới Rāhula tại Amba", titleEn: "Advice to Rāhula at Ambalaṭṭhikā" },
  { type: "sutta", id: "mn62", code: "MN 62", titlePali: "Mahārāhulovāda Sutta", titleVi: "Kinh Đại Giáo Giới Rāhula", titleEn: "The Greater Discourse on Advice to Rāhula" },
  { type: "sutta", id: "mn63", code: "MN 63", titlePali: "Cūḷamālukya Sutta", titleVi: "Kinh Tiểu Māluṅkyā", titleEn: "The Shorter Discourse to Māluṅkyāputta" },
  { type: "sutta", id: "mn64", code: "MN 64", titlePali: "Mahāmālukya Sutta", titleVi: "Kinh Đại Māluṅkyā", titleEn: "The Greater Discourse to Māluṅkyāputta" },
  { type: "sutta", id: "mn65", code: "MN 65", titlePali: "Bhaddāli Sutta", titleVi: "Kinh Bhaddāli", titleEn: "To Bhaddāli" },
  { type: "sutta", id: "mn66", code: "MN 66", titlePali: "Laṭukikopama Sutta", titleVi: "Kinh Ví Dụ Chim Cút", titleEn: "The Quail Simile" },
  { type: "sutta", id: "mn67", code: "MN 67", titlePali: "Cātumā Sutta", titleVi: "Kinh Cātumā", titleEn: "At Cātumā" },
  { type: "sutta", id: "mn68", code: "MN 68", titlePali: "Naḷakapāna Sutta", titleVi: "Kinh Naḷakapāna", titleEn: "At Naḷakapāna" },
  { type: "sutta", id: "mn69", code: "MN 69", titlePali: "Goliyāni Sutta", titleVi: "Kinh Goliyāni", titleEn: "At Goliyāni" },
  { type: "sutta", id: "mn70", code: "MN 70", titlePali: "Kīṭāgiri Sutta", titleVi: "Kinh Kīṭāgiri", titleEn: "At Kīṭāgiri" },
  { type: "sutta", id: "mn71", code: "MN 71", titlePali: "Tevijjavacchagotta Sutta", titleVi: "Kinh Tevijja Vacchagotta", titleEn: "To Vacchagotta on the Threefold Knowledge" },
  { type: "sutta", id: "mn72", code: "MN 72", titlePali: "Aggivacchagotta Sutta", titleVi: "Kinh Aggivacchagotta", titleEn: "To Vacchagotta on Fire" },
  { type: "sutta", id: "mn73", code: "MN 73", titlePali: "Mahāvacchagotta Sutta", titleVi: "Kinh Đại Vacchagotta", titleEn: "The Greater Discourse to Vacchagotta" },
  { type: "sutta", id: "mn74", code: "MN 74", titlePali: "Dīghanakha Sutta", titleVi: "Kinh Trường Trảo", titleEn: "To Dīghanakha" },
  { type: "sutta", id: "mn75", code: "MN 75", titlePali: "Māgaṇḍiya Sutta", titleVi: "Kinh Māgaṇḍiya", titleEn: "To Māgaṇḍiya" },
  { type: "sutta", id: "mn76", code: "MN 76", titlePali: "Sandaka Sutta", titleVi: "Kinh Sandaka", titleEn: "To Sandaka" },
  { type: "sutta", id: "mn77", code: "MN 77", titlePali: "Mahāsakuludāyi Sutta", titleVi: "Kinh Đại Sakuludāyi", titleEn: "The Greater Discourse to Sakuludāyī" },
  { type: "sutta", id: "mn78", code: "MN 78", titlePali: "Samaṇamuṇḍika Sutta", titleVi: "Kinh Sa Môn Trọc Đầu", titleEn: "The Shaveling Ascetic" },
  { type: "sutta", id: "mn79", code: "MN 79", titlePali: "Cūḷasakuludāyi Sutta", titleVi: "Kinh Tiểu Sakuludāyi", titleEn: "The Shorter Discourse to Sakuludāyī" },
  { type: "sutta", id: "mn80", code: "MN 80", titlePali: "Vekhanassa Sutta", titleVi: "Kinh Vekhanassa", titleEn: "To Vekhanassa" },
  { type: "sutta", id: "mn81", code: "MN 81", titlePali: "Ghaṭikāra Sutta", titleVi: "Kinh Ghaṭikāra", titleEn: "With Ghaṭikāra" },
  { type: "sutta", id: "mn82", code: "MN 82", titlePali: "Raṭṭhapāla Sutta", titleVi: "Kinh Raṭṭhapāla", titleEn: "About Raṭṭhapāla" },
  { type: "sutta", id: "mn83", code: "MN 83", titlePali: "Maghadeva Sutta", titleVi: "Kinh Maghadeva", titleEn: "On King Maghadeva" },
  { type: "sutta", id: "mn84", code: "MN 84", titlePali: "Madhura Sutta", titleVi: "Kinh Madhura", titleEn: "At Madhurā" },
  { type: "sutta", id: "mn85", code: "MN 85", titlePali: "Bodhirājakumāra Sutta", titleVi: "Kinh Thái Tử Bodhi", titleEn: "To Prince Bodhi" },
  { type: "sutta", id: "mn86", code: "MN 86", titlePali: "Aṅgulimāla Sutta", titleVi: "Kinh Aṅgulimāla", titleEn: "About Aṅgulimāla" },
  { type: "sutta", id: "mn87", code: "MN 87", titlePali: "Piyajātika Sutta", titleVi: "Kinh Ái Sanh", titleEn: "Born from Affection" },
  { type: "sutta", id: "mn88", code: "MN 88", titlePali: "Bāhitika Sutta", titleVi: "Kinh Bāhitika", titleEn: "About the Cloak" },
  { type: "sutta", id: "mn89", code: "MN 89", titlePali: "Dhammacetiya Sutta", titleVi: "Kinh Bảo Tháp Pháp", titleEn: "Monuments to the Dhamma" },
  { type: "sutta", id: "mn90", code: "MN 90", titlePali: "Kaṇṇakatthala Sutta", titleVi: "Kinh Kaṇṇakatthala", titleEn: "At Kaṇṇakatthala" },
  { type: "sutta", id: "mn91", code: "MN 91", titlePali: "Brahmāyu Sutta", titleVi: "Kinh Bà La Môn Brahmāyu", titleEn: "With Brahmāyu" },
  { type: "sutta", id: "mn92", code: "MN 92", titlePali: "Sela Sutta", titleVi: "Kinh Sela", titleEn: "With Sela" },
  { type: "sutta", id: "mn93", code: "MN 93", titlePali: "Assalāyana Sutta", titleVi: "Kinh Assalāyana", titleEn: "To Assalāyana" },
  { type: "sutta", id: "mn94", code: "MN 94", titlePali: "Ghoṭamukha Sutta", titleVi: "Kinh Ghoṭamukha", titleEn: "With Ghoṭamukha" },
  { type: "sutta", id: "mn95", code: "MN 95", titlePali: "Caṅkī Sutta", titleVi: "Kinh Caṅkī", titleEn: "With Caṅkī" },
  { type: "sutta", id: "mn96", code: "MN 96", titlePali: "Esukārī Sutta", titleVi: "Kinh Esukārī", titleEn: "With Esukārī" },
  { type: "sutta", id: "mn97", code: "MN 97", titlePali: "Dhanañjāni Sutta", titleVi: "Kinh Dhanañjāni", titleEn: "With Dhanañjāni" },
  { type: "sutta", id: "mn98", code: "MN 98", titlePali: "Vāseṭṭha Sutta", titleVi: "Kinh Vāseṭṭha", titleEn: "With Vāseṭṭha" },
  { type: "sutta", id: "mn99", code: "MN 99", titlePali: "Subha Sutta", titleVi: "Kinh Subha", titleEn: "To Subha" },
  { type: "sutta", id: "mn100", code: "MN 100", titlePali: "Saṅgārava Sutta", titleVi: "Kinh Saṅgārava", titleEn: "To Saṅgārava" },
  { type: "sutta", id: "mn101", code: "MN 101", titlePali: "Devadaha Sutta", titleVi: "Kinh Devadaha", titleEn: "At Devadaha" },
  { type: "sutta", id: "mn102", code: "MN 102", titlePali: "Pañcattaya Sutta", titleVi: "Kinh Năm Quan Điểm", titleEn: "Five and Three" },
  { type: "sutta", id: "mn103", code: "MN 103", titlePali: "Kinti Sutta", titleVi: "Kinh Kinti", titleEn: "What Do You Think of Me?" },
  { type: "sutta", id: "mn104", code: "MN 104", titlePali: "Sāmagāma Sutta", titleVi: "Kinh Sāmagāma", titleEn: "At Sāmagāma" },
  { type: "sutta", id: "mn105", code: "MN 105", titlePali: "Sunakkhatta Sutta", titleVi: "Kinh Sunakkhatta", titleEn: "To Sunakkhatta" },
  { type: "sutta", id: "mn106", code: "MN 106", titlePali: "Āneñjasappāya Sutta", titleVi: "Kinh Con Đường Bất Động", titleEn: "The Way to the Imperturbable" },
  { type: "sutta", id: "mn107", code: "MN 107", titlePali: "Gaṇakamoggallāna Sutta", titleVi: "Kinh Moggallāna Người Tính Toán", titleEn: "To Gaṇaka Moggallāna" },
  { type: "sutta", id: "mn108", code: "MN 108", titlePali: "Gopakamoggallāna Sutta", titleVi: "Kinh Gopaka Moggallāna", titleEn: "With Gopaka Moggallāna" },
  { type: "sutta", id: "mn109", code: "MN 109", titlePali: "Mahāpuṇṇama Sutta", titleVi: "Kinh Đêm Trăng Tròn Lớn", titleEn: "The Greater Full-Moon Night" },
  { type: "sutta", id: "mn110", code: "MN 110", titlePali: "Cūḷapuṇṇama Sutta", titleVi: "Kinh Đêm Trăng Tròn Nhỏ", titleEn: "The Smaller Full-Moon Night" },
  { type: "sutta", id: "mn111", code: "MN 111", titlePali: "Anupada Sutta", titleVi: "Kinh Từng Bước", titleEn: "One by One as They Occurred" },
  { type: "sutta", id: "mn112", code: "MN 112", titlePali: "Chabbisodhana Sutta", titleVi: "Kinh Sáu Thanh Tịnh", titleEn: "The Sixfold Purity" },
  { type: "sutta", id: "mn113", code: "MN 113", titlePali: "Sappurisa Sutta", titleVi: "Kinh Người Chân Chánh", titleEn: "The True Person" },
  { type: "sutta", id: "mn114", code: "MN 114", titlePali: "Sevitabba Sutta", titleVi: "Kinh Nên Gần Gũi", titleEn: "To Be Cultivated" },
  { type: "sutta", id: "mn115", code: "MN 115", titlePali: "Bahudhātuka Sutta", titleVi: "Kinh Nhiều Giới", titleEn: "The Many Kinds of Elements" },
  { type: "sutta", id: "mn116", code: "MN 116", titlePali: "Isigili Sutta", titleVi: "Kinh Isigili", titleEn: "At Isigili" },
  { type: "sutta", id: "mn117", code: "MN 117", titlePali: "Mahācattārīsaka Sutta", titleVi: "Kinh Bốn Mươi Lớn", titleEn: "The Great Forty" },
  { type: "sutta", id: "mn118", code: "MN 118", titlePali: "Ānāpānasati Sutta", titleVi: "Kinh Niệm Hơi Thở", titleEn: "Mindfulness of Breathing" },
  { type: "sutta", id: "mn119", code: "MN 119", titlePali: "Kāyagatāsati Sutta", titleVi: "Kinh Niệm Thân", titleEn: "Mindfulness of the Body" },
  { type: "sutta", id: "mn120", code: "MN 120", titlePali: "Saṅkhārupapatti Sutta", titleVi: "Kinh Sanh Theo Nghiệp", titleEn: "Reappearance by Volitional Formations" },
  { type: "sutta", id: "mn121", code: "MN 121", titlePali: "Cūḷasuññata Sutta", titleVi: "Kinh Không Nhỏ", titleEn: "The Shorter Discourse on Emptiness" },
  { type: "sutta", id: "mn122", code: "MN 122", titlePali: "Mahāsuññata Sutta", titleVi: "Kinh Không Lớn", titleEn: "The Greater Discourse on Emptiness" },
  { type: "sutta", id: "mn123", code: "MN 123", titlePali: "Acchariyabbhuta Sutta", titleVi: "Kinh Hy Hữu Vị Tằng Hữu", titleEn: "Wonderful and Marvellous" },
  { type: "sutta", id: "mn124", code: "MN 124", titlePali: "Bakkula Sutta", titleVi: "Kinh Bakkula", titleEn: "About Bakkula" },
  { type: "sutta", id: "mn125", code: "MN 125", titlePali: "Dantabhūmi Sutta", titleVi: "Kinh Điều Phục", titleEn: "The Grade of the Tamed" },
  { type: "sutta", id: "mn126", code: "MN 126", titlePali: "Bhūmija Sutta", titleVi: "Kinh Bhūmija", titleEn: "To Bhūmija" },
  { type: "sutta", id: "mn127", code: "MN 127", titlePali: "Anuruddha Sutta", titleVi: "Kinh Anuruddha", titleEn: "To Anuruddha" },
  { type: "sutta", id: "mn128", code: "MN 128", titlePali: "Upakkilesa Sutta", titleVi: "Kinh Nhiễm Ô", titleEn: "The Corruptions" },
  { type: "sutta", id: "mn129", code: "MN 129", titlePali: "Bālapaṇḍita Sutta", titleVi: "Kinh Người Ngu – Người Trí", titleEn: "The Fool and the Wise" },
  { type: "sutta", id: "mn130", code: "MN 130", titlePali: "Devadūta Sutta", titleVi: "Kinh Sứ Giả Chư Thiên", titleEn: "The Divine Messengers" },
  { type: "sutta", id: "mn131", code: "MN 131", titlePali: "Bhaddekaratta Sutta", titleVi: "Kinh Một Đêm An Lành", titleEn: "One Fortunate Attachment to the Present" },
  { type: "sutta", id: "mn132", code: "MN 132", titlePali: "Ānandabhaddekaratta Sutta", titleVi: "Kinh Một Đêm An Lành với Ananda", titleEn: "Ānanda and One Fortunate Attachment to the Present" },
  { type: "sutta", id: "mn133", code: "MN 133", titlePali: "Mahākaccānabhaddekaratta Sutta", titleVi: "Kinh Một Đêm An Lành với Mahākaccāna", titleEn: "Mahākaccāna and One Fortunate Attachment to the Present" },
  { type: "sutta", id: "mn134", code: "MN 134", titlePali: "Lomasakaṅgiya Sutta", titleVi: "Kinh Lomasakaṅgiya", titleEn: "To Lomasakaṅgiya" },
  { type: "sutta", id: "mn135", code: "MN 135", titlePali: "Cūḷakammavibhaṅga Sutta", titleVi: "Kinh Tiểu Nghiệp Phân Biệt", titleEn: "The Shorter Exposition of Kamma" },
  { type: "sutta", id: "mn136", code: "MN 136", titlePali: "Mahākammavibhaṅga Sutta", titleVi: "Kinh Đại Nghiệp Phân Biệt", titleEn: "The Greater Exposition of Kamma" },
  { type: "sutta", id: "mn137", code: "MN 137", titlePali: "Saḷāyatanavibhaṅga Sutta", titleVi: "Kinh Phân Biệt Sáu Xứ", titleEn: "The Exposition of the Six Sense Bases" },
  { type: "sutta", id: "mn138", code: "MN 138", titlePali: "Uddesavibhaṅga Sutta", titleVi: "Kinh Phân Biệt Tóm Tắt", titleEn: "The Exposition of a Summary" },
  { type: "sutta", id: "mn139", code: "MN 139", titlePali: "Araṇavibhaṅga Sutta", titleVi: "Kinh Phân Biệt Không Tranh Cãi", titleEn: "The Exposition of Non-Conflict" },
  { type: "sutta", id: "mn140", code: "MN 140", titlePali: "Dhātuvibhaṅga Sutta", titleVi: "Kinh Phân Biệt Các Giới", titleEn: "The Exposition of the Elements" },
  { type: "sutta", id: "mn141", code: "MN 141", titlePali: "Saccavibhaṅga Sutta", titleVi: "Kinh Phân Biệt Thánh Đế", titleEn: "The Exposition of the Truths" },
  { type: "sutta", id: "mn142", code: "MN 142", titlePali: "Dakkhiṇāvibhaṅga Sutta", titleVi: "Kinh Phân Biệt Bố Thí", titleEn: "The Exposition of Offerings" },
  { type: "sutta", id: "mn143", code: "MN 143", titlePali: "Anāthapiṇḍikovāda Sutta", titleVi: "Kinh Giáo Giới Cấp Cô Độc", titleEn: "Advice to Anāthapiṇḍika" },
  { type: "sutta", id: "mn144", code: "MN 144", titlePali: "Channovāda Sutta", titleVi: "Kinh Giáo Giới Channa", titleEn: "Advice to Channa" },
  { type: "sutta", id: "mn145", code: "MN 145", titlePali: "Puṇṇovāda Sutta", titleVi: "Kinh Giáo Giới Puṇṇa", titleEn: "Advice to Puṇṇa" },
  { type: "sutta", id: "mn146", code: "MN 146", titlePali: "Nandakovāda Sutta", titleVi: "Kinh Giáo Giới Nandaka", titleEn: "Advice to Nandaka" },
  { type: "sutta", id: "mn147", code: "MN 147", titlePali: "Cūḷarāhulovāda Sutta", titleVi: "Kinh Tiểu Giáo Giới Rāhula", titleEn: "The Shorter Advice to Rāhula" },
  { type: "sutta", id: "mn148", code: "MN 148", titlePali: "Chachakka Sutta", titleVi: "Kinh Sáu Bộ Sáu", titleEn: "The Analysis of the Six Sets of Six" },
  { type: "sutta", id: "mn149", code: "MN 149", titlePali: "Mahāsaḷāyatanika Sutta", titleVi: "Kinh Đại Sáu Xứ", titleEn: "The Great Sixfold Sense Base" },
  { type: "sutta", id: "mn150", code: "MN 150", titlePali: "Nagaravindeyya Sutta", titleVi: "Kinh Nagaravindeyya", titleEn: "At Nagaravinda" },
  { type: "sutta", id: "mn151", code: "MN 151", titlePali: "Piṇḍapātapārisuddhi Sutta", titleVi: "Kinh Thanh Tịnh Thọ Dụng", titleEn: "The Purification of Alms-food" },
  { type: "sutta", id: "mn152", code: "MN 152", titlePali: "Indriyabhāvanā Sutta", titleVi: "Kinh Tu Tập Các Căn", titleEn: "The Development of the Faculties" }


]


  },
  {
    type: "nikaya",
    key: "SN",
    labelVi: "Saṃyutta Nikāya - Tương Ưng Bộ Kinh",
    labelEn: "Saṃyutta Nikāya - Connected Discourses",
    children: [
  { type: "sutta", id: "sn1_v1", code: "SN 1", titlePali: "Devatā-saṁyutta", titleVi: "Chủ đề 1: Chư Thiên", titleEn: "Linked Discourses With Deities" },
  { type: "sutta", id: "sn2_v1", code: "SN 2", titlePali: "Devaputta-saṁyutta", titleVi: "Chủ đề 2: Thiên Tử", titleEn: "Linked Discourses With Gods' Sons" },
  { type: "sutta", id: "sn3_v1", code: "SN 3", titlePali: "Kosala-saṁyutta", titleVi: "Chủ đề 3: Kosala", titleEn: "Linked Discourses With the Kosalan" },
  { type: "sutta", id: "sn4_v1", code: "SN 4", titlePali: "Māra-saṁyutta", titleVi: "Chủ đề 4: Mara", titleEn: "Linked Discourses With Māra" },
  { type: "sutta", id: "sn5_v1", code: "SN 5", titlePali: "Bhikkhunī-saṁyutta", titleVi: "Chủ đề 5: Tỳ-Kheo-Ni", titleEn: "Linked Discourses With Nuns" },
  { type: "sutta", id: "sn6_v1", code: "SN 6", titlePali: "Brahma-saṁyutta", titleVi: "Chủ đề 6: Phạm Thiên", titleEn: "Linked Discourses With Brahmās" },
  { type: "sutta", id: "sn7_v1", code: "SN 7", titlePali: "Brāhmaṇa-saṁyutta", titleVi: "Chủ đề 7: Bà-la-môn", titleEn: "Linked Discourses With Brahmins" },
  { type: "sutta", id: "sn8_v1", code: "SN 8", titlePali: "Vaṅgīsa-saṁyutta", titleVi: "Chủ đề 8: Trưởng Lão Vaṅgīsa", titleEn: "Linked Discourses With Vaṅgīsa" },
  { type: "sutta", id: "sn9_v1", code: "SN 9", titlePali: "Vana-saṁyutta", titleVi: "Chủ đề 9: Rừng", titleEn: "Linked Discourses in the Woods" },
  { type: "sutta", id: "sn10_v1", code: "SN 10", titlePali: "Yakkha-saṁyutta", titleVi: "Chủ đề 10: Dạ Xoa", titleEn: "Linked Discourses With Spirits" },
  { type: "sutta", id: "sn11_v1", code: "SN 11", titlePali: "Sakka-saṁyutta", titleVi: "Chủ đề 11: Đế thích", titleEn: "Linked Discourses With Sakka" },
  { type: "sutta", id: "sn12_v1", code: "SN 12", titlePali: "Nidāna-saṁyutta", titleVi: "Chủ đề 12: Nhân duyên", titleEn: "Linked Discourses on Causation" },
  { type: "sutta", id: "sn13_v1", code: "SN 13", titlePali: "Abhisamaya-saṁyutta", titleVi: "Chủ đề 13: Đột hiện", titleEn: "Linked Discourses on Comprehension" },
  { type: "sutta", id: "sn14_v1", code: "SN 14", titlePali: "Dhātu-saṁyutta", titleVi: "Chủ đề 14: Các thành tố", titleEn: "Linked Discourses on the Elements" },
  { type: "sutta", id: "sn15_v1", code: "SN 15", titlePali: "Anamatagga-saṁyutta", titleVi: "Chủ đề 15: Vô thỉ", titleEn: "Linked Discourses on the Unknown Beginning" },
  { type: "sutta", id: "sn16_v1", code: "SN 16", titlePali: "Kassapa-saṁyutta", titleVi: "Chủ đề 16: Kassapa", titleEn: "Linked Discourses With Kassapa" },
  { type: "sutta", id: "sn17_v1", code: "SN 17", titlePali: "Lābhasakkāra-saṁyutta", titleVi: "Chủ đề 17: Lợi đắc Cung kính", titleEn: "Linked Discourses on Possessions, Honor, and Popularity" },
  { type: "sutta", id: "sn18_v1", code: "SN 18", titlePali: "Rāhula-saṁyutta", titleVi: "Chủ đề 18: Rāhula", titleEn: "Linked Discourses With Rāhula" },
  { type: "sutta", id: "sn19_v1", code: "SN 19", titlePali: "Lakkhaṇa-saṁyutta", titleVi: "Chủ đề 19: Lakkhaṇa", titleEn: "Linked Discourses With Lakkhaṇa" },
  { type: "sutta", id: "sn20_v1", code: "SN 20", titlePali: "Opamma-saṁyutta", titleVi: "Chủ đề 20: Thí dụ", titleEn: "Linked Discourses on Similes" },
  { type: "sutta", id: "sn21_v1", code: "SN 21", titlePali: "Bhikkhu-saṁyutta", titleVi: "Chủ đề 21: Tỳ-Kheo", titleEn: "Linked Discourses With Mendicants" },
  { type: "sutta", id: "sn22_v1", code: "SN 22", titlePali: "Khandha-saṁyutta", titleVi: "Chủ đề 22: Uẩn", titleEn: "Linked Discourses on the Aggregates" },
  { type: "sutta", id: "sn23_v1", code: "SN 23", titlePali: "Rādha-saṁyutta", titleVi: "Chủ đề 23: Rādha", titleEn: "Linked Discourses With Rādha" },
  { type: "sutta", id: "sn24_v1", code: "SN 24", titlePali: "Diṭṭhi-saṁyutta", titleVi: "Chủ đề 24: Kiến", titleEn: "Linked Discourses on Views" },
  { type: "sutta", id: "sn25_v1", code: "SN 25", titlePali: "Okkanta-saṁyutta", titleVi: "Chủ đề 25: Nhập", titleEn: "Linked Discourses on Arrival" },
  { type: "sutta", id: "sn26_v1", code: "SN 26", titlePali: "Uppāda-saṁyutta", titleVi: "Chủ đề 26: Sanh", titleEn: "Linked Discourses on Arising" },
  { type: "sutta", id: "sn27_v1", code: "SN 27", titlePali: "Kilesa-saṁyutta", titleVi: "Chủ đề 27: Phiền não", titleEn: "Linked Discourses on Corruptions" },
  { type: "sutta", id: "sn28_v1", code: "SN 28", titlePali: "Sāriputta-saṁyutta", titleVi: "Chủ đề 28: Sāriputta", titleEn: "Linked Discourses With Sāriputta" },
  { type: "sutta", id: "sn29_v1", code: "SN 29", titlePali: "Nāga-saṁyutta", titleVi: "Chủ đề 29: Rồng", titleEn: "Linked Discourses on Dragons" },
  { type: "sutta", id: "sn30_v1", code: "SN 30", titlePali: "Supaṇṇa-saṁyutta", titleVi: "Chủ đề 30: Supaṇṇa", titleEn: "Linked Discourses With Phoenixes" },
  { type: "sutta", id: "sn31_v1", code: "SN 31", titlePali: "Gandhabbakāya-saṁyutta", titleVi: "Chủ đề 31: Gandhabba", titleEn: "Linked Discourses With Centaurs" },
  { type: "sutta", id: "sn32_v1", code: "SN 32", titlePali: "Valāhaka-saṁyutta", titleVi: "Chủ đề 32: Thần mây", titleEn: "Linked Discourses With Cloud Gods" },
  { type: "sutta", id: "sn33_v1", code: "SN 33", titlePali: "Vacchagotta-saṁyutta", titleVi: "Chủ đề 33: Vacchagotta", titleEn: "Linked Discourses With Vacchagotta" },
  { type: "sutta", id: "sn34_v1", code: "SN 34", titlePali: "Jhāna-saṁyutta", titleVi: "Chủ đề 34: Thiền định", titleEn: "Linked Discourses on Absorption" },
  { type: "sutta", id: "sn35_v1", code: "SN 35", titlePali: "Saḷāyatana-saṁyutta", titleVi: "Chủ đề 35: Sáu xứ", titleEn: "Linked Discourses on the Six Sense Fields" },
  { type: "sutta", id: "sn36_v1", code: "SN 36", titlePali: "Vedanā-saṁyutta", titleVi: "Chủ đề 36: Cảm giác", titleEn: "Linked Discourses on Feeling" },
  { type: "sutta", id: "sn37_v1", code: "SN 37", titlePali: "Mātugāma-saṁyutta", titleVi: "Chủ đề 37: Nữ nhân", titleEn: "Linked Discourses With Women" },
  { type: "sutta", id: "sn38_v1", code: "SN 38", titlePali: "Jambukhādaka-saṁyutta", titleVi: "Chủ đề 38: Jambukhādaka", titleEn: "Linked Discourses With Jambukhādaka" },
  { type: "sutta", id: "sn39_v1", code: "SN 39", titlePali: "Sāmaṇḍaka-saṁyutta", titleVi: "Chủ đề 39: Jambukhādaka", titleEn: "Linked Discourses With Sāmaṇḍaka" },
  { type: "sutta", id: "sn40_v1", code: "SN 40", titlePali: "Moggallāna-saṁyutta", titleVi: "Chủ đề 40: Moggallāna", titleEn: "Linked Discourses With Moggallāna" },
  { type: "sutta", id: "sn41_v1", code: "SN 41", titlePali: "Citta-saṁyutta", titleVi: "Chủ đề 41: Cư sĩ Citta", titleEn: "Linked Discourses With Citta" },
  { type: "sutta", id: "sn42_v1", code: "SN 42", titlePali: "Gāmaṇi-saṁyutta", titleVi: "Chủ đề 42: Thôn trưởng", titleEn: "Linked Discourses With Chiefs" },
  { type: "sutta", id: "sn43_v1", code: "SN 43", titlePali: "Asaṅkhata-saṁyutta", titleVi: "Chủ đề 43: Vô vi", titleEn: "Linked Discourses on the Unconditioned" },
  { type: "sutta", id: "sn44_v1", code: "SN 44", titlePali: "Abyākata-saṁyutta", titleVi: "Chủ đề 44: Không thuyết", titleEn: "Linked Discourses on the Undeclared Points" },
  { type: "sutta", id: "sn45_v1", code: "SN 45", titlePali: "Magga-saṁyutta", titleVi: "Chủ đề 45: Đạo", titleEn: "Linked Discourses on the Path" },
  { type: "sutta", id: "sn46_v1", code: "SN 46", titlePali: "Bojjhaṅga-saṁyutta", titleVi: "Chủ đề 46: Giác chi", titleEn: "Linked Discourses on the Awakening Factors" },
  { type: "sutta", id: "sn47_v1", code: "SN 47", titlePali: "Satipaṭṭhāna-saṁyutta", titleVi: "Chủ đề 47: Niệm xứ", titleEn: "Linked Discourses on Mindfulness Meditation" },
  { type: "sutta", id: "sn48_v1", code: "SN 48", titlePali: "Indriya-saṁyutta", titleVi: "Chủ đề 48: Căn", titleEn: "Linked Discourses on the Faculties" },
  { type: "sutta", id: "sn49_v1", code: "SN 49", titlePali: "Sammappadhāna-saṁyutta", titleVi: "Chủ đề 49: Chánh cần", titleEn: "Linked Discourses on the Right Efforts" },
  { type: "sutta", id: "sn50_v1", code: "SN 50", titlePali: "Bala-saṁyutta", titleVi: "Chủ đề 50: Lực", titleEn: "Linked Discourses on the Powers" },
  { type: "sutta", id: "sn51_v1", code: "SN 51", titlePali: "Iddhipāda-saṁyutta", titleVi: "Chủ đề 51: Thần túc", titleEn: "Linked Discourses on the Bases of Psychic Power" },
  { type: "sutta", id: "sn52_v1", code: "SN 52", titlePali: "Anuruddha-saṁyutta", titleVi: "Chủ đề 52: Anuruddha", titleEn: "Linked Discourses With Anuruddha" },
  { type: "sutta", id: "sn53_v1", code: "SN 53", titlePali: "Jhāna-saṁyutta", titleVi: "Chủ đề 53: Thiền", titleEn: "Linked Discourses on Absorption" },
  { type: "sutta", id: "sn54_v1", code: "SN 54", titlePali: "Ānāpāna-saṁyutta", titleVi: "Chủ đề 54: Hơi thở vào Hơi thở ra", titleEn: "Linked Discourses on Breath Meditation" },
  { type: "sutta", id: "sn55_v1", code: "SN 55", titlePali: "Sotāpatti-saṁyutta", titleVi: "Chủ đề 55: Dự lưu", titleEn: "Linked Discourses on Stream-Entry" },
  { type: "sutta", id: "sn56_v1", code: "SN 56", titlePali: "Sacca-saṁyutta", titleVi: "Chủ đề 56: Sự thật", titleEn: "Linked Discourses on the Truths" }
]
},
  {
    type: "nikaya",
    key: "AN",
    labelVi: "Aṅguttara Nikāya - Tăng Chi Bộ Kinh",
    labelEn: "Aṅguttara Nikāya - Numerical Discourses",
    children: [
  
  { type: "sutta", id: "an1_v1", code: "AN 1", titlePali: "Aṅguttara Nikāya 1", titleVi: "Nhóm Một Pháp", titleEn: "Numbered Discourses 1" },
  { type: "sutta", id: "an2_v1", code: "AN 2", titlePali: "Aṅguttara Nikāya 2", titleVi: "Nhóm Hai Pháp", titleEn: "Numbered Discourses 2" },
  { type: "sutta", id: "an3_v1", code: "AN 3", titlePali: "Aṅguttara Nikāya 3", titleVi: "Nhóm Ba Pháp", titleEn: "Numbered Discourses 3" },
  { type: "sutta", id: "an4_v1", code: "AN 4", titlePali: "Aṅguttara Nikāya 4", titleVi: "Nhóm Bốn Pháp", titleEn: "Numbered Discourses 4" },
  { type: "sutta", id: "an5_v1", code: "AN 5", titlePali: "Aṅguttara Nikāya 5", titleVi: "Nhóm Năm Pháp", titleEn: "Numbered Discourses 5" },
  { type: "sutta", id: "an6_v1", code: "AN 6", titlePali: "Aṅguttara Nikāya 6", titleVi: "Nhóm Sáu Pháp", titleEn: "Numbered Discourses 6" },
  { type: "sutta", id: "an7_v1", code: "AN 7", titlePali: "Aṅguttara Nikāya 7", titleVi: "Nhóm Bảy Pháp", titleEn: "Numbered Discourses 7" },
  { type: "sutta", id: "an8_v1", code: "AN 8", titlePali: "Aṅguttara Nikāya 8", titleVi: "Nhóm Tám Pháp", titleEn: "Numbered Discourses 8" },
  { type: "sutta", id: "an9_v1", code: "AN 9", titlePali: "Aṅguttara Nikāya 9", titleVi: "Nhóm Chín Pháp", titleEn: "Numbered Discourses 9" },
  { type: "sutta", id: "an10_v1", code: "AN 10", titlePali: "Aṅguttara Nikāya 10", titleVi: "Nhóm Mười Pháp", titleEn: "Numbered Discourses 10" },
  { type: "sutta", id: "an11_v1", code: "AN 11", titlePali: "Aṅguttara Nikāya 11", titleVi: "Nhóm Mười Một Pháp", titleEn: "Numbered Discourses 11" }
	]
   }
]
;

/* ================================================================
   utils.js — Module [1/8]
   Khởi tạo namespace SA, các hàm tiện ích dùng chung.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.$, SA.escapeHtml, SA.escapeAttr, SA.safeDomId,
            SA.debounce, SA.throttle, SA.storage, SA.safeCssEscape
   Depends: (none — first module loaded)
   ================================================================ */
(function () {
'use strict';
var SA = window.SA = window.SA || {};

try { if (history.scrollRestoration) history.scrollRestoration = 'manual'; } catch(_) {}

SA.DEBUG = true;
SA.$ = function (id) { return document.getElementById(id); };

SA.escapeHtml = function (str) {
	if (str === undefined || str === null) return '';
	return String(str)
		.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
		.replace(/"/g, '&quot;').replace(/'/g, '&#39;');
};

SA.escapeAttr = function (val) {
	if (val === undefined || val === null) return '';
	return String(val)
		.replace(/&/g, '&amp;').replace(/"/g, '&quot;').replace(/'/g, '&#39;')
		.replace(/</g, '&lt;').replace(/`/g, '&#96;').replace(/>/g, '&gt;');
};

SA.safeDomId = function (base) { return String(base).replace(/[^a-z0-9_-]/gi, '-'); };

SA.debounce = function (fn, wait) {
	if (wait === undefined) wait = 200;
	var t;
	var debounced = function () {
		var args = arguments;
		clearTimeout(t);
		t = setTimeout(function () { fn.apply(null, args); }, wait);
	};
	debounced.cancel = function () { clearTimeout(t); t = null; };
	return debounced;
};

SA.throttle = function (fn, wait) {
	if (wait === undefined) wait = 120;
	var last = 0;
	return function () {
		var now = Date.now();
		if (now - last >= wait) { last = now; fn.apply(null, arguments); }
	};
};

SA.storage = {
	get: function (key) { try { return localStorage.getItem(key); } catch (e) { return null; } },
	set: function (key, val) { try { localStorage.setItem(key, val); } catch (e) {} },
	remove: function (key) { try { localStorage.removeItem(key); } catch (e) {} }
};

SA.safeCssEscape = function (str) {
	if (typeof CSS !== 'undefined' && typeof CSS.escape === 'function') {
		return CSS.escape(str);
	}
	return String(str).replace(/([^\w-])/g, '\\$1');
};
})();

/* ================================================================
   bilara.js — Module [2/8]
   Tải dữ liệu kinh từ file pack (pli/en/vi), cache LRU 20 bài.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.loadMerged(id), SA.MERGED_CACHE, SA.LOADED_PACKS
   Depends: utils.js
   Data format: sutta/<lang>/<id>.js → ghi vào window.BILARA[id]
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;

var LOADED_PACKS = new Set();
var PACK_PROMISES = new Map();
var PACK_LOAD_TIMEOUT_MS = 15000;

function loadPackIfNeeded(pack) {
	if (!pack) return Promise.resolve();
	if (LOADED_PACKS.has(pack)) return Promise.resolve();
	if (PACK_PROMISES.has(pack)) return PACK_PROMISES.get(pack);
	var p = new Promise(function (res, rej) {
		var settled = false;
		var s = null;
		var tid = null;
		var cleanup = function () {
			if (tid) { clearTimeout(tid); tid = null; }
			if (s) { try { s.onload = s.onerror = null; if (s.parentNode) s.parentNode.removeChild(s); } catch(_){} s = null; }
			PACK_PROMISES.delete(pack);
		};
		try {
			s = document.createElement('script');
			s.src = pack + '.js'; s.async = true;
			s.onload = function () { if (settled) return; settled = true; LOADED_PACKS.add(pack); cleanup(); res(); };
			s.onerror = function (e) { if (settled) return; settled = true; cleanup(); rej(e || new Error('load fail: ' + pack)); };
			tid = setTimeout(function () {
				if (settled) return; settled = true; cleanup();
				rej(new Error('pack load timeout: ' + pack));
			}, PACK_LOAD_TIMEOUT_MS);
			document.body.appendChild(s);
		} catch (e) { settled = true; cleanup(); rej(e); }
	});
	PACK_PROMISES.set(pack, p);
	return p;
}

(function purgeStaleServiceWorkers() {
	try {
		if (!('serviceWorker' in navigator)) return;
		navigator.serviceWorker.getRegistrations().then(function (regs) {
			if (!regs || !regs.length) return;
			regs.forEach(function (r) { try { r.unregister(); } catch(_){} });
			if (window.caches && caches.keys) {
				caches.keys().then(function (keys) {
					keys.forEach(function (k) { try { caches.delete(k); } catch(_){} });
				}).catch(function(){});
			}
		}).catch(function(){});
	} catch(_){}
})();

window.BILARA = window.BILARA || {};
var BILARA_BASE_DIR = './sutta';

function getBilaraPack(lang, id) {
	if (!id) return null;
	if (!/^[A-Za-z0-9_-]+$/.test(id)) return null;
	return BILARA_BASE_DIR + '/' + lang + '/' + id;
}

var MERGED_CACHE = new Map();
var MERGED_PROMISES = new Map();
var CACHE_ORDER = [];
var MAX_CACHE_SUTTAS = 20;

function touchCache(id) {
	var i = CACHE_ORDER.indexOf(id);
	if (i !== -1) CACHE_ORDER.splice(i, 1);
	CACHE_ORDER.push(id);
	while (CACHE_ORDER.length > MAX_CACHE_SUTTAS) {
		var old = CACHE_ORDER.shift();
		if (old) MERGED_CACHE.delete(old);
	}
}

function unionKeys3(a, b, c) {
	var set = new Set();
	if (a) Object.keys(a).forEach(function (k) { set.add(k); });
	if (b) Object.keys(b).forEach(function (k) { set.add(k); });
	if (c) Object.keys(c).forEach(function (k) { set.add(k); });
	return Array.from(set);
}

var _BILARA_COLLATOR = (typeof Intl !== 'undefined' && Intl.Collator)
	? new Intl.Collator('en', { numeric: true }) : null;

function sortBilaraKeys(keys) {
	if (_BILARA_COLLATOR) return keys.sort(_BILARA_COLLATOR.compare);
	return keys.sort(function (x, y) { return x.localeCompare(y, 'en', { numeric: true }); });
}

function getCommentPack(lang, id) {
	if (!id || !/^[A-Za-z0-9_-]+$/.test(id)) return null;
	return BILARA_BASE_DIR + '/comment/' + id + '_' + lang;
}

function shouldLoadCommentPack(lang, id) {
	var idx = window.COMMENT_INDEX;
	if (!idx || typeof idx !== 'object') return true;
	var entry = idx[id];
	if (!entry) return false;
	if (Array.isArray(entry)) return entry.indexOf(lang) !== -1;
	return !!entry;
}

SA.loadMerged = async function loadMerged(id) {
	if (!id) return null;
	if (MERGED_CACHE.has(id)) return MERGED_CACHE.get(id);
	if (MERGED_PROMISES.has(id)) return MERGED_PROMISES.get(id);
	var p = (async function () {
		var tasks = [
			loadPackIfNeeded(getBilaraPack('pli', id)),
			loadPackIfNeeded(getBilaraPack('en', id)),
			loadPackIfNeeded(getBilaraPack('vi', id))
		];
		await Promise.all(tasks);
		var entry = window.BILARA[id] || {};
		var paliMap = entry.pli || {};
		var engMap  = entry.en  || {};
		var vieMap  = entry.vi  || {};
		var cmtPli  = entry.commentPli || {};
		var cmtEn   = entry.commentEn  || {};
		var cmtVi   = entry.commentVi  || {};
		var cmtLegacy = entry.comment  || {};
		var keys = sortBilaraKeys(unionKeys3(paliMap, engMap, vieMap));
		var rows = keys.map(function (k) {
			return {
				key: k, pali: paliMap[k]||'', eng: engMap[k]||'', vie: vieMap[k]||'',
				commentPli: cmtPli[k] || '', commentEn: cmtEn[k] || '',
				commentVie: cmtVi[k] || '', comment: cmtLegacy[k] || ''
			};
		});
		var merged = {
			paliMap: paliMap, engMap: engMap, vieMap: vieMap,
			commentPliMap: cmtPli, commentEnMap: cmtEn, commentVieMap: cmtVi,
			commentMap: cmtLegacy, keys: keys, rows: rows
		};
		MERGED_CACHE.set(id, merged);
		touchCache(id);
		MERGED_PROMISES.delete(id);
		return merged;
	})().catch(function (e) { MERGED_PROMISES.delete(id); throw e; });
	MERGED_PROMISES.set(id, p);
	return p;
};

SA.MERGED_CACHE = MERGED_CACHE;
SA.LOADED_PACKS = LOADED_PACKS;
})();

/* ================================================================
   state.js — Module [3/8]
   DOM refs (SA.dom), shared mutable state (SA.state), bookmark
   management, anchor get/set (dual sessionStorage + localStorage),
   view data cache (LRU), paragraph merging, DOM mode cache.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.dom, SA.state, SA.anchorSet/Get/Remove,
            SA.getAnchorKeyFor, SA._parseAnchorHash,
            SA.loadBookmarks, SA.isBookmarked, SA.toggleBookmark,
            SA.getSingleVisibleLang, SA.mergeRowsToParagraphRows,
            SA.getViewData, SA.buildViewData, SA.saveViewPrefs,
            SA.loadViewPrefs, SA._dmSaveCurrent, SA._dmTryRestore,
            SA.maybeRerenderIfModeChanged
   Depends: utils.js, bilara.js
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;
var $ = SA.$;
var storage = SA.storage;

var card        = $('card');
var titleEl      = $('title');
var subtitleEl   = $('subtitle');
var superTitleEl = $('supertitle');
var titleMetaEl  = $('titleMeta');
var readerArea   = $('readerArea');
var scrollEl     = readerArea || null;
var grid        = $('sutraGrid');
if (!scrollEl) scrollEl = grid;
var btnSutraMenu  = $('sidebar-btn');
var btnSettings   = $('btnSettings');
var btnGuide      = $('btnGuide');
var btnBackTop    = $('btnBackTop');
var btnUiLang     = $('btnUiLang');
var settingsPanel  = $('settingsPanel');
var sutraMenuPanel = $('sutraMenuPanel');
var sutraMenuList  = $('sutraMenuList');
var guideOverlay   = $('guideOverlay');
var searchInput    = $('sutraSearch');
var searchResultsEl = $('sutraSearchResults');
var btnPali      = $('btnPali');
var btnEng       = $('btnEng');
var btnVie       = $('btnVie');
var btnLayout    = $('btnLayout');
var btnReadTts   = $('btnReadTts');
var btnPauseTts  = $('btnPauseTts');
var btnStopTts   = $('btnStopTts');
var btnFullWidth = $('btnFullWidth');

SA.dom = {
	card: card, titleEl: titleEl, subtitleEl: subtitleEl, superTitleEl: superTitleEl,
	titleMetaEl: titleMetaEl, readerArea: readerArea, scrollEl: scrollEl, grid: grid,
	btnSutraMenu: btnSutraMenu, btnSettings: btnSettings, btnGuide: btnGuide,
	btnBackTop: btnBackTop, btnUiLang: btnUiLang,
	settingsPanel: settingsPanel, sutraMenuPanel: sutraMenuPanel,
	sutraMenuList: sutraMenuList, guideOverlay: guideOverlay,
	searchInput: searchInput, searchResultsEl: searchResultsEl,
	btnPali: btnPali, btnEng: btnEng, btnVie: btnVie, btnLayout: btnLayout,
	btnReadTts: btnReadTts, btnPauseTts: btnPauseTts, btnStopTts: btnStopTts,
	btnFullWidth: btnFullWidth
};

var LANG_STORAGE_KEY = 'sutra_ui_lang';
var uiLang = storage.get(LANG_STORAGE_KEY) === 'vi' ? 'vi' : 'en';
window.SUTRA_UI_LANG = uiLang;

SA.state = {
	currentSutraId: null,
	SUTRA_ORDER: [],
	FLAT_SUTTAS: [],
	showPali: true, showEng: true, showVie: true,
	isRendering: false,
	renderToken: 0,
	lastSingleLangMode: null,
	cachedRows: [],
	virtChunks: [],
	virtAllRows: [],
	virtMatObs: null,
	virtDemObs: null,
	keyToRowIdx: Object.create(null),
	vaggaMarkers: [],
	suttaMarkers: [],
	isAN: false,
	isSN: false,
	fallbackTitle: '',
	fallbackTitleMeta: '',
	superLastSlotEl: null,
	lastAppliedVaggaIdx: -2,
	lastAppliedSuttaIdx: -2,
	anchorObserver: null,
	firstVisibleKey: null,
	activeNikayaKey: null,
	showSegKey: true,
	showColHdr: true,
	showCmtPli: true,
	showCmtEng: true,
	showCmtVie: true,
	hlPli: true,
	hlEng: false,
	hlVie: false,
	show3Cols: false,
	paragraphBreak: true,
	PARAGRAPH_BREAK_LEN: 5,
	suppressBackTop: false,
	_progScrollUntil: 0,
	DEBUG: true,
	LANG_STORAGE_KEY: LANG_STORAGE_KEY,
	get uiLang() { return uiLang; },
	set uiLang(v) { uiLang = v; window.SUTRA_UI_LANG = v; }
};

SA.LANG_STORAGE_KEY = LANG_STORAGE_KEY;
SA.KEY_LAST     = 'lastSutraId';
SA.KEY_VIEW     = 'sutra_view_prefs';
SA.KEY_ANCHOR_K = function (id) { return 'scroll_anchor_key_' + id; };
SA.KEY_ACTIVE_NIKAYA = 'active_nikaya_tile';

var _ssOk = (function(){ try{ sessionStorage.setItem('__t','1'); sessionStorage.removeItem('__t'); return true; } catch(_){ return false; } })();

SA.anchorSet = function (key, val) {
	storage.set(key, val);
	if (_ssOk) { try { sessionStorage.setItem(key, val); } catch(_){} }
};

SA.anchorGet = function (key) {
	if (_ssOk) {
		try { var v = sessionStorage.getItem(key); if (v) return v; } catch(_){}
	}
	return storage.get(key);
};

SA.anchorRemove = function (key) {
	storage.remove(key);
	if (_ssOk) { try { sessionStorage.removeItem(key); } catch(_){} }
};

var _SEG_PREFIX_MAP = null;
SA._resolveSegPrefixToSuttaId = function (prefix) {
	if (_SEG_PREFIX_MAP === null) {
		_SEG_PREFIX_MAP = {};
		if (window.SUTRA_INDEX) {
			(function walk(arr) {
				for (var i = 0; i < arr.length; i++) {
					var n = arr[i];
					if (n && n.type === 'sutta' && n.id) {
						var p = String(n.id).replace(/_v\d+$/, '').replace(/^([a-z]+)0+(\d)/, '$1$2');
						_SEG_PREFIX_MAP[p] = n.id;
					}
					if (n && n.children) walk(n.children);
				}
			})(window.SUTRA_INDEX);
		}
	}
	return _SEG_PREFIX_MAP[prefix]
		|| _SEG_PREFIX_MAP[prefix.replace(/\.\d+$/, '')]
		|| prefix;
};

SA._parseAnchorHash = function () {
	var h = String(location.hash || '').replace(/^#/, '');
	if (!h) return null;
	var m = h.match(/^([A-Za-z0-9._-]+)(?::.+)?$/);
	if (!m) return null;
	var rawPrefix = m[1].toLowerCase();
	var suttaId = SA._resolveSegPrefixToSuttaId(rawPrefix);
	return { sutta: suttaId, key: h };
};

SA._writeAnchorHash = function (key) {
	if (!key) return;
	try { history.replaceState(null, '', '#' + key); } catch (e) {}
};

SA._clearAnchorHash = function () {
	try {
		if (location.hash) history.replaceState(null, '', location.pathname + location.search);
	} catch (e) {}
};

SA.getAnchorKeyFor = function (id) {
	var h = SA._parseAnchorHash();
	if (h && h.sutta === id && h.key) return h.key;
	return SA.anchorGet(SA.KEY_ANCHOR_K(id));
};

var WIDE_STORAGE_KEY = 'sutra_layout_wide';
SA.state.isWide = storage.get(WIDE_STORAGE_KEY) === '1';
SA.WIDE_STORAGE_KEY = WIDE_STORAGE_KEY;

var KEY_BOOKMARKS = 'sutra_bookmarks';
var BOOKMARKS = new Set();

function loadBookmarks() {
	var raw = storage.get(KEY_BOOKMARKS);
	if (!raw) return;
	try {
		var arr = JSON.parse(raw);
		if (Array.isArray(arr)) {
			for (var i = 0; i < arr.length; i++) {
				if (typeof arr[i] === 'string' && arr[i]) BOOKMARKS.add(arr[i]);
			}
		}
	} catch (e) {}
}
function saveBookmarks() { storage.set(KEY_BOOKMARKS, JSON.stringify(Array.from(BOOKMARKS))); }

SA.loadBookmarks = loadBookmarks;
SA.isBookmarked = function (id) { return !!id && BOOKMARKS.has(id); };
SA.setBookmark = function (id, on) {
	if (!id) return false;
	if (on) BOOKMARKS.add(id); else BOOKMARKS.delete(id);
	saveBookmarks();
	return BOOKMARKS.has(id);
};
SA.toggleBookmark = function (id) { return SA.setBookmark(id, !SA.isBookmarked(id)); };
SA.bookmarkLabels = function () {
	return SA.state.uiLang === 'en'
		? { on: 'Remove bookmark', off: 'Add bookmark', empty: 'No bookmarks yet. Tap ☆ next to any sutta to save it.' }
		: { on: 'Bỏ đánh dấu', off: 'Lưu bài kinh', empty: 'Chưa có bài kinh nào được lưu. Bấm ☆ cạnh tên bài kinh để lưu.' };
};
SA.BOOKMARKS = BOOKMARKS;

SA.getSingleVisibleLang = function () {
	var s = SA.state;
	var count = (s.showPali?1:0) + (s.showEng?1:0) + (s.showVie?1:0);
	if (count !== 1) return null;
	if (s.showPali) return 'pali';
	if (s.showEng) return 'eng';
	if (s.showVie) return 'vie';
	return null;
};

function isNumberedHeadingLine(text) { return /^\d+\.\s*/.test((text||'').trim()); }
SA.isNumberedHeadingLine = isNumberedHeadingLine;

SA.mergeRowsToParagraphRows = function (rows, lang) {
	var s = SA.state;
	var out = [];
	if (!Array.isArray(rows)||!rows.length) return out;
	var cmtField = lang==='pali' ? 'commentPli' : lang==='eng' ? 'commentEn' : 'commentVie';
	var buf = '', bufKey = null;
	var bufComments = [];
	var bufSegCount = 0;
	var flush = function () {
		var text = (buf||'').trim();
		if (!text) { buf=''; bufKey=null; bufComments=[]; bufSegCount=0; return; }
		var r = { key: bufKey||'', pali:'', eng:'', vie:'', _merged: true };
		if (lang==='pali') r.pali=text;
		if (lang==='eng') r.eng=text;
		if (lang==='vie') r.vie=text;
		if (bufComments.length) r._mergedComments = bufComments.slice();
		out.push(r); buf=''; bufKey=null; bufComments=[]; bufSegCount=0;
	};
	for (var i = 0; i < rows.length; i++) {
		var r = rows[i];
		var key = String(r.key||'');
		var raw = lang==='pali'?(r.pali||''):lang==='eng'?(r.eng||''):(r.vie||'');
		var t = (raw||'').trim();
		if (!t) continue;
		var cmtText = (r[cmtField] || '').trim();
		if (/:0\.1$/.test(key)) continue;
		if (isNumberedHeadingLine(t)) {
			flush();
			var rr = { key: key, pali:'', eng:'', vie:'', _merged: true, _isHeading: true };
			if (lang==='pali') rr.pali=t;
			if (lang==='eng') rr.eng=t;
			if (lang==='vie') rr.vie=t;
			if (cmtText) rr._mergedComments = [{ segKey: key, text: cmtText }];
			out.push(rr); continue;
		}
		if (/:0\.3$/.test(key)) {
			flush();
			var ss = { key: key, pali:'', eng:'', vie:'' };
			if (lang==='pali') ss.pali=t;
			if (lang==='eng') ss.eng=t;
			if (lang==='vie') ss.vie=t;
			if (cmtText) ss._mergedComments = [{ segKey: key, text: cmtText }];
			out.push(ss); continue;
		}
		if (!buf) { buf=t; bufKey=key; } else buf+=' '+t;
		bufSegCount++;
		if (cmtText) {
			bufComments.push({ segKey: key, text: cmtText });
			flush();
			continue;
		}
		if (s.paragraphBreak && bufSegCount >= s.PARAGRAPH_BREAK_LEN) flush();
	}
	flush(); return out;
};

var VIEW_CACHE = new Map();
var VIEW_CACHE_MAX = 30;

SA.buildViewData = function (rowsRaw, lang, isAN, isSN) {
	var rows = lang ? SA.mergeRowsToParagraphRows(rowsRaw, lang) : rowsRaw;
	var keyIdx = Object.create(null);
	var vagga = [];
	var sutta = [];
	var rawByKey = Object.create(null);
	for (var j = 0; j < rowsRaw.length; j++) {
		rawByKey[String(rowsRaw[j].key || '')] = rowsRaw[j];
	}
	for (var i = 0; i < rows.length; i++) {
		var k = String(rows[i].key || '');
		keyIdx[k] = i;
		if ((isAN || isSN) && /:0\.2$/.test(k)) {
			var rv = rawByKey[k] || rows[i];
			vagga.push({ rowIdx: i, titleVi: (rv.vie || '').trim(), titleEn: (rv.eng || '').trim(), titlePali: (rv.pali || '').trim() });
		}
		if (isSN && /:0\.3$/.test(k)) {
			var rs = rawByKey[k] || rows[i];
			sutta.push({ rowIdx: i, titleVi: (rs.vie || '').trim(), titleEn: (rs.eng || '').trim(), titlePali: (rs.pali || '').trim() });
		}
	}
	return { rows: rows, keyToRowIdx: keyIdx, vaggaMarkers: vagga, suttaMarkers: sutta };
};

SA.getViewData = function (id, rowsRaw, lang, isAN, isSN) {
	var cacheKey = id + '|' + (lang || 'multi');
	if (VIEW_CACHE.has(cacheKey)) {
		var cached = VIEW_CACHE.get(cacheKey);
		VIEW_CACHE.delete(cacheKey);
		VIEW_CACHE.set(cacheKey, cached);
		return cached;
	}
	var v = SA.buildViewData(rowsRaw, lang, isAN, isSN);
	VIEW_CACHE.set(cacheKey, v);
	while (VIEW_CACHE.size > VIEW_CACHE_MAX) {
		var firstKey = VIEW_CACHE.keys().next().value;
		VIEW_CACHE.delete(firstKey);
	}
	return v;
};

SA.saveViewPrefs = function () {
	var s = SA.state;
	var d = SA.dom;
	storage.set(SA.KEY_VIEW, JSON.stringify({
		showPali: s.showPali, showEng: s.showEng, showVie: s.showVie,
		stack: d.card ? d.card.classList.contains('stack') : false,
		showSegKey: s.showSegKey, showColHdr: s.showColHdr,
		showCmtPli: s.showCmtPli, showCmtEng: s.showCmtEng, showCmtVie: s.showCmtVie,
		hlPli: s.hlPli, hlEng: s.hlEng, hlVie: s.hlVie,
		show3Cols: s.show3Cols
	}));
};

SA.loadViewPrefs = function () {
	try {
		var raw = storage.get(SA.KEY_VIEW);
		if (!raw) return;
		var v = JSON.parse(raw);
		var s = SA.state;
		var d = SA.dom;
		if (typeof v.showPali === 'boolean') s.showPali = v.showPali;
		if (typeof v.showEng  === 'boolean') s.showEng  = v.showEng;
		if (typeof v.showVie  === 'boolean') s.showVie  = v.showVie;
		if (d.card && typeof v.stack === 'boolean') d.card.classList.toggle('stack', v.stack);
		if (typeof v.showSegKey === 'boolean') s.showSegKey = v.showSegKey;
		if (typeof v.showColHdr === 'boolean') s.showColHdr = v.showColHdr;
		if (typeof v.showCmtPli === 'boolean') s.showCmtPli = v.showCmtPli;
		if (typeof v.showCmtEng === 'boolean') s.showCmtEng = v.showCmtEng;
		if (typeof v.showCmtVie === 'boolean') s.showCmtVie = v.showCmtVie;
		if (typeof v.hlPli === 'boolean') s.hlPli = v.hlPli;
		if (typeof v.hlEng === 'boolean') s.hlEng = v.hlEng;
		if (typeof v.hlVie === 'boolean') s.hlVie = v.hlVie;
		if (typeof v.show3Cols === 'boolean') s.show3Cols = v.show3Cols;
	} catch(e){}
};

var DOM_MODE_CACHE = new Map();
var DOM_MODE_CACHE_MAX = (function () {
	var mem = navigator.deviceMemory;
	if (mem && mem < 2) return 2;
	if (mem && mem < 4) return 3;
	return 6;
})();

function _dmCacheKey(id, mode) { return id + '|' + (mode || 'multi'); }

function _dmSnapshotCurrent() {
	var s = SA.state;
	var d = SA.dom;
	if (!s.currentSutraId || !s.virtChunks || !s.virtChunks.length) return null;
	return {
		chunks: s.virtChunks.slice(),
		rows: s.virtAllRows,
		cachedRows: s.cachedRows.slice(),
		keyToRowIdx: s.keyToRowIdx,
		vaggaMarkers: s.vaggaMarkers,
		suttaMarkers: s.suttaMarkers,
		isAN: s.isAN, isSN: s.isSN,
		fallbackTitle: s.fallbackTitle,
		scrollTop: d.grid ? d.grid.scrollTop : 0,
		scrollKey: s.firstVisibleKey
	};
}

function _dmReleaseSnap(snap) {
	if (!snap || !snap.chunks) return;
	for (var ci = 0; ci < snap.chunks.length; ci++) {
		var c = snap.chunks[ci];
		if (c && c.div && c.materialized) {
			while (c.div.firstChild) c.div.removeChild(c.div.firstChild);
			c.materialized = false;
		}
	}
}

SA._dmSaveCurrent = function () {
	var s = SA.state;
	if (!s.currentSutraId) return;
	var mode = s.lastSingleLangMode || 'multi';
	var snap = _dmSnapshotCurrent();
	if (!snap) return;
	var k = _dmCacheKey(s.currentSutraId, mode);
	DOM_MODE_CACHE.delete(k); DOM_MODE_CACHE.set(k, snap);
	while (DOM_MODE_CACHE.size > DOM_MODE_CACHE_MAX) {
		var oldestKey = DOM_MODE_CACHE.keys().next().value;
		var evicted = DOM_MODE_CACHE.get(oldestKey);
		_dmReleaseSnap(evicted);
		DOM_MODE_CACHE.delete(oldestKey);
	}
};

SA._dmInvalidateForSutta = function (id) {
	if (!id) return;
	var keys = [];
	DOM_MODE_CACHE.forEach(function (_v, k) { if (k.indexOf(id + '|') === 0) keys.push(k); });
	keys.forEach(function (k) { _dmReleaseSnap(DOM_MODE_CACHE.get(k)); DOM_MODE_CACHE.delete(k); });
};

SA._dmTryRestore = function (targetMode) {
	var s = SA.state;
	var d = SA.dom;
	if (!s.currentSutraId || !d.grid) return false;
	var k = _dmCacheKey(s.currentSutraId, targetMode);
	var snap = DOM_MODE_CACHE.get(k);
	if (!snap) return false;
	SA._dmSaveCurrent();
	DOM_MODE_CACHE.delete(k); DOM_MODE_CACHE.set(k, snap);
	if (s.anchorObserver) { s.anchorObserver.disconnect(); s.anchorObserver = null; }
	SA.teardownChunkObservers();
	while (d.grid.firstChild) d.grid.removeChild(d.grid.firstChild);
	s.virtChunks = snap.chunks;
	s.virtAllRows = snap.rows;
	s.cachedRows = snap.cachedRows;
	s.keyToRowIdx = snap.keyToRowIdx;
	s.vaggaMarkers = snap.vaggaMarkers;
	s.suttaMarkers = snap.suttaMarkers;
	s.isAN = snap.isAN; s.isSN = snap.isSN;
	s.fallbackTitle = snap.fallbackTitle;
	s.lastAppliedVaggaIdx = -2; s.lastAppliedSuttaIdx = -2;
	var frag = document.createDocumentFragment();
	for (var i = 0; i < snap.chunks.length; i++) frag.appendChild(snap.chunks[i].div);
	d.grid.appendChild(frag);
	SA.applyVisibility();
	SA.setupChunkObservers();
	SA.setupAnchorObserver();
	if (typeof snap.scrollTop === 'number') d.grid.scrollTop = snap.scrollTop;
	s.firstVisibleKey = snap.scrollKey || null;
	s.lastSingleLangMode = targetMode;
	return true;
};

var _modeRerenderTimer = null;
SA.maybeRerenderIfModeChanged = function () {
	var s = SA.state;
	var mode = SA.getSingleVisibleLang();
	if (mode === s.lastSingleLangMode) return;
	clearTimeout(_modeRerenderTimer);
	_modeRerenderTimer = setTimeout(function () {
		_modeRerenderTimer = null;
		var modeNow = SA.getSingleVisibleLang();
		if (modeNow === s.lastSingleLangMode) return;
		if (SA._dmTryRestore(modeNow)) return;
		if (s.currentSutraId) SA.renderSutra(s.currentSutraId);
	}, 180);
};
})();

/* ================================================================
   ui.js — Module [4/8]
   Giao diện: mở/đóng panel, settings, guide dialog, zoom,
   line-height, bật/tắt cột ngôn ngữ, segment key/header visibility,
   reading progress toggle, UI language rendering.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.togglePanel, SA.closePanels, SA.openGuide,
            SA.closeGuide, SA.renderUiLangFlag,
            SA.applyUiLanguageToSearchUi,
            SA.applyUiLanguageToSettingsPanel,
            SA.renderGuideDialog, SA.updateMenuPanelTop,
            SA.applySegKeyHdrVis, SA.updateVisibleCols,
            SA.applyVisibility, SA.preserveTopAndSave,
            SA.loadZoom, SA.loadLineHeight, SA.syncCmtButtons
   Depends: utils.js, state.js
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;
var $ = SA.$;
var storage = SA.storage;
var escapeHtml = SA.escapeHtml;
var escapeAttr = SA.escapeAttr;
var safeDomId = SA.safeDomId;
var debounce = SA.debounce;
var safeCssEscape = SA.safeCssEscape;
var s = SA.state;
var d = SA.dom;

var FLAG_VI = '<span class="lang-letters">VN</span>';
var FLAG_EN = '<span class="lang-letters">EN</span>';

function renderUiLangFlag() {
	if (!d.btnUiLang) return;
	d.btnUiLang.innerHTML = s.uiLang === 'en' ? FLAG_EN : FLAG_VI;
	d.btnUiLang.setAttribute('aria-label', s.uiLang === 'en'
		? 'Interface: English — click to switch to Vietnamese'
		: 'Giao diện: Tiếng Việt — bấm để chuyển sang English');
}
function applyUiLanguageToSearchUi() {
	if (!d.searchInput) return;
	d.searchInput.placeholder = s.uiLang === 'en' ? 'Search sutta...' : 'Tìm bài kinh...';
}
function applyUiLanguageToSettingsPanel() {
	var isEn = s.uiLang === 'en';
	var setText = function (id, text) { var el=$(id); if(el) el.textContent=text; };
	setText('settingsTitle',          isEn ? 'Settings'            : 'Tuỳ chỉnh');
	setText('settingsLangLabel',      isEn ? 'Languages'           : 'Ngôn ngữ');
	setText('settingsLangSub',        isEn ? 'Show / hide columns' : 'Hiện / ẩn cột');
	setText('settingsHlLabel',        isEn ? 'Emphasize'           : 'Nổi bật');
	setText('settingsHlSub',          isEn ? 'Italic + darker ink' : 'In nghiêng + đậm');
	setText('settingsLayoutSub',      isEn ? 'Display options'     : 'Cách hiển thị');
	setText('settingsCmtLabel',       isEn ? 'Commentary'          : 'Chú giải');
	setText('settingsCmtSub',         isEn ? 'Show / hide by lang' : 'Hiện / ẩn theo ngôn ngữ');
	setText('settingsLayoutLabel',    isEn ? 'Layout'              : 'Bố cục');
	setText('settingsDisplayLabel',   isEn ? 'Display'             : 'Hiển thị');
	setText('settingsFontSizeLabel',  isEn ? 'Font size'           : 'Cỡ chữ');
	setText('settingsLineHeightLabel',isEn ? 'Line spacing'        : 'Giãn dòng');
	setText('settingsTtsTitle',       isEn ? 'Read aloud'          : 'Đọc kinh');
	setText('settingsTtsUiLabel',     isEn ? 'Text-to-Speech'      : 'Text-to-Speech');
	setText('settingsFullWidthLabel', isEn ? 'Full width'          : 'Toàn màn hình');
	var note = $('settingsTtsNote');
	if (note) note.innerHTML = isEn
		? '* Uses browser built-in voices, quality may vary by device.'
		: '* TTS dùng giọng có sẵn của trình duyệt, có thể khác nhau giữa thiết bị.';
	if (d.btnLayout) d.btnLayout.innerHTML = isEn
		? '<span class="pill-icon">☰</span> Stacked'
		: '<span class="pill-icon">☰</span> Xếp dọc';
	var _btn3C = $('btn3Cols');
	if (_btn3C) _btn3C.innerHTML = isEn
		? '<span class="pill-icon">⫴</span> 3 columns'
		: '<span class="pill-icon">⫴</span> 3 cột ngang';
	var _btnCM = $('btnCmtMaster');
	if (_btnCM) _btnCM.innerHTML = isEn
		? '<span class="pill-icon">💬</span> Commentary'
		: '<span class="pill-icon">💬</span> Chú giải';
	var _btnCP = $('btnCmtPli');
	if (_btnCP) _btnCP.innerHTML = '<span class="pill-icon">💬</span> Pāli';
	var _btnCE = $('btnCmtEng');
	if (_btnCE) _btnCE.innerHTML = '<span class="pill-icon">💬</span> Eng';
	var _btnCV = $('btnCmtVie');
	if (_btnCV) _btnCV.innerHTML = isEn
		? '<span class="pill-icon">💬</span> Viet'
		: '<span class="pill-icon">💬</span> Việt';
	var btnFW = $('btnFullWidth');
	if (btnFW) btnFW.innerHTML = isEn
		? '<span class="pill-icon">⛶</span> Full width'
		: '<span class="pill-icon">⛶</span> Toàn màn hình';
	if (d.btnGuide)     d.btnGuide.setAttribute('aria-label',     isEn ? 'User guide'       : 'Hướng dẫn sử dụng');
	if (d.btnSutraMenu) d.btnSutraMenu.setAttribute('aria-label', isEn ? 'Sutta Index'      : 'Danh mục bài kinh');
	if (d.btnSettings)  d.btnSettings.setAttribute('aria-label',  isEn ? 'Display settings'  : 'Cài đặt hiển thị');
	if (d.btnBackTop)   d.btnBackTop.setAttribute('aria-label',   isEn ? 'Back to top'       : 'Lên đầu trang');
	if (d.btnPauseTts)  d.btnPauseTts.setAttribute('aria-label',
		isEn ? 'Pause (current sentence will restart)' : 'Tạm dừng (câu hiện tại sẽ đọc lại từ đầu)');
	var sideLabel = document.querySelector('#sidebar-btn .sidebar-label');
	if (sideLabel) sideLabel.textContent = isEn ? 'Library' : 'Thư viện';
	setText('btnSettingsLabel', isEn ? 'Settings' : 'Cài đặt');
	setText('btnGuideLabel',    isEn ? 'Guide'    : 'Hướng dẫn');
}

function renderGuideDialog() {
	if (!d.guideOverlay) return;
	var dlg = d.guideOverlay.querySelector('.guide-dialog');
	if (!dlg) return;
	var isEn = s.uiLang === 'en';
	var viHtml =
	'<h2>Hướng dẫn sử dụng</h2>' +
	'<h3>📖 Thư viện bài kinh</h3><ul><li>Bấm <strong>Thư viện</strong> ở giữa footer để mở danh sách kinh. Hoặc nhập vào <strong>Tìm kiếm</strong> để tìm bài kinh.</li></ul>' +
	'<h3>⭐ Đánh dấu (Bookmark)</h3><ul><li>Bấm <strong>☆</strong> cạnh tiêu đề bài đang đọc (góc trên-trái) để lưu / bỏ lưu bài kinh yêu thích.</li><li>Tile <strong>★ Đã lưu</strong> hiện số bài yêu thích đã lưu.</li></ul>' +
	'<h3>📜 Đọc kinh</h3><ul><li>Nút <strong>‹ TRƯỚC / SAU ›</strong>: dùng chuyển bài kinh.</li><li><strong>⬆</strong>: về đầu bài kinh.</li><li>Thanh tiến độ đọc dọc bên trái + badge <code>%</code> góc dưới-phải: cho biết đã đọc tới đâu. Tắt/bật trong Cài đặt (nút <code>▮ %</code>).</li></ul>' +
	'<h3>🔗 Chia sẻ & Sao chép</h3><ul><li><strong>🔗 Share đầu bài</strong> (góc trên-phải): chia sẻ link bài kinh.</li><li><strong>🔗 Share đoạn</strong> (icon nhỏ cạnh mã đoạn): chia sẻ link đến đúng đoạn đó.</li><li><strong>📋 Copy</strong> cạnh label <code>PĀLI</code> / <code>ENGLISH</code> / <code>VIỆT</code>: sao chép văn bản của cột đó cho đoạn hiện tại.</li></ul>' +
	'<h3>⚙ Cài đặt</h3><ul><li><strong>Giao diện</strong>: <strong>🌙/☀</strong> tối/sáng · <strong>VN/EN</strong> ngôn ngữ giao diện · <strong>🖨</strong> in / lưu PDF bài kinh hiện tại. Lưu ý: bài kinh dài (hàng ngàn đoạn) có thể mất vài giây đến vài chục giây để chuẩn bị — vui lòng đợi.</li><li><strong>Ngôn ngữ</strong>: bật/tắt cột <code>PĀLI</code> · <code>ENGLISH</code> · <code>VIỆT</code>.</li><li><strong>Bố cục</strong>: <code>☰ Xếp dọc</code> — stack 3 cột · <code># Segment</code> — ẩn/hiện mã đoạn · <code>▦ Label</code> — ẩn/hiện nhãn cột.</li><li><strong>Cỡ chữ</strong>: slider 80–160% (chỉ áp cho nội dung). <strong>Giãn dòng</strong>: 1.3–2.6.</li><li><strong>↺ A</strong> / <strong>↺ ☰</strong>: reset cỡ chữ / giãn dòng về mặc định.</li><li><strong>▮ %</strong>: bật/tắt thanh tiến độ đọc (dọc bên trái + badge phần trăm).</li><li><strong>🐞</strong>: debug.</li></ul>' +
	'<h3>🔊 Đọc to (TTS)</h3><ul><li><strong>▶ Play</strong>: đọc kinh theo ngôn ngữ giao diện (Việt hoặc Anh). Pāli chưa hỗ trợ.</li><li><strong>⏸ Pause</strong>: giới hạn trình duyệt — khi tiếp tục sẽ đọc lại câu hiện tại từ đầu.</li><li><strong>⏹ Stop</strong>: dừng hẳn, lần sau Play đọc từ đầu bài.</li><li><em>*Một số thiết bị không hỗ trợ sẽ không đọc được.</em></li></ul>' +
	'<h3>ℹ Nguồn</h3><p>Văn bản Pāli + bản dịch tiếng Anh Bhikkhu Sujato từ <a href="https://suttacentral.net/" target="_blank" rel="noopener">SuttaCentral</a> (dự án Bilara). Bản dịch tiếng Việt biên tập từ nhiều nguồn, có thể còn sai sót — vui lòng đối chiếu bản Pāli và tiếng Anh.</p><p>Góp ý, báo lỗi: <a href="mailto:tuanctvn199@gmail.com">tuanctvn199@gmail.com</a></p><p><a href="about.html" target="_blank" rel="noopener">Giới thiệu &amp; Điều khoản</a></p><button id="btnCloseGuide" type="button">Đóng</button>';
	var enHtml =
	'<h2>User Guide</h2>' +
	'<h3>📖 Sutta Library</h3><ul><li>Tap <strong>Library</strong> in the footer center to open the sutta list. Or use <strong>Search</strong> to find a sutta.</li></ul>' +
	'<h3>⭐ Bookmarks</h3><ul><li>Tap <strong>☆</strong> next to the current sutta title (top-left) to save / unsave a favorite sutta.</li><li>The <strong>★ Saved</strong> tile shows the count of saved suttas.</li></ul>' +
	'<h3>📜 Reading</h3><ul><li><strong>‹ PREV / NEXT ›</strong> buttons: navigate between suttas.</li><li><strong>⬆</strong>: jump to the top of the sutta.</li><li>Reading progress bar (left edge) + <code>%</code> badge (bottom-right): shows how far you have read. Toggle in Settings (<code>▮ %</code>).</li></ul>' +
	'<h3>🔗 Share & Copy</h3><ul><li><strong>🔗 Title share</strong> (top-right): share link to the sutta.</li><li><strong>🔗 Segment share</strong> (small icon next to segment ID): share link to that exact segment.</li><li><strong>📋 Copy</strong> next to <code>PĀLI</code> / <code>ENGLISH</code> / <code>VIỆT</code> labels: copy the text of that column for the current segment.</li></ul>' +
	'<h3>⚙ Settings</h3><ul><li><strong>Interface</strong>: <strong>🌙/☀</strong> dark/light · <strong>VN/EN</strong> interface language · <strong>🖨</strong> print / save current sutta to PDF. Note: long suttas (thousands of segments) may take a few seconds to tens of seconds to prepare — please wait.</li><li><strong>Languages</strong>: toggle <code>PĀLI</code> · <code>ENGLISH</code> · <code>VIỆT</code> columns.</li><li><strong>Layout</strong>: <code>☰ Stack</code> — stack 3 columns · <code># Segment</code> — show/hide segment IDs · <code>▦ Label</code> — show/hide column headers.</li><li><strong>Font size</strong>: slider 80–160% (body text only). <strong>Line height</strong>: 1.3–2.6.</li><li><strong>↺ A</strong> / <strong>↺ ☰</strong>: reset font size / line height.</li><li><strong>▮ %</strong>: toggle reading progress bar.</li><li><strong>🐞</strong>: debug.</li></ul>' +
	'<h3>🔊 Text-to-Speech (TTS)</h3><ul><li><strong>▶ Play</strong>: reads in UI language (VI or EN). Pāli not supported.</li><li><strong>⏸ Pause</strong>: browser limitation — resume restarts the current sentence.</li><li><strong>⏹ Stop</strong>: stops entirely; next Play starts from the beginning.</li><li><em>*Some devices may not support TTS and won\'t read.</em></li></ul>' +
	'<h3>ℹ Sources</h3><p>Pāli text and Bhikkhu Sujato English translations from <a href="https://suttacentral.net/" target="_blank" rel="noopener">SuttaCentral</a> (Bilara project). Vietnamese translations compiled from multiple sources — please cross-reference with Pāli and English originals.</p><p>Feedback / bug reports: <a href="mailto:tuanctvn199@gmail.com">tuanctvn199@gmail.com</a></p><p><a href="about.html" target="_blank" rel="noopener">About &amp; Terms</a></p><button id="btnCloseGuide" type="button">Close</button>';
	dlg.innerHTML = isEn ? enHtml : viHtml;
	var btnClose = $('btnCloseGuide');
	if (btnClose) btnClose.onclick = closeGuide;
}

function openGuide() {
	if (!d.guideOverlay) return;
	renderGuideDialog();
	d.guideOverlay.classList.add('show');
	d.guideOverlay.setAttribute('aria-hidden', 'false');
	var dialog = d.guideOverlay.querySelector('.guide-dialog');
	if (dialog) dialog.scrollTop = 0;
	setTimeout(function () { var b = $('btnCloseGuide'); if (b) b.focus({ preventScroll: true }); }, 50);
}
function closeGuide() {
	if (!d.guideOverlay) return;
	d.guideOverlay.classList.remove('show');
	d.guideOverlay.setAttribute('aria-hidden', 'true');
	if (d.btnGuide) d.btnGuide.focus();
}

var resizeObserver = new ResizeObserver(function (entries) {
	for (var i = 0; i < entries.length; i++) {
		requestAnimationFrame(function () {
			if (d.sutraMenuPanel) {
				d.sutraMenuPanel.style.top = entries[0].contentRect.height + 'px';
			}
		});
	}
});

function updateMenuPanelTop() {
	if (!d.card) return;
	var topNote = d.card.querySelector('.top-note');
	if (topNote) {
		resizeObserver.disconnect();
		resizeObserver.observe(topNote);
	} else if (d.sutraMenuPanel) {
		d.sutraMenuPanel.style.top = '0px';
	}
}

function togglePanel(panel, force) {
	if (!panel) return;
	var isOpen = typeof force === 'boolean' ? force : !panel.classList.contains('open');
	if (!isOpen && panel.contains(document.activeElement)) {
		try {
			var triggerBtn = (panel === d.settingsPanel) ? d.btnSettings
			              : (panel === d.sutraMenuPanel) ? d.btnSutraMenu
			              : null;
			if (triggerBtn) triggerBtn.focus();
			else document.activeElement.blur();
		} catch(_){}
	}
	try {
		panel.classList.toggle('open', isOpen);
		if (isOpen) {
			panel.setAttribute('aria-hidden', 'false');
			panel.removeAttribute('inert');
		} else {
			panel.setAttribute('aria-hidden', 'true');
			panel.setAttribute('inert', '');
		}
	} finally {
		if (panel === d.settingsPanel && d.btnSettings) {
			d.btnSettings.setAttribute('aria-expanded', String(isOpen));
			d.btnSettings.classList.toggle('active', isOpen);
			if (!isOpen) _clearStickyHover(d.btnSettings);
			document.body.classList.toggle('settings-open', isOpen);
		}
		if (panel === d.sutraMenuPanel && d.btnSutraMenu) {
			d.btnSutraMenu.setAttribute('aria-expanded', String(isOpen));
			d.btnSutraMenu.classList.toggle('is-open', isOpen);
			if (!isOpen) _clearStickyHover(d.btnSutraMenu);
		}
	}
}
function _clearStickyHover(btn) {
	if (!btn) return;
	try { btn.blur(); } catch(_){}
	btn.classList.add('no-hover');
	setTimeout(function () { btn.classList.remove('no-hover'); }, 280);
}
function positionSettingsPanel() {
	if (!d.settingsPanel || !d.btnSettings) return;
	var r = d.btnSettings.getBoundingClientRect();
	var footer = document.querySelector('.status');
	var footerH = footer ? footer.offsetHeight : (window.innerHeight - r.top);
	d.settingsPanel.style.left   = r.left + 'px';
	d.settingsPanel.style.bottom = (footerH + 8) + 'px';
	d.settingsPanel.style.top    = 'auto';
	d.settingsPanel.style.right  = 'auto';
}
function closePanels() {
	togglePanel(d.settingsPanel, false);
	togglePanel(d.sutraMenuPanel, false);
}

SA.togglePanel = togglePanel;
SA.closePanels = closePanels;
SA.openGuide = openGuide;
SA.closeGuide = closeGuide;
SA.renderUiLangFlag = renderUiLangFlag;
SA.applyUiLanguageToSearchUi = applyUiLanguageToSearchUi;
SA.applyUiLanguageToSettingsPanel = applyUiLanguageToSettingsPanel;
SA.renderGuideDialog = renderGuideDialog;
SA.updateMenuPanelTop = updateMenuPanelTop;
SA.positionSettingsPanel = positionSettingsPanel;

// Wire up panel buttons
if (d.btnSutraMenu) {
	d.btnSutraMenu.onclick = function (e) {
		e.stopPropagation();
		var willOpen = !d.sutraMenuPanel.classList.contains('open');
		togglePanel(d.settingsPanel, false);
		togglePanel(d.sutraMenuPanel, willOpen);
		if (willOpen && d.sutraMenuList && d.sutraMenuList.children.length === 0) {
			var nikKey = s.activeNikayaKey || (window.SUTRA_INDEX && window.SUTRA_INDEX[0] && window.SUTRA_INDEX[0].key);
			if (nikKey) {
				if (!window.SUTRA_INDEX || !window.SUTRA_INDEX.length) return;
				if (!s.FLAT_SUTTAS.length) SA.buildSutraMenuFromIndex();
				else SA.setActiveNikaya(nikKey, false);
			}
		}
	};
}
var menuCloseBtn = $('menuCloseBtn');
if (menuCloseBtn) {
	menuCloseBtn.onclick = function (e) {
		e.stopPropagation();
		togglePanel(d.sutraMenuPanel, false);
	};
}
if (d.btnSettings) {
	d.btnSettings.onclick = function (e) {
		e.stopPropagation();
		var willOpen = !d.settingsPanel.classList.contains('open');
		if (willOpen) {
			togglePanel(d.sutraMenuPanel, false);
			positionSettingsPanel();
		}
		togglePanel(d.settingsPanel, willOpen);
		SA._blurIfMouse(d.btnSettings);
	};
}
var btnSettingsClose = $('btnSettingsClose');
if (btnSettingsClose) {
	btnSettingsClose.onclick = function (e) {
		e.stopPropagation();
		togglePanel(d.settingsPanel, false);
	};
}
if (d.btnGuide && d.guideOverlay) {
	d.btnGuide.onclick = function (e) {
		e.stopPropagation();
		openGuide();
	};
}
var dedTextEl = $('dedicationText');
if (dedTextEl) {
	dedTextEl.classList.add('ded-clickable');
	dedTextEl.setAttribute('title', s.uiLang === 'en' ? 'Double-click to return to home' : 'Nháy đôi để về trang chủ');
	dedTextEl.addEventListener('dblclick', function (e) {
		e.preventDefault();
		try { storage.remove(SA.KEY_LAST); } catch(_) {}
		location.replace(location.pathname + location.search);
	});
}
if (d.guideOverlay) {
	d.guideOverlay.addEventListener('click', function (e) {
		if (e.target === d.guideOverlay) closeGuide();
	});
}
document.addEventListener('click', function (e) {
	var t = e.target;
	if (d.sutraMenuPanel && d.sutraMenuPanel.contains(t)) return;
	if (d.settingsPanel && d.settingsPanel.contains(t)) return;
	if (d.btnSutraMenu && d.btnSutraMenu.contains(t)) return;
	if (d.guideOverlay && d.guideOverlay.contains(t)) return;
	closePanels();
});
document.addEventListener('keydown', function (e) {
	if (e.key === 'Escape') {
		if (d.guideOverlay && d.guideOverlay.classList.contains('show')) return closeGuide();
		closePanels();
	}
});

function applySegKeyHdrVis() {
	if (!d.grid) return;
	d.grid.classList.toggle('hide-seg-key', !s.showSegKey);
	d.grid.classList.toggle('hide-col-header', !s.showColHdr);
	d.grid.classList.toggle('hide-cmt-pli', !s.showCmtPli);
	d.grid.classList.toggle('hide-cmt-eng', !s.showCmtEng);
	d.grid.classList.toggle('hide-cmt-vie', !s.showCmtVie);
	d.grid.classList.toggle('hl-pli', !!s.hlPli);
	d.grid.classList.toggle('hl-eng', !!s.hlEng);
	d.grid.classList.toggle('hl-vie', !!s.hlVie);
	if (d.card) d.card.classList.toggle('grid-3cols', !!s.show3Cols);
}
SA.applySegKeyHdrVis = applySegKeyHdrVis;

var mql = window.matchMedia('(max-width: 500px)');
function updateVisibleCols() {
	var isNarrow = mql.matches;
	var isStack = d.card ? d.card.classList.contains('stack') : false;
	var count = (s.showPali ? 1 : 0) + (s.showEng ? 1 : 0) + (s.showVie ? 1 : 0);
	count = Math.max(1, count);
	requestAnimationFrame(function () {
		document.documentElement.style.setProperty('--visible-cols', isNarrow || isStack ? '1' : String(count));
	});
}
mql.addEventListener('change', updateVisibleCols);
SA.updateVisibleCols = updateVisibleCols;

function applyVisibility() {
	if (!d.grid) return;
	d.grid.classList.toggle('hide-pali', !s.showPali);
	d.grid.classList.toggle('hide-eng',  !s.showEng);
	d.grid.classList.toggle('hide-vie',  !s.showVie);
	updateVisibleCols();
}
SA.applyVisibility = applyVisibility;

window.addEventListener('resize', function () { updateVisibleCols(); updateMenuPanelTop(); });

var _langDepsBackup = { pli: null, eng: null, vie: null };
function _applyHlBtnUi(lang) {
	var on = lang === 'pli' ? s.hlPli : lang === 'eng' ? s.hlEng : s.hlVie;
	var id = lang === 'pli' ? 'btnHlPli' : lang === 'eng' ? 'btnHlEng' : 'btnHlVie';
	var b = $(id);
	if (b) { b.classList.toggle('active', !!on); b.setAttribute('aria-pressed', String(!!on)); }
}
function syncCmtButtons() {
	var pairs = [['btnCmtPli', s.showCmtPli], ['btnCmtEng', s.showCmtEng], ['btnCmtVie', s.showCmtVie]];
	for (var i = 0; i < pairs.length; i++) {
		var b = $(pairs[i][0]); if (!b) continue;
		b.classList.toggle('active', pairs[i][1]);
		b.setAttribute('aria-pressed', String(pairs[i][1]));
	}
	var m = $('btnCmtMaster');
	if (m) {
		var any = s.showCmtPli || s.showCmtEng || s.showCmtVie;
		m.classList.toggle('active', any);
		m.setAttribute('aria-pressed', String(any));
	}
}
SA.syncCmtButtons = syncCmtButtons;

function _syncDepsOnLangHide(lang) {
	if (lang === 'pli') { _langDepsBackup.pli = { hl: s.hlPli, cmt: s.showCmtPli }; s.hlPli = false; s.showCmtPli = false; }
	else if (lang === 'eng') { _langDepsBackup.eng = { hl: s.hlEng, cmt: s.showCmtEng }; s.hlEng = false; s.showCmtEng = false; }
	else if (lang === 'vie') { _langDepsBackup.vie = { hl: s.hlVie, cmt: s.showCmtVie }; s.hlVie = false; s.showCmtVie = false; }
	_applyHlBtnUi(lang); syncCmtButtons(); applySegKeyHdrVis();
}
function _syncDepsOnLangShow(lang) {
	var bak = _langDepsBackup[lang];
	if (!bak) return;
	if (lang === 'pli')      { s.hlPli = !!bak.hl; s.showCmtPli = !!bak.cmt; }
	else if (lang === 'eng') { s.hlEng = !!bak.hl; s.showCmtEng = !!bak.cmt; }
	else if (lang === 'vie') { s.hlVie = !!bak.hl; s.showCmtVie = !!bak.cmt; }
	_langDepsBackup[lang] = null;
	_applyHlBtnUi(lang); syncCmtButtons(); applySegKeyHdrVis();
}

function preserveTopAndSave(action) {
	var topKey = SA.computeTopVisibleKey();
	if (SA._saveAnchorDebounced && SA._saveAnchorDebounced.cancel) SA._saveAnchorDebounced.cancel();
	if (topKey && s.currentSutraId) {
		s._progScrollUntil = Date.now() + 1500;
		s.firstVisibleKey = topKey;
		SA.anchorSet(SA.KEY_ANCHOR_K(s.currentSutraId), topKey);
	}
	action();
	if (!topKey || !s.currentSutraId) return;
	requestAnimationFrame(function () {
		var scrollRoot = SA.getScrollRoot();
		if (!scrollRoot) return;
		var safeKey = safeCssEscape(topKey);
		var row = scrollRoot.querySelector('.sutra-row[data-key="' + safeKey + '"]');
		if (!row) return;
		var tgt = row.closest('.sutra-row-wrap') || row;
		var rootRect = scrollRoot.getBoundingClientRect();
		var tgtRect = tgt.getBoundingClientRect();
		var y = tgtRect.top - rootRect.top + scrollRoot.scrollTop;
		var max = Math.max(0, scrollRoot.scrollHeight - scrollRoot.clientHeight);
		var targetY = Math.max(0, Math.min(y, max));
		if (Math.abs(targetY - scrollRoot.scrollTop) > 1) {
			s._progScrollUntil = Date.now() + 1500;
			scrollRoot.scrollTop = targetY;
		}
		if (SA._saveAnchorDebounced && SA._saveAnchorDebounced.cancel) SA._saveAnchorDebounced.cancel();
	});
}
SA.preserveTopAndSave = preserveTopAndSave;

if (d.btnPali) d.btnPali.onclick = function () {
	if (s.showPali && (s.showEng || s.showVie)) {}
	else if (!s.showPali) {}
	else return;
	preserveTopAndSave(function () {
		s.showPali = !s.showPali;
		d.btnPali.classList.toggle('active', s.showPali);
		d.btnPali.setAttribute('aria-pressed', String(s.showPali));
		if (!s.showPali) _syncDepsOnLangHide('pli'); else _syncDepsOnLangShow('pli');
		applyVisibility(); SA.saveViewPrefs(); SA.maybeRerenderIfModeChanged();
	});
};
if (d.btnEng) d.btnEng.onclick = function () {
	if (s.showEng && (s.showPali || s.showVie)) {}
	else if (!s.showEng) {}
	else return;
	preserveTopAndSave(function () {
		s.showEng = !s.showEng;
		d.btnEng.classList.toggle('active', s.showEng);
		d.btnEng.setAttribute('aria-pressed', String(s.showEng));
		if (!s.showEng) _syncDepsOnLangHide('eng'); else _syncDepsOnLangShow('eng');
		applyVisibility(); SA.saveViewPrefs(); SA.maybeRerenderIfModeChanged();
	});
};
if (d.btnVie) d.btnVie.onclick = function () {
	if (s.showVie && (s.showPali || s.showEng)) {}
	else if (!s.showVie) {}
	else return;
	preserveTopAndSave(function () {
		s.showVie = !s.showVie;
		d.btnVie.classList.toggle('active', s.showVie);
		d.btnVie.setAttribute('aria-pressed', String(s.showVie));
		if (!s.showVie) _syncDepsOnLangHide('vie'); else _syncDepsOnLangShow('vie');
		applyVisibility(); SA.saveViewPrefs(); SA.maybeRerenderIfModeChanged();
	});
};
if (d.btnLayout) d.btnLayout.onclick = function () {
	preserveTopAndSave(function () {
		if (d.card) d.card.classList.toggle('stack');
		var isStack = d.card ? d.card.classList.contains('stack') : false;
		d.btnLayout.classList.toggle('active', isStack);
		d.btnLayout.setAttribute('aria-pressed', String(isStack));
		if (isStack && s.show3Cols) {
			s.show3Cols = false;
			if (d.card) d.card.classList.remove('grid-3cols');
			var _b3c = $('btn3Cols');
			if (_b3c) { _b3c.classList.remove('active'); _b3c.setAttribute('aria-pressed', 'false'); }
		}
		updateVisibleCols(); SA.saveViewPrefs();
	});
};
var btnSegKey = $('btnSegKey');
if (btnSegKey) btnSegKey.onclick = function () {
	preserveTopAndSave(function () {
		s.showSegKey = !s.showSegKey;
		btnSegKey.classList.toggle('active', s.showSegKey);
		btnSegKey.setAttribute('aria-pressed', String(s.showSegKey));
		applySegKeyHdrVis(); SA.saveViewPrefs();
	});
};
var btnSegHdr = $('btnSegHdr');
if (btnSegHdr) btnSegHdr.onclick = function () {
	preserveTopAndSave(function () {
		s.showColHdr = !s.showColHdr;
		btnSegHdr.classList.toggle('active', s.showColHdr);
		btnSegHdr.setAttribute('aria-pressed', String(s.showColHdr));
		applySegKeyHdrVis(); SA.saveViewPrefs();
	});
};
function wireLangCmt(btnId, setter) {
	var btn = $(btnId); if (!btn) return;
	btn.onclick = function () {
		preserveTopAndSave(function () { setter(); syncCmtButtons(); applySegKeyHdrVis(); SA.saveViewPrefs(); });
	};
}
wireLangCmt('btnCmtPli', function(){ s.showCmtPli = !s.showCmtPli; });
wireLangCmt('btnCmtEng', function(){ s.showCmtEng = !s.showCmtEng; });
wireLangCmt('btnCmtVie', function(){ s.showCmtVie = !s.showCmtVie; });
var btnCmtMaster = $('btnCmtMaster');
if (btnCmtMaster) btnCmtMaster.onclick = function () {
	preserveTopAndSave(function () {
		var target = !(s.showCmtPli || s.showCmtEng || s.showCmtVie);
		s.showCmtPli = target; s.showCmtEng = target; s.showCmtVie = target;
		syncCmtButtons(); applySegKeyHdrVis(); SA.saveViewPrefs();
	});
};
function wireHlToggle(btnId, setter) {
	var btn = $(btnId); if (!btn) return;
	btn.onclick = function () {
		preserveTopAndSave(function () {
			setter(); applySegKeyHdrVis();
			var active = (btnId === 'btnHlPli') ? s.hlPli : (btnId === 'btnHlEng') ? s.hlEng : s.hlVie;
			btn.classList.toggle('active', active);
			btn.setAttribute('aria-pressed', String(active));
			SA.saveViewPrefs();
		});
	};
}
wireHlToggle('btnHlPli', function(){ s.hlPli = !s.hlPli; });
wireHlToggle('btnHlEng', function(){ s.hlEng = !s.hlEng; });
wireHlToggle('btnHlVie', function(){ s.hlVie = !s.hlVie; });
var btn3Cols = $('btn3Cols');
if (btn3Cols) btn3Cols.onclick = function () {
	preserveTopAndSave(function () {
		s.show3Cols = !s.show3Cols;
		btn3Cols.classList.toggle('active', s.show3Cols);
		btn3Cols.setAttribute('aria-pressed', String(s.show3Cols));
		if (d.card) d.card.classList.toggle('grid-3cols', s.show3Cols);
		if (s.show3Cols && d.card && d.card.classList.contains('stack')) {
			d.card.classList.remove('stack');
			if (d.btnLayout) { d.btnLayout.classList.remove('active'); d.btnLayout.setAttribute('aria-pressed', 'false'); }
		}
		updateVisibleCols(); SA.saveViewPrefs();
	});
};

function applyWideLayout(on) {
	s.isWide = !!on;
	document.documentElement.classList.toggle('layout-wide', s.isWide);
	if (d.btnFullWidth) {
		d.btnFullWidth.classList.toggle('active', s.isWide);
		d.btnFullWidth.setAttribute('aria-pressed', String(s.isWide));
	}
}
if (d.btnFullWidth) {
	applyWideLayout(s.isWide);
	d.btnFullWidth.addEventListener('click', function () {
		applyWideLayout(!s.isWide);
		storage.set(SA.WIDE_STORAGE_KEY, s.isWide ? '1' : '0');
	});
}

// Zoom
var ZOOM_STORAGE_KEY = 'sutra_zoom';
var LH_STORAGE_KEY   = 'sutra_line_height';
var MIN_ZOOM=0.8, MAX_ZOOM=1.6, MIN_LH=1.3, MAX_LH=2.6;
var zoomLevel=1, lineHeightLevel=1.85;
var sliderZoom       = $('sliderZoom');
var sliderLineHeight = $('sliderLineHeight');
var zoomBadge        = $('zoomValueBadge');
var lhBadge          = $('lineHeightValueBadge');
var btnZoomReset     = $('btnZoomReset');
var btnLhReset       = $('btnLineHeightReset');
function clampZoom(z) { return Math.max(MIN_ZOOM, Math.min(MAX_ZOOM, z)); }
function clampLh(v)   { return Math.max(MIN_LH,   Math.min(MAX_LH, v)); }
function updateSliderFill(el, min, max, val) {
	if (!el) return;
	el.style.setProperty('--slider-pct', ((val - min) / (max - min) * 100).toFixed(1) + '%');
}
function applyZoom() {
	document.documentElement.style.setProperty('--sutra-font-scale', String(zoomLevel));
	var pct = Math.round(zoomLevel * 100);
	if (zoomBadge) zoomBadge.textContent = pct + '%';
	if (sliderZoom) { sliderZoom.value = String(pct); updateSliderFill(sliderZoom, 80, 160, pct); }
}
function applyLineHeight() {
	document.documentElement.style.setProperty('--sutra-line-height', String(lineHeightLevel));
	if (lhBadge) lhBadge.textContent = lineHeightLevel.toFixed(2);
	if (sliderLineHeight) {
		var val = Math.round(lineHeightLevel * 100);
		sliderLineHeight.value = String(val);
		updateSliderFill(sliderLineHeight, 130, 260, val);
	}
}
function loadZoom() {
	var sv = storage.get(ZOOM_STORAGE_KEY);
	if (sv) { var v = parseFloat(sv); if (!Number.isNaN(v)) zoomLevel = clampZoom(v); }
	applyZoom();
}
function loadLineHeight() {
	var sv = storage.get(LH_STORAGE_KEY);
	if (sv) { var v = parseFloat(sv); if (!Number.isNaN(v)) lineHeightLevel = clampLh(v); }
	applyLineHeight();
}
function saveZoom()       { storage.set(ZOOM_STORAGE_KEY, String(zoomLevel)); }
function saveLineHeight() { storage.set(LH_STORAGE_KEY, String(lineHeightLevel)); }
SA.loadZoom = loadZoom;
SA.loadLineHeight = loadLineHeight;

if (sliderZoom) sliderZoom.addEventListener('input', function () {
	zoomLevel = clampZoom(parseInt(sliderZoom.value, 10) / 100); applyZoom(); saveZoom();
});
if (sliderLineHeight) sliderLineHeight.addEventListener('input', function () {
	lineHeightLevel = clampLh(parseInt(sliderLineHeight.value, 10) / 100); applyLineHeight(); saveLineHeight();
});
if (btnZoomReset) btnZoomReset.onclick = function () { zoomLevel = 1; applyZoom(); saveZoom(); };
if (btnLhReset)   btnLhReset.onclick   = function () { lineHeightLevel = 1.85; applyLineHeight(); saveLineHeight(); };

(function _wireProgressToggle() {
	var btn = $('btnProgressToggle');
	if (!btn) return;
	var KEY = 'sutra_show_progress';
	function apply(on) {
		document.documentElement.classList.toggle('hide-reading-progress', !on);
		btn.setAttribute('aria-pressed', String(!!on));
		btn.classList.toggle('active', !!on);
	}
	var saved = null;
	try { saved = storage.get(KEY); } catch(_){}
	var isOn = saved !== '0';
	apply(isOn);
	btn.addEventListener('click', function () {
		isOn = !isOn;
		apply(isOn);
		try { storage.set(KEY, isOn ? '1' : '0'); } catch(_){}
		if (isOn) try { SA.updateReadingProgress(); } catch(_){}
	});
})();
})();

/* ================================================================
   anchor.js — Module [5/8]
   Hệ thống lưu/khôi phục vị trí đọc (scroll anchor).
   IntersectionObserver theo dõi segment nào đang ở đầu viewport,
   lưu vào sessionStorage + localStorage. Khi reload hoặc mở lại
   bài kinh, khôi phục scroll đến đúng vị trí đã đọc.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.getScrollRoot, SA.setupAnchorObserver,
            SA.computeTopVisibleKey, SA.restoreScrollByAnchor,
            SA.toggleBackTop, SA._saveAnchorDebounced,
            SA.updateReadingProgress
   Depends: utils.js, state.js
   ─────────────────────────────────────────────────────────────────
   Bug fixes applied:
   1. scroll-behavior: smooth → dùng scrollTo({behavior:'instant'})
   2. Scope fallback key mismatch → track foundKey thực tế
   3. _progScrollUntil quá ngắn (700ms) → tăng lên 1200ms
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;
var $ = SA.$;
var storage = SA.storage;
var throttle = SA.throttle;
var debounce = SA.debounce;
var safeCssEscape = SA.safeCssEscape;
var s = SA.state;
var d = SA.dom;

function getScrollRoot() { return d.readerArea || d.grid; }
SA.getScrollRoot = getScrollRoot;

function findActiveMarkerIdx(markers, curIdx) {
	var idx = -1;
	for (var v = 0; v < markers.length; v++) {
		if (markers[v].rowIdx <= curIdx) idx = v;
		else break;
	}
	return idx;
}
function pickPrimaryByLang(marker) {
	return s.uiLang === 'en'
		? (marker.titleEn || marker.titlePali || marker.titleVi || '').trim()
		: (marker.titleVi || marker.titleEn || marker.titlePali || '').trim();
}
function pickAltByLang(marker) {
	return s.uiLang === 'en'
		? (marker.titleVi || marker.titlePali || '').trim()
		: (marker.titleEn || marker.titlePali || '').trim();
}

function updateDynamicTitles() {
	if (!s.isAN && !s.isSN) return;
	var curIdx = s.firstVisibleKey ? (s.keyToRowIdx[s.firstVisibleKey]) : undefined;
	if (curIdx === undefined) curIdx = -1;
	if (s.isAN) {
		var anIdx = findActiveMarkerIdx(s.vaggaMarkers, curIdx);
		if (anIdx !== s.lastAppliedSuttaIdx) {
			if (anIdx < 0) {
				if (d.titleEl) d.titleEl.textContent = s.fallbackTitle;
				if (d.titleMetaEl) d.titleMetaEl.textContent = s.fallbackTitleMeta;
			} else {
				var vmA = s.vaggaMarkers[anIdx];
				if (d.titleEl) d.titleEl.textContent = pickPrimaryByLang(vmA) || s.fallbackTitle;
				if (d.titleMetaEl) d.titleMetaEl.textContent = pickAltByLang(vmA) || s.fallbackTitleMeta;
			}
			s.lastAppliedSuttaIdx = anIdx;
		}
		return;
	}
	var vIdx = findActiveMarkerIdx(s.vaggaMarkers, curIdx);
	if (vIdx !== s.lastAppliedVaggaIdx && s.superLastSlotEl) {
		var vaggaText = vIdx < 0 ? '' : (pickPrimaryByLang(s.vaggaMarkers[vIdx]) || '');
		s.superLastSlotEl.textContent = vaggaText;
		if (s.superLastSlotEl._sep) s.superLastSlotEl._sep.style.display = vaggaText ? '' : 'none';
		s.lastAppliedVaggaIdx = vIdx;
	}
	var sIdx = findActiveMarkerIdx(s.suttaMarkers, curIdx);
	if (sIdx !== s.lastAppliedSuttaIdx) {
		if (sIdx < 0) {
			if (d.titleEl) d.titleEl.textContent = s.fallbackTitle;
			if (d.titleMetaEl) d.titleMetaEl.textContent = s.fallbackTitleMeta;
		} else {
			var sm = s.suttaMarkers[sIdx];
			if (d.titleEl) d.titleEl.textContent = pickPrimaryByLang(sm) || s.fallbackTitle;
			if (d.titleMetaEl) d.titleMetaEl.textContent = pickAltByLang(sm) || s.fallbackTitleMeta;
		}
		s.lastAppliedSuttaIdx = sIdx;
	}
}

function setupAnchorObserver() {
	if (s.anchorObserver) { s.anchorObserver.disconnect(); s.anchorObserver = null; }
	var scrollRoot = getScrollRoot();
	if (!scrollRoot) return;
	var _ioRescan = throttle(function () {
		var k = computeTopVisibleKey();
		if (k) s.firstVisibleKey = k;
		updateDynamicTitles();
	}, 80);
	s.anchorObserver = new IntersectionObserver(_ioRescan, { root: scrollRoot, rootMargin: '0px 0px -80% 0px', threshold: 0 });
	scrollRoot.querySelectorAll('.sutra-row').forEach(function (r) { s.anchorObserver.observe(r); });
}
SA.setupAnchorObserver = setupAnchorObserver;

function computeTopVisibleKey() {
	var scrollRoot = getScrollRoot();
	if (!scrollRoot) return null;
	var rootRect = scrollRoot.getBoundingClientRect();
	var topBoundary = rootRect.top;
	var rows = scrollRoot.querySelectorAll('.sutra-row[data-key]');
	for (var i = 0; i < rows.length; i++) {
		var rect = rows[i].getBoundingClientRect();
		if (rect.top >= topBoundary) return rows[i].getAttribute('data-key') || null;
	}
	for (var j = 0; j < rows.length; j++) {
		var rect2 = rows[j].getBoundingClientRect();
		if (rect2.bottom > topBoundary) return rows[j].getAttribute('data-key') || null;
	}
	return null;
}
SA.computeTopVisibleKey = computeTopVisibleKey;

var _retrySaveTimer = 0;
function _scheduleRetrySave(after) {
	if (_retrySaveTimer) clearTimeout(_retrySaveTimer);
	_retrySaveTimer = setTimeout(function () {
		_retrySaveTimer = 0;
		saveScrollAnchorNow();
	}, Math.max(50, after));
}

function saveScrollAnchorNow() {
	if (!s.currentSutraId) return;
	var now = Date.now();
	if (now < s._progScrollUntil) {
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR SAVE] skip — programmatic scroll window, retry in', s._progScrollUntil - now);
		_scheduleRetrySave(s._progScrollUntil - now + 50);
		return;
	}
	if (s.isRendering) {
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR SAVE] skip — isRendering=true, retry in 150');
		_scheduleRetrySave(150);
		return;
	}
	var scrollRoot = getScrollRoot();
	if (!scrollRoot || scrollRoot.scrollTop === 0) {
		SA.anchorRemove(SA.KEY_ANCHOR_K(s.currentSutraId));
		SA._clearAnchorHash();
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR SAVE] cleared (scrollTop=0) for', s.currentSutraId);
		return;
	}
	var topKey = s.firstVisibleKey || computeTopVisibleKey();
	if (!topKey) {
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR SAVE] skip — no top key computable');
		return;
	}
	s.firstVisibleKey = topKey;
	SA.anchorSet(SA.KEY_ANCHOR_K(s.currentSutraId), topKey);
	SA._writeAnchorHash(topKey);
	if (window.DEBUG_ANCHOR) console.log('[ANCHOR SAVE]', s.currentSutraId, '→', topKey, 'scrollTop=' + scrollRoot.scrollTop);
}

// BUG FIX: anchor restore with instant scroll, correct key for scope fallback, extended suppression
function restoreScrollByAnchor(id) {
	var scrollRoot = getScrollRoot();
	if (!scrollRoot) return false;
	try {
		var key = SA.getAnchorKeyFor(id);
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR RESTORE] id=' + id + ' key=' + key);
		if (!key) return false;
		var foundIdx = -1;
		var foundKey = key;
		for (var j = 0; j < s.virtAllRows.length; j++) {
			if (String(s.virtAllRows[j].key || '') === key) { foundIdx = j; break; }
		}
		if (foundIdx < 0) {
			var savedScopeMatch = String(key).match(/^(.+):(\d+)/);
			if (savedScopeMatch) {
				var savedScope = savedScopeMatch[1] + ':' + savedScopeMatch[2];
				for (var sk = 0; sk < s.virtAllRows.length; sk++) {
					var curKey = String(s.virtAllRows[sk].key || '');
					var m = curKey.match(/^(.+):(\d+)/);
					if (m && (m[1] + ':' + m[2]) === savedScope) {
						foundIdx = sk;
						foundKey = curKey; // FIX: use the actual found key, not original
						if (window.DEBUG_ANCHOR) console.log('[ANCHOR RESTORE] fallback matched scope "' + savedScope + '" at idx=' + sk + ' key=' + curKey);
						break;
					}
				}
			}
		}
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR RESTORE] foundIdx=' + foundIdx + ' in virtAllRows.length=' + s.virtAllRows.length);
		if (foundIdx < 0) return false;
		SA.ensureRowRendered(foundIdx);
		// FIX: use foundKey (actual row's key) for DOM query, not original key
		var safeKey = safeCssEscape(foundKey);
		var row = scrollRoot.querySelector('.sutra-row[data-key="' + safeKey + '"]');
		if (window.DEBUG_ANCHOR) console.log('[ANCHOR RESTORE] DOM row found:', !!row);
		if (!row) return false;
		var scrollTarget = row.closest('.sutra-row-wrap') || row;
		// FIX: extend suppression window from 700ms to 1200ms
		s._progScrollUntil = Date.now() + 1200;
		function scrollToSegmentTop(label) {
			var rootRect = scrollRoot.getBoundingClientRect();
			var tgtRect  = scrollTarget.getBoundingClientRect();
			var y = tgtRect.top - rootRect.top + scrollRoot.scrollTop;
			var max = Math.max(0, scrollRoot.scrollHeight - scrollRoot.clientHeight);
			y = Math.max(0, Math.min(y, max));
			var oldTop = scrollRoot.scrollTop;
			// FIX: use instant scroll instead of assignment (which triggers smooth animation)
			if (Math.abs(y - oldTop) > 1) {
				scrollRoot.scrollTo({ top: y, behavior: 'instant' });
			}
			if (window.DEBUG_ANCHOR) {
				console.log('[ANCHOR RESTORE ' + label + '] tgt.top=' + tgtRect.top.toFixed(0) +
					' root.top=' + rootRect.top.toFixed(0) +
					' relTop=' + (tgtRect.top - rootRect.top).toFixed(0) +
					' targetY=' + y.toFixed(0) + ' oldScrollTop=' + oldTop + ' → newScrollTop=' + scrollRoot.scrollTop);
			}
		}
		scrollToSegmentTop('initial');
		SA.toggleBackTop(scrollRoot.scrollTop > 0);
		requestAnimationFrame(function () { requestAnimationFrame(function () {
			scrollToSegmentTop('rAF-correction');
			setTimeout(function () { scrollToSegmentTop('timeout-correction'); }, 100);
			setTimeout(function () { scrollToSegmentTop('settle-correction'); }, 500);
		}); });
		return true;
	} catch(e) {
		if (window.DEBUG_ANCHOR) console.error('[ANCHOR RESTORE] error:', e);
		return false;
	}
}
SA.restoreScrollByAnchor = restoreScrollByAnchor;

window.addEventListener('pagehide', function (e) {
	s.firstVisibleKey = null;
	saveScrollAnchorNow();
	if (!e.persisted) {
		if (s.anchorObserver) { s.anchorObserver.disconnect(); s.anchorObserver = null; }
		SA.teardownChunkObservers();
	}
});
window.addEventListener('pageshow', function (e) {
	if (!e.persisted || !s.currentSutraId) return;
	if (!s.anchorObserver) setupAnchorObserver();
	try { SA.setupChunkObservers(); } catch(_) {}
});
document.addEventListener('visibilitychange', function () {
	if (document.visibilityState === 'hidden') {
		s.firstVisibleKey = null;
		var prevProg = s._progScrollUntil;
		s._progScrollUntil = 0;
		try { saveScrollAnchorNow(); } finally { s._progScrollUntil = prevProg; }
	}
});

var _backTopPendingState = null;
var _backTopRAF = 0;
function toggleBackTop(show) {
	if (!d.btnBackTop) return;
	_backTopPendingState = !!show;
	if (_backTopRAF) return;
	_backTopRAF = requestAnimationFrame(function () {
		_backTopRAF = 0;
		var sv = _backTopPendingState;
		_backTopPendingState = null;
		if (sv === null) return;
		d.btnBackTop.classList.toggle('visible', sv);
		if (sv) {
			d.btnBackTop.style.removeProperty('display');
		} else {
			d.btnBackTop.style.setProperty('display', 'none', 'important');
			try { d.btnBackTop.blur(); } catch(_) {}
		}
	});
}
SA.toggleBackTop = toggleBackTop;

var _saveAnchorThrottled = throttle(saveScrollAnchorNow, 250);
var _saveAnchorDebounced = debounce(saveScrollAnchorNow, 200);
SA._saveAnchorDebounced = _saveAnchorDebounced;
var _backTopThrottled = throttle(function (v) { toggleBackTop(v); }, 120);
var _progressIdleTimer = null;

function _ensureProgressElements(wrap) {
	var bar = wrap.querySelector('.rp-bar');
	if (!bar) {
		bar = document.createElement('span');
		bar.className = 'rp-bar';
		wrap.insertBefore(bar, wrap.firstChild);
	}
	var existing = wrap.querySelectorAll('.rp-dot');
	if (existing.length !== 5) {
		Array.prototype.forEach.call(existing, function (dt) { dt.remove(); });
		for (var i = 0; i < 5; i++) {
			var dt = document.createElement('span');
			dt.className = 'rp-dot';
			dt.dataset.idx = String(i);
			wrap.appendChild(dt);
		}
	}
	return { bar: bar, dots: wrap.querySelectorAll('.rp-dot') };
}

function updateReadingProgress() {
	var wrap = document.getElementById('readingProgress');
	var pctEl = document.getElementById('readingProgressPct');
	if (!wrap || !d.scrollEl) return;
	var max = d.scrollEl.scrollHeight - d.scrollEl.clientHeight;
	if (max <= 10 || !s.currentSutraId) { wrap.classList.remove('visible'); return; }
	var gridRect = d.scrollEl.getBoundingClientRect();
	wrap.style.top = gridRect.top + 'px';
	wrap.style.bottom = Math.max(0, window.innerHeight - gridRect.bottom) + 'px';
	var pct = Math.min(1, Math.max(0, d.scrollEl.scrollTop / max));
	var wrapH = gridRect.height;
	var BAR_HEIGHT = 48, DOT_SIZE = 4;
	var DOT_SPACING_INIT = (BAR_HEIGHT - DOT_SIZE) / 4;
	var leadStart = 8 + (BAR_HEIGHT - DOT_SIZE);
	var range = Math.max(0, wrapH - 16 - leadStart);
	var leadY = leadStart + pct * range;
	var els = _ensureProgressElements(wrap);
	var bar = els.bar, dots = els.dots;
	var BAR_FULL_END = 0.05, BAR_FADE_END = 0.08;
	var BALL_START_PCT = 0.025, BALL_FULL_AT = 0.04, BALL_PHASE_TRIGGER = 0.05;
	var initLead = leadStart + BALL_PHASE_TRIGGER * range;
	var ballPct = Math.max(0, pct - BALL_PHASE_TRIGGER);
	var SPEEDS = [1.00, 0.82, 0.64, 0.46, 0.28];
	var FADE_WIN = [null, [0.65, 0.97], [0.50, 0.80], [0.35, 0.63], [0.20, 0.43]];
	var topDotY = pct < BALL_PHASE_TRIGGER
		? (leadY - 4 * DOT_SPACING_INIT)
		: (initLead - 4 * DOT_SPACING_INIT) + ballPct * SPEEDS[4] * range;
	var barOp;
	if (pct <= BAR_FULL_END) barOp = 1;
	else if (pct >= BAR_FADE_END) barOp = 0;
	else barOp = (BAR_FADE_END - pct) / (BAR_FADE_END - BAR_FULL_END);
	bar.style.top = topDotY.toFixed(1) + 'px';
	bar.style.height = '';
	bar.style.opacity = barOp.toFixed(2);
	var gradientFactor = Math.max(0, Math.min(1, (pct - 0.03) / 0.02));
	var bottomMix = (100 - 75 * gradientFactor).toFixed(0);
	bar.style.background = 'linear-gradient(to bottom,var(--accent) 0%,var(--accent) 30%,color-mix(in oklab,var(--accent) ' + bottomMix + '%,transparent) 100%)';
	var dotAppearOp;
	if (pct < BALL_START_PCT) dotAppearOp = 0;
	else if (pct >= BALL_FULL_AT) dotAppearOp = 1;
	else dotAppearOp = (pct - BALL_START_PCT) / (BALL_FULL_AT - BALL_START_PCT);
	for (var i = 0; i < 5; i++) {
		var dot = dots[i];
		var dotY = pct < BALL_PHASE_TRIGGER
			? (leadY - i * DOT_SPACING_INIT)
			: (initLead - i * DOT_SPACING_INIT) + ballPct * SPEEDS[i] * range;
		dot.style.top = dotY.toFixed(1) + 'px';
		var op;
		var fw = FADE_WIN[i];
		if (!fw) { op = dotAppearOp; }
		else if (pct < fw[0]) { op = dotAppearOp; }
		else if (pct >= fw[1]) { op = 0; }
		else { op = dotAppearOp * (1 - (pct - fw[0]) / (fw[1] - fw[0])); }
		dot.style.opacity = Math.max(0, Math.min(1, op)).toFixed(2);
	}
	if (pctEl) pctEl.textContent = Math.round(pct * 100) + '%';
	wrap.classList.add('visible');
	wrap.classList.remove('idle');
	clearTimeout(_progressIdleTimer);
	_progressIdleTimer = setTimeout(function () { wrap.classList.add('idle'); }, 1500);
}
SA.updateReadingProgress = updateReadingProgress;

var _readingProgressThrottled = throttle(updateReadingProgress, 80);
if (d.scrollEl) d.scrollEl.addEventListener('scroll', function () {
	if (!s.suppressBackTop) _backTopThrottled(d.scrollEl.scrollTop > 0);
	_saveAnchorThrottled();
	_saveAnchorDebounced();
	_readingProgressThrottled();
}, { passive: true });
window.addEventListener('resize', updateReadingProgress);

if (d.btnBackTop && d.scrollEl) d.btnBackTop.onclick = function () {
	s.suppressBackTop = true;
	toggleBackTop(false);
	s._progScrollUntil = Date.now() + 2000;
	d.scrollEl.scrollTo({ top: 0, behavior: 'smooth' });
	var doneCalled = false;
	var done = function () {
		if (doneCalled) return;
		doneCalled = true;
		s.suppressBackTop = false;
		s._progScrollUntil = 0;
		toggleBackTop(false);
		if (s.currentSutraId) SA.anchorRemove(SA.KEY_ANCHOR_K(s.currentSutraId));
	};
	setTimeout(done, 2000);
	if ('onscrollend' in d.scrollEl) {
		d.scrollEl.addEventListener('scrollend', done, { once: true });
	} else {
		var prev = -1;
		var poll = function () {
			if (doneCalled) return;
			var st = d.scrollEl.scrollTop;
			if (st === 0 && st === prev) { done(); return; }
			prev = st;
			requestAnimationFrame(poll);
		};
		requestAnimationFrame(poll);
	}
};
})();

/* ================================================================
   render.js — Module [6/8]
   Virtual scroll engine: chia rows thành chunks ~50 dòng,
   materialize/dematerialize bằng IntersectionObserver.
   renderSutra() là pipeline chính: load data → build chunks →
   eager materialize quanh anchor → restore scroll.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.findMetaById, SA.renderSutra, SA.renderWelcomeScreen,
            SA.setupChunkObservers, SA.teardownChunkObservers,
            SA.ensureRowRendered, SA.buildSuttaPrintHtml,
            SA.resolveCommentLang
   Depends: utils.js, bilara.js, state.js, ui.js, anchor.js
   Runtime calls (defined in later modules, resolved at call time):
     SA.resetTts, SA.setTtsUiState, SA.syncTileToCurrentSutta,
     SA.highlightActiveInMenu, SA.updateNavButtons,
     SA.applyTitleBookmarkState, SA.scheduleNextPreload
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;
var $ = SA.$;
var storage = SA.storage;
var escapeHtml = SA.escapeHtml;
var escapeAttr = SA.escapeAttr;
var s = SA.state;
var d = SA.dom;

function findMetaById(id) {
	var index = window.SUTRA_INDEX || [];
	var found = null;
	function walk(children, currentParent, rootNikaya) {
		if (!children || !children.length || found) return;
		for (var i = 0; i < children.length; i++) {
			if (found) return;
			var ch = children[i];
			if (ch.type === 'sutta' && ch.id === id) {
				found = Object.assign({}, ch);
				found.parentGroup = currentParent;
				found.rootNikaya  = rootNikaya;
				return;
			}
			if (ch.type === 'group') walk(ch.children || [], ch, rootNikaya);
		}
	}
	for (var i = 0; i < index.length; i++) {
		walk(index[i].children || [], index[i], index[i]);
		if (found) break;
	}
	return found;
}
SA.findMetaById = findMetaById;

function getColHeaders() {
	return s.uiLang === 'en'
		? { pali: 'Pali', eng: 'English', vie: 'Vietnamese' }
		: { pali: 'Pali', eng: 'English', vie: 'Tiếng Việt' };
}

function resolveCommentLang(v) {
	if (!v) return '';
	if (typeof v === 'string') return v.trim();
	return '';
}
SA.resolveCommentLang = resolveCommentLang;

function resolveCommentText(cmt) {
	if (!cmt) return '';
	if (typeof cmt === 'string') return cmt.trim();
	if (typeof cmt === 'object') {
		var pref = s.uiLang === 'en' ? ['en','vi','pli'] : ['vi','en','pli'];
		for (var i = 0; i < pref.length; i++) {
			var v = cmt[pref[i]];
			if (typeof v === 'string' && v.trim()) return v.trim();
		}
	}
	return '';
}

function createRow(r) {
	var wrap = document.createElement('div'); wrap.className = 'sutra-row-wrap';
	var keyRaw = String(r.key || '');
	var tp = (r.pali || '').trim();
	var te = (r.eng  || '').trim();
	var tv = (r.vie  || '').trim();
	var isSectionNum = (tp.length <= 6 && te.length <= 6 && tv.length <= 6) &&
		/^[IVXLCDM]+\.?$|^\d+\.?$/.test(tp || te || tv);
	if (isSectionNum) wrap.classList.add('is-section-num');
	if (/:source$/i.test(keyRaw)) wrap.classList.add('is-source');
	if (/:0\.[123]$/.test(keyRaw) && (!r._merged || r._isHeading)) wrap.classList.add('is-subtitle');
	wrap.setAttribute('data-key', keyRaw);
	var keyShort = '';
	if (keyRaw.includes(':')) {
		var parts = keyRaw.split(':');
		var prefix = parts[0].replace(/([a-zA-Z]+)(\d*)/, function (_, letters, nums) { return letters.toUpperCase() + nums; });
		keyShort = parts[1] ? prefix + '.' + parts[1] : prefix;
	} else { keyShort = keyRaw.toUpperCase(); }
	if (keyShort) {
		var segWrap = document.createElement('div');
		segWrap.className = 'sutra-seg-keywrap';
		var seg = document.createElement('div');
		seg.className = 'sutra-seg-key'; seg.textContent = keyShort;
		seg.setAttribute('aria-hidden', 'true');
		segWrap.appendChild(seg);
		var shareBtn = document.createElement('button');
		shareBtn.type = 'button';
		shareBtn.className = 'sutra-seg-share';
		shareBtn.setAttribute('data-share-key', keyRaw);
		shareBtn.setAttribute('aria-label', s.uiLang === 'en' ? 'Share / copy link to this segment' : 'Chia sẻ / sao chép link đoạn này');
		shareBtn.title = s.uiLang === 'en' ? 'Share / copy link' : 'Chia sẻ / sao chép link';
		shareBtn.innerHTML = '<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="5" cy="10" r="2.4"/><circle cx="15" cy="5" r="2.4"/><circle cx="15" cy="15" r="2.4"/><path d="M7.2 8.9l5.6-3.4M7.2 11.1l5.6 3.4"/></svg>';
		segWrap.appendChild(shareBtn);
		wrap.appendChild(segWrap);
	}
	var row = document.createElement('div');
	row.className = 'sutra-row'; row.setAttribute('data-key', keyRaw);
	var headers = getColHeaders();
	var cPli = resolveCommentLang(r.commentPli);
	var cEng = resolveCommentLang(r.commentEn);
	var cVie = resolveCommentLang(r.commentVie);
	function ensureCmtHeader(col) {
		if (col.querySelector('.sutra-col-comments-header')) return;
		var h = document.createElement('div');
		h.className = 'sutra-col-comments-header';
		h.setAttribute('aria-hidden', 'true');
		if (col.classList.contains('pali-col')) h.textContent = 'Aṭṭhakathā';
		else if (col.classList.contains('eng-col')) h.textContent = 'Commentary';
		else h.textContent = 'Chú giải';
		col.appendChild(h);
	}
	function makeCol(className, headerText, contentText, contentClass, cmtText) {
		var col  = document.createElement('div'); col.className = 'sutra-col ' + className;
		var hdr  = document.createElement('div'); hdr.className = 'sutra-col-header';
		var hdrLabel = document.createElement('span');
		hdrLabel.className = 'sutra-col-header-label';
		hdrLabel.textContent = headerText;
		hdr.appendChild(hdrLabel);
		var copyBtn = document.createElement('button');
		copyBtn.type = 'button';
		copyBtn.className = 'sutra-col-copy';
		copyBtn.setAttribute('aria-label', s.uiLang === 'en' ? 'Copy this paragraph' : 'Sao chép đoạn này');
		copyBtn.title = s.uiLang === 'en' ? 'Copy' : 'Sao chép';
		copyBtn.innerHTML = '<svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="4.5" y="4.5" width="7.5" height="7.5" rx="1"/><path d="M2 9V3a1 1 0 0 1 1-1h6"/></svg>';
		hdr.appendChild(copyBtn);
		var body = document.createElement('div'); body.className = 'sutra-col-body';
		var inner = document.createElement('div'); inner.className = contentClass;
		inner.textContent = contentText || '';
		body.appendChild(inner); col.appendChild(hdr); col.appendChild(body);
		if (cmtText) {
			ensureCmtHeader(col);
			var cmt = document.createElement('div');
			cmt.className = 'sutra-col-comment';
			var icon = document.createElement('span');
			icon.className = 'sutra-col-comment-icon'; icon.setAttribute('aria-hidden', 'true');
			icon.textContent = '💬';
			var txt = document.createElement('span');
			txt.className = 'sutra-col-comment-text'; txt.textContent = cmtText;
			cmt.appendChild(icon); cmt.appendChild(txt);
			col.appendChild(cmt);
		}
		return col;
	}
	var pcol = makeCol('pali-col', headers.pali, r.pali, 'pali', cPli);
	var ecol = makeCol('eng-col',  headers.eng,  r.eng,  'eng', cEng);
	var vcol = makeCol('vie-col',  headers.vie,  r.vie,  'vie', cVie);
	row.appendChild(pcol); row.appendChild(ecol); row.appendChild(vcol);
	wrap.appendChild(row);
	if (r._merged && Array.isArray(r._mergedComments) && r._mergedComments.length) {
		var targetCol = r.pali ? pcol : (r.eng ? ecol : (r.vie ? vcol : null));
		if (targetCol) {
			ensureCmtHeader(targetCol);
			for (var mi = 0; mi < r._mergedComments.length; mi++) {
				var mc = r._mergedComments[mi];
				var cBlk = document.createElement('div');
				cBlk.className = 'sutra-col-comment sutra-col-comment-merged';
				var icon2 = document.createElement('span');
				icon2.className = 'sutra-col-comment-icon'; icon2.setAttribute('aria-hidden', 'true');
				icon2.textContent = '💬';
				var txt2 = document.createElement('span');
				txt2.className = 'sutra-col-comment-text'; txt2.textContent = mc.text;
				cBlk.appendChild(icon2); cBlk.appendChild(txt2);
				targetCol.appendChild(cBlk);
			}
		}
	}
	var cLegacy = resolveCommentText(r.comment);
	if (cLegacy && !cPli && !cEng && !cVie) {
		var legacy = document.createElement('div');
		legacy.className = 'sutra-comment-legacy';
		var li = document.createElement('span');
		li.className = 'sutra-comment-legacy-icon'; li.setAttribute('aria-hidden', 'true');
		li.textContent = '💬';
		var lt = document.createElement('span');
		lt.className = 'sutra-comment-legacy-text'; lt.textContent = cLegacy;
		legacy.appendChild(li); legacy.appendChild(lt);
		wrap.appendChild(legacy);
	}
	return wrap;
}

function materializeChunk(chunkInfo) {
	if (!chunkInfo || chunkInfo.materialized) return;
	var scroller = d.scrollEl;
	var needsCompensation = false;
	var oldChunkBottom = 0;
	if (scroller && chunkInfo.div.parentNode === scroller && Date.now() >= s._progScrollUntil) {
		var preRect = chunkInfo.div.getBoundingClientRect();
		var preRootRect = scroller.getBoundingClientRect();
		oldChunkBottom = preRect.bottom - preRootRect.top + scroller.scrollTop;
		if (oldChunkBottom <= scroller.scrollTop) needsCompensation = true;
	}
	var frag = document.createDocumentFragment();
	for (var i = chunkInfo.rowStart; i < chunkInfo.rowEnd; i++) {
		var rowData = s.virtAllRows[i];
		if (!rowData) continue;
		var wrap = createRow(rowData);
		frag.appendChild(wrap);
		var innerRow = wrap.querySelector ? wrap.querySelector('.sutra-row') : wrap;
		s.cachedRows[i] = innerRow || wrap;
	}
	chunkInfo.div.appendChild(frag);
	chunkInfo.div.style.minHeight = '';
	chunkInfo.materialized = true;
	if (s.anchorObserver) {
		var newRows = chunkInfo.div.querySelectorAll('.sutra-row');
		for (var k = 0; k < newRows.length; k++) s.anchorObserver.observe(newRows[k]);
	}
	if (needsCompensation && scroller) {
		var newChunkRect = chunkInfo.div.getBoundingClientRect();
		var newRootRect  = scroller.getBoundingClientRect();
		var newChunkBottom = newChunkRect.bottom - newRootRect.top + scroller.scrollTop;
		var delta = newChunkBottom - oldChunkBottom;
		if (Math.abs(delta) > 0.5) scroller.scrollTop += delta;
	}
}

function dematerializeChunk(chunkInfo) {
	if (!chunkInfo || !chunkInfo.materialized) return;
	if (s.anchorObserver) {
		var oldRows = chunkInfo.div.querySelectorAll('.sutra-row');
		for (var k = 0; k < oldRows.length; k++) s.anchorObserver.unobserve(oldRows[k]);
	}
	var realH = chunkInfo.div.offsetHeight;
	if (realH > 0) chunkInfo.measuredH = realH;
	else if (!chunkInfo.measuredH) chunkInfo.measuredH = (chunkInfo.rowEnd - chunkInfo.rowStart) * 120;
	var scroller = d.scrollEl;
	var needsCompensation = false;
	var oldChunkBottom = 0;
	if (scroller && chunkInfo.div.parentNode === scroller && Date.now() >= s._progScrollUntil) {
		var preRect = chunkInfo.div.getBoundingClientRect();
		var preRootRect = scroller.getBoundingClientRect();
		oldChunkBottom = preRect.bottom - preRootRect.top + scroller.scrollTop;
		if (oldChunkBottom <= scroller.scrollTop) needsCompensation = true;
	}
	while (chunkInfo.div.firstChild) chunkInfo.div.removeChild(chunkInfo.div.firstChild);
	chunkInfo.div.style.minHeight = chunkInfo.measuredH + 'px';
	chunkInfo.materialized = false;
	for (var i = chunkInfo.rowStart; i < chunkInfo.rowEnd; i++) s.cachedRows[i] = null;
	if (needsCompensation && scroller) {
		var newChunkRect = chunkInfo.div.getBoundingClientRect();
		var newRootRect  = scroller.getBoundingClientRect();
		var newChunkBottom = newChunkRect.bottom - newRootRect.top + scroller.scrollTop;
		var delta = newChunkBottom - oldChunkBottom;
		if (Math.abs(delta) > 0.5) scroller.scrollTop += delta;
	}
}

function teardownChunkObservers() {
	if (s.virtMatObs) { s.virtMatObs.disconnect(); s.virtMatObs = null; }
	if (s.virtDemObs) { s.virtDemObs.disconnect(); s.virtDemObs = null; }
}
SA.teardownChunkObservers = teardownChunkObservers;

function setupChunkObservers() {
	teardownChunkObservers();
	if (!d.scrollEl || !s.virtChunks.length) return;
	s.virtMatObs = new IntersectionObserver(function (entries) {
		for (var i = 0; i < entries.length; i++) {
			if (entries[i].isIntersecting) {
				var idx = parseInt(entries[i].target.getAttribute('data-chunk-idx'), 10);
				if (Number.isFinite(idx) && s.virtChunks[idx]) materializeChunk(s.virtChunks[idx]);
			}
		}
	}, { root: d.scrollEl, rootMargin: '200% 0px 100% 0px', threshold: 0 });
	s.virtDemObs = new IntersectionObserver(function (entries) {
		for (var i = 0; i < entries.length; i++) {
			if (!entries[i].isIntersecting) {
				var idx = parseInt(entries[i].target.getAttribute('data-chunk-idx'), 10);
				if (Number.isFinite(idx) && s.virtChunks[idx]) dematerializeChunk(s.virtChunks[idx]);
			}
		}
	}, { root: d.scrollEl, rootMargin: '300% 0px 300% 0px', threshold: 0 });
	for (var k = 0; k < s.virtChunks.length; k++) {
		s.virtMatObs.observe(s.virtChunks[k].div);
		s.virtDemObs.observe(s.virtChunks[k].div);
	}
}
SA.setupChunkObservers = setupChunkObservers;

function _findChunkForRow(rowIdx) {
	for (var k = 0; k < s.virtChunks.length; k++) {
		if (rowIdx >= s.virtChunks[k].rowStart && rowIdx < s.virtChunks[k].rowEnd) return k;
	}
	return 0;
}

function _progressiveFill(anchorChunkIdx, renderToken) {
	var above = anchorChunkIdx - 2;
	var below = anchorChunkIdx + 2;
	var RANGE = 8;
	var minChunk = Math.max(0, anchorChunkIdx - RANGE);
	var maxChunk = Math.min(s.virtChunks.length - 1, anchorChunkIdx + RANGE);
	var queue = [];
	while (above >= minChunk || below <= maxChunk) {
		if (above >= minChunk) queue.push(above--);
		if (below <= maxChunk) queue.push(below++);
	}
	var idx = 0;
	function fillNext() {
		if (renderToken !== s.renderToken) return;
		if (idx >= queue.length) return;
		var ci = queue[idx++];
		if (s.virtChunks[ci] && !s.virtChunks[ci].materialized) {
			materializeChunk(s.virtChunks[ci]);
		}
		if (idx < queue.length) {
			if (typeof requestIdleCallback === 'function') {
				requestIdleCallback(fillNext, { timeout: 200 });
			} else {
				setTimeout(fillNext, 50);
			}
		}
	}
	fillNext();
}

function ensureRowRendered(rowIdx) {
	for (var k = 0; k < s.virtChunks.length; k++) {
		var c = s.virtChunks[k];
		if (rowIdx >= c.rowStart && rowIdx < c.rowEnd) {
			if (!c.materialized) materializeChunk(c);
			return;
		}
	}
}
SA.ensureRowRendered = ensureRowRendered;

function getByExactOrSuffix(map, exactKey, suffix) {
	if (!map) return '';
	if (exactKey && map[exactKey]) return map[exactKey];
	var keys = Object.keys(map);
	var k = exactKey ? keys.find(function (x) { return x === exactKey; }) : null;
	if (!k && suffix) k = keys.find(function (x) { return String(x).endsWith(suffix); });
	return k ? map[k] || '' : '';
}
function pickTextForUiLangSuffix(merged, id, suffix) {
	var exactKey = id + suffix;
	if (s.uiLang === 'en') return getByExactOrSuffix(merged.engMap, exactKey, suffix)
		|| getByExactOrSuffix(merged.vieMap, exactKey, suffix)
		|| getByExactOrSuffix(merged.paliMap, exactKey, suffix) || '';
	return getByExactOrSuffix(merged.vieMap, exactKey, suffix)
		|| getByExactOrSuffix(merged.engMap, exactKey, suffix)
		|| getByExactOrSuffix(merged.paliMap, exactKey, suffix) || '';
}

function renderWelcomeScreen() {
	if (!d.grid || s.currentSutraId) return;
	var isEn = s.uiLang === 'en';
	if (d.superTitleEl) d.superTitleEl.textContent = '';
	if (d.titleMetaEl)  d.titleMetaEl.textContent  = '';
	if (d.titleEl)      d.titleEl.textContent      = '';
	if (d.subtitleEl)   d.subtitleEl.textContent   = '';
	document.documentElement.classList.add('is-welcome');
	SA.applyTitleBookmarkState();
	var heroSub = isEn
		? 'Reverently saluting the Blessed One, the Worthy One, the Perfectly Self-Awakened.<br>A library of canonical suttas for practitioners and scholars.'
		: 'Cung kính đảnh lễ Đức Thế Tôn, bậc A-la-hán, Chánh Đẳng Giác.<br>Một thư viện kinh điển dành cho người tu học và nghiên cứu Phật pháp.';
	var R = 46, CX = 60, CY = 60;
	var petalDots = '';
	for (var pd = 0; pd < 9; pd++) {
		var ang = pd * 40 * Math.PI / 180;
		var x = (CX + R * Math.sin(ang)).toFixed(1);
		var y = (CY - R * Math.cos(ang)).toFixed(1);
		var dx = (CX - x).toFixed(1);
		var dy = (CY - y).toFixed(1);
		petalDots += '<circle class="welcome-petal-dot" cx="' + x + '" cy="' + y + '" r="1.6" style="--dx:' + dx + 'px;--dy:' + dy + 'px;--idx:' + pd + '"/>';
	}
	var mandalaSvg = '<svg viewBox="0 0 120 120" fill="none" stroke="currentColor" aria-hidden="true"><g class="welcome-ring r1"><circle cx="60" cy="60" r="54" stroke-width=".7" opacity=".55"/><circle cx="60" cy="60" r="54" stroke-width=".7" stroke-dasharray="1 6" opacity=".8"/></g><g class="welcome-ring r2"><circle cx="60" cy="60" r="42" stroke-width=".6" stroke-dasharray="2 4" opacity=".6"/></g><g class="welcome-core" stroke-width=".9"><g opacity=".95"><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(45 60 60)"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(90 60 60)"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(135 60 60)"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(180 60 60)"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(225 60 60)"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(270 60 60)"/><path d="M60 30 C 70 42, 70 52, 60 60 C 50 52, 50 42, 60 30 Z" transform="rotate(315 60 60)"/></g><circle cx="60" cy="60" r="4" fill="currentColor" stroke="none"/></g><g class="welcome-petals">' + petalDots + '</g></svg>';
	var verses = [
		{ pali: 'Vayadhammā saṅkhārā, appamādena sampādetha',
		  tr: isEn ? 'All conditioned things are subject to decay. Strive on with diligence' : 'Các pháp hữu vi đều vô thường, hãy tinh tấn chớ có buông lung',
		  src: isEn ? "The Buddha's last words — Mahāparinibbāna Sutta (DN 16)" : 'Lời dạy cuối cùng của Đức Phật trước khi nhập Niết-bàn — Kinh Đại Bát Niết-bàn (DN 16)',
		  segKey: 'dn16:3.51.4' },
		{ pali: 'Attadīpā viharatha, attasaraṇā, anaññasaraṇā',
		  tr: isEn ? 'Dwell as your own island, your own refuge, with no other refuge' : 'Hãy tự mình làm hòn đảo cho chính mình, hãy tự mình nương tựa chính mình, không nương tựa ai khác',
		  src: isEn ? 'Mahāparinibbāna Sutta (DN 16)' : 'Kinh Đại Bát Niết-bàn (DN 16)',
		  segKey: 'dn16:2.26.1' }
	];
	var versesHtml = verses.map(function (v) {
		return '<article class="welcome-verse"><p class="welcome-verse-pali">' + escapeHtml(v.pali) + '</p><p class="welcome-verse-tr">' + escapeHtml(v.tr) + '</p><p class="welcome-verse-source">' + escapeHtml(v.src) + '</p></article>';
	}).join('');
	var virtues = [
		{ pali: 'Arahaṃ', tr: isEn ? 'The Worthy One' : 'Ứng Cúng' },
		{ pali: 'Sammāsambuddho', tr: isEn ? 'The Self-Awakened' : 'Chánh Biến Tri' },
		{ pali: 'Vijjā­caraṇa­sampanno', tr: isEn ? 'Knowledge & Conduct' : 'Minh Hạnh Túc' },
		{ pali: 'Sugato', tr: isEn ? 'The Well-Gone' : 'Thiện Thệ' },
		{ pali: 'Lokavidū', tr: isEn ? 'Knower of the World' : 'Thế Gian Giải' },
		{ pali: 'Anuttaro­purisa­damma­sārathi', tr: isEn ? 'Supreme Trainer' : 'Điều Ngự Trượng Phu' },
		{ pali: 'Satthā­deva­manussānaṃ', tr: isEn ? 'Teacher of Gods & Humans' : 'Thiên Nhân Sư' },
		{ pali: 'Buddho', tr: isEn ? 'The Awakened' : 'Phật' },
		{ pali: 'Bhagavā', tr: isEn ? 'The Blessed One' : 'Thế Tôn' }
	];
	var virtuesHtml = '<ol class="welcome-virtues" aria-label="' + (isEn ? 'Nine Virtues of the Buddha' : 'Cửu Đức Phật') + '">' +
		virtues.map(function (v, i) {
			return '<li class="wv-item" style="--idx:' + i + ';--angle:' + (i * 40) + 'deg"><span class="wv-pali">' + escapeHtml(v.pali) + '</span><span class="wv-tr">' + escapeHtml(v.tr) + '</span></li>';
		}).join('') + '</ol>';
	var iconLib  = '<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" aria-hidden="true"><path d="M2 3v10M5 3v10M8 3v10M12 4l3 9M11 13h5"/></svg>';
	var iconHelp = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="12" r="9"/><path d="M9.5 9a2.5 2.5 0 0 1 5 0c0 1.4-.9 2-1.6 2.4-.7.4-1.1.7-1.1 1.6v.4"/><path d="M12 16.2h.01"/></svg>';
	var helperRows = isEn
		? [{action:'guide', text: '<em>Help</em> — view the guide', icon: iconHelp},
		   {action:'library', text: '<em>Library</em> — browse suttas', icon: iconLib}]
		: [{action:'guide', text: '<em>Trợ giúp</em> — xem hướng dẫn', icon: iconHelp},
		   {action:'library', text: '<em>Thư viện</em> — chọn bài kinh', icon: iconLib}];
	var helperHtml = helperRows.map(function (r) {
		return '<button type="button" class="welcome-help-row" data-action="' + r.action + '"><span class="welcome-help-text">' + r.text + '</span><span class="welcome-help-key">' + r.icon + '</span></button>';
	}).join('');
	d.grid.innerHTML =
		'<div class="welcome-screen"><div class="welcome-mandala">' + mandalaSvg + virtuesHtml + '</div>' +
		'<h1 class="welcome-hero-title" data-action="library" tabindex="0" role="button" aria-label="' + (isEn ? 'Open library' : 'Mở thư viện') + '" title="' + (isEn ? 'Open library' : 'Mở thư viện') + '">' + (isEn ? 'The <em>Sutta</em><br>Nikāya' : 'Tạng <em>Kinh</em><br>Nikāya') + '</h1>' +
		'<p class="welcome-hero-sub">' + heroSub + '</p>' +
		'<div class="welcome-hero-langs" role="tablist" aria-label="' + (isEn ? 'Interface language' : 'Ngôn ngữ giao diện') + '">' +
		'<button type="button" class="welcome-lang-btn' + (s.uiLang === 'vi' ? ' active' : '') + '" data-ui-lang="vi" role="tab" aria-selected="' + (s.uiLang === 'vi' ? 'true' : 'false') + '">Việt</button>' +
		'<span class="dot" aria-hidden="true"></span>' +
		'<button type="button" class="welcome-lang-btn' + (s.uiLang === 'en' ? ' active' : '') + '" data-ui-lang="en" role="tab" aria-selected="' + (s.uiLang === 'en' ? 'true' : 'false') + '">English</button>' +
		'</div><section class="welcome-verses">' + versesHtml + '</section><div class="welcome-helper">' + helperHtml + '</div></div>';
	d.grid.querySelectorAll('.welcome-help-row[data-action], .welcome-hero-title[data-action]').forEach(function (el) {
		el.addEventListener('click', function (e) {
			e.stopPropagation();
			var action = el.getAttribute('data-action');
			if (action === 'library' && d.btnSutraMenu) d.btnSutraMenu.click();
			else if (action === 'guide' && d.btnGuide) d.btnGuide.click();
		});
	});
	var heroTitleEl = d.grid.querySelector('.welcome-hero-title[data-action]');
	if (heroTitleEl) heroTitleEl.addEventListener('keydown', function (e) {
		if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); heroTitleEl.click(); }
	});
	d.grid.querySelectorAll('.welcome-lang-btn[data-ui-lang]').forEach(function (el) {
		el.addEventListener('click', function (e) {
			e.stopPropagation();
			var chosen = el.getAttribute('data-ui-lang');
			if (chosen === s.uiLang) return;
			if (d.btnUiLang) d.btnUiLang.click();
		});
	});
}
SA.renderWelcomeScreen = renderWelcomeScreen;

SA.renderSutra = async function renderSutra(id) {
	if (!id || !d.grid) return;
	if (s.currentSutraId && s.currentSutraId !== id) SA._dmInvalidateForSutta(s.currentSutraId);
	SA.resetTts(true, false);
	if (s.anchorObserver) { s.anchorObserver.disconnect(); s.anchorObserver = null; }
	teardownChunkObservers();
	s.virtChunks = [];
	s.virtAllRows = [];
	s.firstVisibleKey = null;
	s.cachedRows = [];
	var token = ++s.renderToken;
	s.isRendering = true;
	d.grid.setAttribute('aria-busy', 'true');
	if (d.btnReadTts)  d.btnReadTts.disabled  = true;
	if (d.btnPauseTts) d.btnPauseTts.disabled = true;
	if (d.btnStopTts)  d.btnStopTts.disabled  = true;
	var merged = null;
	try { merged = await SA.loadMerged(id); } catch(e) { merged = null; }
	if (token !== s.renderToken) { s.isRendering = false; return; }
	s.currentSutraId = id;
	SA.syncTileToCurrentSutta();
	SA.highlightActiveInMenu();
	SA.updateNavButtons();
	if (!merged || !merged.rows || !merged.rows.length) {
		if (storage.get(SA.KEY_LAST) === id) storage.remove(SA.KEY_LAST);
		if (d.titleEl) d.titleEl.textContent = s.uiLang === 'en' ? 'Sutta data not found' : 'Không tìm thấy dữ liệu bài kinh';
		if (d.subtitleEl) d.subtitleEl.textContent = (s.uiLang === 'en' ? 'ID: ' : 'Mã bài: ') + id;
		d.grid.innerHTML = '<div style="max-width:520px;margin:48px auto;padding:24px;text-align:center;font-family:var(--serif-vi);color:var(--ink-3);font-style:italic;border:1px dashed var(--rule);border-radius:6px">'
			+ (s.uiLang === 'en' ? 'No data for this sutta yet. Please choose another.' : 'Bài kinh chưa có dữ liệu — vui lòng chọn bài khác.') + '</div>';
		s.isRendering = false;
		d.grid.setAttribute('aria-busy', 'false'); SA.setTtsUiState('idle'); return;
	}
	storage.set(SA.KEY_LAST, id);
	var titleFromBilara    = (pickTextForUiLangSuffix(merged, id, ':0.2') || '').trim();
	var subtitleFromBilara = (pickTextForUiLangSuffix(merged, id, ':0.1') || '').trim();
	var meta = findMetaById(id) || {};
	var titleFallback = s.uiLang === 'en'
		? meta.titleEn || meta.titleVi || meta.titlePali || meta.title || id
		: meta.titleVi || meta.titleEn || meta.titlePali || meta.title || id;
	var rootLabelVi = meta.rootNikaya ? (meta.rootNikaya.labelVi || meta.rootNikaya.key) : '';
	var rootLabelEn = meta.rootNikaya ? (meta.rootNikaya.labelEn || meta.rootNikaya.key) : '';
	var parentLabelVi = meta.parentGroup ? (meta.parentGroup.labelVi || meta.parentGroup.key) : '';
	var parentLabelEn = meta.parentGroup ? (meta.parentGroup.labelEn || meta.parentGroup.key) : '';
	function extractShortLabel(label, lang) {
		if (!label) return '';
		var parts = label.split(/\s+-\s+/);
		return lang === 'vi' ? (parts[1] || parts[0] || '').trim() : (parts[0] || label).trim();
	}
	var rootShort   = extractShortLabel(s.uiLang === 'en' ? rootLabelEn : rootLabelVi, s.uiLang);
	var parentShort = extractShortLabel(s.uiLang === 'en' ? parentLabelEn : parentLabelVi, s.uiLang);
	var rootKey = meta.rootNikaya ? meta.rootNikaya.key : '';
	var titleOverride = null, titleMetaOverride = null;
	var AN_ORDINALS_EN = ['', 'Ones', 'Twos', 'Threes', 'Fours', 'Fives', 'Sixes', 'Sevens', 'Eights', 'Nines', 'Tens', 'Elevens'];
	if (rootKey === 'SN') {
		titleOverride = s.uiLang === 'en' ? (meta.titleEn || meta.titleVi) : (meta.titleVi || meta.titleEn);
		titleMetaOverride = s.uiLang === 'en' ? (meta.titleVi || '') : (meta.titleEn || '');
	} else if (rootKey === 'AN' && meta.code) {
		var anM = String(meta.code).match(/AN\s*(\d+)/);
		if (anM) {
			var n = parseInt(anM[1], 10);
			titleOverride = s.uiLang === 'en' ? 'Book of ' + (AN_ORDINALS_EN[n] || n) : 'Nhóm ' + n + ' Pháp';
			titleMetaOverride = s.uiLang === 'en' ? 'Nhóm ' + n + ' Pháp' : 'Book of ' + (AN_ORDINALS_EN[n] || n);
		}
	}
	var resolvedTitle = titleOverride || titleFromBilara || titleFallback;
	if (d.titleEl) d.titleEl.textContent = resolvedTitle;
	SA.applyTitleBookmarkState();
	var paliName = (meta.titlePali || subtitleFromBilara || '').trim();
	s.isAN = (rootKey === 'AN');
	s.isSN = (rootKey === 'SN');
	s.fallbackTitle = (resolvedTitle || '').trim();
	s.superLastSlotEl = null;
	if (d.superTitleEl) {
		var superParts = [];
		if (rootShort) superParts.push(rootShort);
		if (parentShort && parentShort !== rootShort && rootKey !== 'SN') superParts.push(parentShort);
		var codeTxt = (meta.code || '').trim();
		if (codeTxt) superParts.push(codeTxt);
		if (s.isSN) {
			if (s.fallbackTitle) superParts.push(s.fallbackTitle);
			d.superTitleEl.textContent = '';
			for (var spi = 0; spi < superParts.length; spi++) {
				if (spi > 0) d.superTitleEl.appendChild(document.createTextNode(' · '));
				var spSpan = document.createElement('span');
				spSpan.textContent = superParts[spi];
				d.superTitleEl.appendChild(spSpan);
			}
			var sepNode = document.createElement('span');
			sepNode.className = 'super-sep'; sepNode.textContent = ' · '; sepNode.style.display = 'none';
			d.superTitleEl.appendChild(sepNode);
			var dynSpan = document.createElement('span');
			dynSpan.id = 'superLastSlot'; dynSpan.textContent = '';
			d.superTitleEl.appendChild(dynSpan);
			s.superLastSlotEl = dynSpan;
			s.superLastSlotEl._sep = sepNode;
		} else {
			var lastSlotText = s.isAN ? s.fallbackTitle : paliName;
			if (lastSlotText) superParts.push(lastSlotText);
			d.superTitleEl.textContent = superParts.join(' · ');
		}
	}
	s.lastAppliedVaggaIdx = -2;
	s.lastAppliedSuttaIdx = -2;
	if (d.subtitleEl) d.subtitleEl.textContent = '';
	var altName = titleMetaOverride || (s.uiLang === 'en' ? (meta.titleVi || '') : (meta.titleEn || ''));
	s.fallbackTitleMeta = (altName || '').trim();
	if (d.titleMetaEl) d.titleMetaEl.textContent = altName;
	var normId = String(id).replace(/([A-Za-z]+)0*(\d)/g, '$1$2');
	var mainPrefix1 = id + ':0.';
	var mainPrefix2 = normId + ':0.';
	var mainRows = [];
	var sourceRows = [];
	(merged.rows || []).forEach(function (r) {
		var k = String(r.key || '');
		if (k.startsWith(mainPrefix1) || k.startsWith(mainPrefix2)) return;
		if (/:source$/i.test(k)) sourceRows.push(r);
		else mainRows.push(r);
	});
	var rowsForViewRaw = mainRows.concat(sourceRows);
	var singleLang = SA.getSingleVisibleLang(); s.lastSingleLangMode = singleLang;
	var viewData = SA.getViewData(id, rowsForViewRaw, singleLang, s.isAN, s.isSN);
	var rowsForView = viewData.rows;
	d.grid.innerHTML = '';
	s.cachedRows = [];
	s._progScrollUntil = Date.now() + 5000;
	SA.applyVisibility();
	var CHUNK_SIZE = 50;
	var EST_ROW_H = singleLang ? 130 : (d.card && d.card.classList.contains('stack') ? 220 : 180);
	s.virtChunks = [];
	s.virtAllRows = rowsForView;
	s.keyToRowIdx = viewData.keyToRowIdx;
	s.vaggaMarkers = viewData.vaggaMarkers;
	s.suttaMarkers = viewData.suttaMarkers;
	s.lastAppliedVaggaIdx = -2;
	s.lastAppliedSuttaIdx = -2;
	var placeFrag = document.createDocumentFragment();
	for (var ci = 0; ci < rowsForView.length; ci += CHUNK_SIZE) {
		var ce = Math.min(ci + CHUNK_SIZE, rowsForView.length);
		var cdiv = document.createElement('div');
		cdiv.className = 'row-chunk';
		cdiv.setAttribute('data-chunk-idx', String(s.virtChunks.length));
		cdiv.style.minHeight = ((ce - ci) * EST_ROW_H) + 'px';
		s.virtChunks.push({ div: cdiv, rowStart: ci, rowEnd: ce, materialized: false, measuredH: 0 });
		placeFrag.appendChild(cdiv);
	}
	d.grid.appendChild(placeFrag);
	// Eager materialize around anchor — with instant scroll fix
	(function eagerAroundAnchor() {
		try {
			var anchorKey = SA.getAnchorKeyFor(id);
			var scroller = SA.getScrollRoot() || d.scrollEl;
			if (!anchorKey) {
				if (scroller && scroller.scrollTop > 0) scroller.scrollTo({ top: 0, behavior: 'instant' });
				materializeChunk(s.virtChunks[0]);
				return;
			}
			var anchorIdx = (s.keyToRowIdx[anchorKey] != null) ? s.keyToRowIdx[anchorKey] : 0;
			var anchorChunkIdx = Math.floor(anchorIdx / CHUNK_SIZE);
			var lo = Math.max(0, anchorChunkIdx - 1);
			var hi = Math.min(s.virtChunks.length - 1, anchorChunkIdx + 1);
			for (var eci = lo; eci <= hi; eci++) materializeChunk(s.virtChunks[eci]);
			var maxScrollY = scroller ? Math.max(0, scroller.scrollHeight - scroller.clientHeight) : 0;
			if (anchorChunkIdx > 0 && scroller && s.virtChunks[anchorChunkIdx] && s.virtChunks[anchorChunkIdx].div && maxScrollY > 10) {
				var chunkRect = s.virtChunks[anchorChunkIdx].div.getBoundingClientRect();
				var rootRect  = scroller.getBoundingClientRect();
				var rawY = (chunkRect.top - rootRect.top) + scroller.scrollTop;
				var maxY = Math.max(0, scroller.scrollHeight - scroller.clientHeight);
				var targetY = Math.max(0, Math.min(rawY, maxY));
				scroller.scrollTo({ top: targetY, behavior: 'instant' });
			}
		} catch (e) {}
	})();
	requestAnimationFrame(function () {
		if (token !== s.renderToken) { s.isRendering = false; return; }
		setupChunkObservers();
		d.grid.setAttribute('aria-busy', 'false');
		requestAnimationFrame(function () {
			SA.updateVisibleCols();
			if (!SA.restoreScrollByAnchor(id)) {
				var _sr = SA.getScrollRoot();
				if (_sr && _sr.scrollTop > 0) _sr.scrollTo({ top: 0, behavior: 'instant' });
			}
			SA.setupAnchorObserver(); SA.updateNavButtons();
			try { SA._dmSaveCurrent(); } catch (_) {}
			try { SA.updateReadingProgress(); } catch (_) {}
			setTimeout(function () {
				s._progScrollUntil = 0;
				s.isRendering = false;
				SA.setTtsUiState('idle');
				if (token === s.renderToken && s.virtChunks.length > 3) {
					var aKey = SA.getAnchorKeyFor(id);
					var aIdx = aKey && s.keyToRowIdx[aKey] != null ? s.keyToRowIdx[aKey] : 0;
					_progressiveFill(_findChunkForRow(aIdx), token);
				}
			}, 500);
		});
		SA.scheduleNextPreload(id);
	});
};

SA.buildSuttaPrintHtml = function (id, merged) {
	var meta = findMetaById(id) || {};
	var rootKey = meta.rootNikaya ? meta.rootNikaya.key : '';
	var skipDupTitle = (rootKey === 'MN' || rootKey === 'DN');
	var cmtOnPli = !!(s.showCmtPli && s.showPali);
	var cmtOnEng = !!(s.showCmtEng && s.showEng);
	var cmtOnVie = !!(s.showCmtVie && s.showVie);
	var cmtHeaders = s.uiLang === 'en'
		? { pali: 'Aṭṭhakathā', eng: 'Commentary', vie: 'Vietnamese Cmt' }
		: { pali: 'Aṭṭhakathā', eng: 'Commentary', vie: 'Chú giải' };
	var title = (d.titleEl && d.titleEl.textContent) || (meta.titleVi || meta.titleEn || id);
	var subtitle = (d.subtitleEl && d.subtitleEl.textContent) || '';
	var supertitle = (d.superTitleEl && d.superTitleEl.textContent) || '';
	var titleMeta = (d.titleMetaEl && d.titleMetaEl.textContent) || '';
	var headers = s.uiLang === 'en'
		? { pali: 'Pali', eng: 'English', vie: 'Vietnamese' }
		: { pali: 'Pali', eng: 'English', vie: 'Tiếng Việt' };
	function esc(sv) { return String(sv == null ? '' : sv).replace(/[&<>"']/g, function (c) { return ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[c]; }); }
	function shortKey(raw) {
		if (!raw) return '';
		if (raw.indexOf(':') !== -1) { var parts = raw.split(':'); var prefix = parts[0].replace(/([a-zA-Z]+)(\d*)/, function (_, l, n) { return l.toUpperCase() + n; }); return parts[1] ? prefix + '.' + parts[1] : prefix; }
		return String(raw).toUpperCase();
	}
	var activeLangCount = (s.showPali ? 1 : 0) + (s.showEng ? 1 : 0) + (s.showVie ? 1 : 0);
	var singleLang = activeLangCount === 1;
	function tag(name) { return singleLang ? '' : '<span class="lang-tag">' + esc(name) + '</span>'; }
	function rowHtml(r) {
		var tp = (r.pali || '').trim(), te = (r.eng || '').trim(), tv = (r.vie || '').trim();
		var keyRaw = String(r.key || '');
		if (skipDupTitle && /:0\.[12]$/.test(keyRaw)) return '';
		var isSectionNum = (tp.length <= 6 && te.length <= 6 && tv.length <= 6) && /^[IVXLCDM]+\.?$|^\d+\.?$/.test(tp || te || tv);
		if (isSectionNum) return '';
		var isSource = /:source$/i.test(keyRaw);
		var isSubtitle = /:0\.[123]$/.test(keyRaw);
		var cls = 'prow';
		if (isSource) cls += ' prow-source';
		if (isSubtitle) cls += ' prow-subtitle';
		var inner = [];
		if (s.showSegKey !== false) inner.push('<div class="prow-key">' + esc(shortKey(keyRaw)) + '</div>');
		if (s.showPali && tp) inner.push('<div class="prow-pali">' + tag(headers.pali) + esc(tp) + '</div>');
		if (s.showEng && te) inner.push('<div class="prow-eng">' + tag(headers.eng) + esc(te) + '</div>');
		if (s.showVie && tv) inner.push('<div class="prow-vie">' + tag(headers.vie) + esc(tv) + '</div>');
		if (!isSubtitle) {
			var cP = cmtOnPli ? resolveCommentLang(r.commentPli) : '';
			var cE = cmtOnEng ? resolveCommentLang(r.commentEn) : '';
			var cV = cmtOnVie ? resolveCommentLang(r.commentVie) : '';
			if (cP) inner.push('<div class="prow-cmt prow-cmt-pli">' + tag(cmtHeaders.pali) + esc(cP) + '</div>');
			if (cE) inner.push('<div class="prow-cmt prow-cmt-eng">' + tag(cmtHeaders.eng) + esc(cE) + '</div>');
			if (cV) inner.push('<div class="prow-cmt prow-cmt-vie">' + tag(cmtHeaders.vie) + esc(cV) + '</div>');
			if (r._merged && Array.isArray(r._mergedComments)) {
				for (var mi = 0; mi < r._mergedComments.length; mi++) {
					var mc = r._mergedComments[mi];
					var mcText = mc && typeof mc.text === 'string' ? mc.text.trim() : '';
					if (!mcText) continue;
					var mcLang = mc && mc.lang;
					var inclMc = (mcLang === 'pali' && cmtOnPli) || (mcLang === 'eng' && cmtOnEng) || (mcLang === 'vie' && cmtOnVie) || (!mcLang && (cmtOnPli || cmtOnEng || cmtOnVie));
					if (!inclMc) continue;
					var mcCls = mcLang === 'pali' ? 'prow-cmt prow-cmt-pli' : mcLang === 'eng' ? 'prow-cmt prow-cmt-eng' : mcLang === 'vie' ? 'prow-cmt prow-cmt-vie' : 'prow-cmt';
					var mcHdr = mcLang === 'pali' ? cmtHeaders.pali : mcLang === 'eng' ? cmtHeaders.eng : mcLang === 'vie' ? cmtHeaders.vie : cmtHeaders.vie;
					inner.push('<div class="' + mcCls + '">' + tag(mcHdr) + esc(mcText) + '</div>');
				}
			}
		}
		if (!inner.length) return '';
		return '<div class="' + cls + '">' + inner.join('') + '</div>';
	}
	var bodyRows = [], srcRows = [];
	merged.rows.forEach(function (r) { if (/:source$/i.test(String(r.key || ''))) srcRows.push(r); else bodyRows.push(r); });
	var rowsHtml = bodyRows.map(rowHtml).join('') + srcRows.map(rowHtml).join('');
	var docTitle = esc((meta.code ? meta.code + ' · ' : '') + title);
	var footerText = s.uiLang === 'en'
		? 'Sutta Archive · Pāli & English: SuttaCentral / Bilara · ID: ' + esc(id)
		: 'Sutta Archive · Pāli & Anh: SuttaCentral / Bilara · Mã bài: ' + esc(id);
	return '<!DOCTYPE html><html lang="' + s.uiLang + '"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>' + docTitle + '</title><style>@page{size:A4;margin:0}*{box-sizing:border-box}html,body{margin:0;padding:0}body{font-family:Cambria,"Noto Serif",Georgia,"Times New Roman",serif;color:#111;line-height:1.55;font-size:11pt;-webkit-print-color-adjust:exact;print-color-adjust:exact}.sheet{width:100%;border-collapse:collapse}.sheet>thead>tr>td,.sheet>tbody>tr>td,.sheet>tfoot>tr>td{padding:0 22mm;vertical-align:top}.pad-top{height:20mm}.pad-bot{height:22mm}.phdr{text-align:center;border-bottom:1px solid #999;padding-bottom:12px;margin-bottom:18px}.phdr .supertitle{font-size:9pt;color:#666;letter-spacing:1.5px;text-transform:uppercase;font-family:Consolas,"Courier New",monospace}.phdr h1{font-size:20pt;margin:6px 0 4px;font-weight:500;letter-spacing:-0.3px}.phdr .subtitle{font-size:11pt;color:#444;font-style:italic}.phdr .meta{font-size:9.5pt;color:#777;margin-top:4px}.prow{margin:0 0 10px;page-break-inside:avoid;break-inside:avoid}.prow-key{font-family:Consolas,"Courier New",monospace;font-size:7.5pt;color:#999;margin-bottom:2px;letter-spacing:0.3px}.prow-pali{font-style:italic;color:#333;margin-bottom:2px}.prow-eng{color:#222;margin-bottom:2px}.prow-vie{color:#111}.lang-tag{display:inline-block;font-family:Consolas,"Courier New",monospace;font-size:7pt;text-transform:uppercase;color:#aaa;margin-right:6px;letter-spacing:0.5px;vertical-align:1px}.prow-subtitle{text-align:center;font-size:12pt;margin-bottom:14px}.prow-subtitle .prow-key,.prow-subtitle .lang-tag{display:none}.prow-cmt{font-size:9.5pt;color:#555;margin:2px 0 2px 12px;padding-left:6px;border-left:2px solid #ddd}.prow-cmt-pli{font-style:italic}.prow-source{font-size:9pt;color:#888;margin-top:18px;padding-top:10px;border-top:1px dashed #ccc}.pftr{margin-top:22px;padding-top:10px;border-top:1px solid #ddd;font-size:8pt;color:#888;text-align:center}@media print{.prow{margin-bottom:9px}}</style></head><body><table class="sheet"><thead><tr><td><div class="pad-top"></div></td></tr></thead><tbody><tr><td><header class="phdr">' +
		(supertitle ? '<div class="supertitle">' + esc(supertitle) + '</div>' : '') +
		'<h1>' + esc(title) + '</h1>' +
		(subtitle && subtitle !== '—' ? '<div class="subtitle">' + esc(subtitle) + '</div>' : '') +
		(titleMeta ? '<div class="meta">' + esc(titleMeta) + '</div>' : '') +
		'</header><main>' + rowsHtml + '</main><footer class="pftr">' + footerText + '</footer></td></tr></tbody><tfoot><tr><td><div class="pad-bot"></div></td></tr></tfoot></table></body></html>';
};
})();

/* ================================================================
   menu.js — Module [7/8]
   Menu thư viện (TOC tree), tìm kiếm bài kinh, bookmark UI,
   share/copy link (segment + title), toast notification,
   event delegation cho grid (copy cột, share segment) và menu
   (mở bài kinh, toggle nhóm, bookmark star).
   ─────────────────────────────────────────────────────────────────
   Exports: SA.buildSutraMenuFromIndex, SA.highlightActiveInMenu,
            SA.syncTileToCurrentSutta, SA.findNikayaOfSutta,
            SA.initDelegations, SA.applyTitleBookmarkState,
            SA.updateBookmarksCount, SA.reflectBookmarkState,
            SA.revealCurrentSuttaInMenu, SA.renderBookmarksList,
            SA.extractGroupKey, SA._showToast
   Depends: utils.js, state.js, ui.js, render.js (SA.findMetaById)
   Runtime calls: SA.openSutra (from js/tts.js)
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;
var $ = SA.$;
var storage = SA.storage;
var escapeHtml = SA.escapeHtml;
var escapeAttr = SA.escapeAttr;
var safeDomId = SA.safeDomId;
var safeCssEscape = SA.safeCssEscape;
var debounce = SA.debounce;
var s = SA.state;
var d = SA.dom;

var sutraMenuPanel = d.sutraMenuPanel;
var sutraMenuList  = d.sutraMenuList;
var searchInput    = d.searchInput;
var searchResultsEl = d.searchResultsEl;

var activeNikayaKey = null;
var KEY_ACTIVE_NIKAYA = 'active_nikaya_tile';

function buildSuttaLinkHtml(meta) {
	var codePrefix = meta.code ? meta.code + ' – ' : '';
	var viLabel = meta.titleVi || '', enLabel = meta.titleEn || '', paliLabel = meta.titlePali || '';
	var mainText, subText;
	if (s.uiLang === 'en') {
		mainText = codePrefix + (enLabel || viLabel || paliLabel || meta.id);
		subText  = paliLabel || viLabel || '';
	} else {
		mainText = codePrefix + (viLabel || enLabel || paliLabel || meta.id);
		subText  = paliLabel || enLabel || '';
	}
	var marked = SA.isBookmarked(meta.id);
	var bl = SA.bookmarkLabels();
	var starTitle = marked ? bl.on : bl.off;
	var starHtml =
		'<button type="button" class="menu-bookmark-btn' + (marked ? ' is-on' : '') + '" ' +
		'data-id="' + escapeAttr(meta.id) + '" aria-pressed="' + (marked ? 'true' : 'false') + '" ' +
		'aria-label="' + escapeAttr(starTitle) + '" title="' + escapeAttr(starTitle) + '">' +
		'<svg viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' +
		'<path d="M10 2.3l2.39 4.84 5.34.78-3.86 3.77.91 5.31L10 14.49l-4.78 2.51.91-5.31L2.27 7.92l5.34-.78z"/>' +
		'</svg></button>';
	return '<a href="#" class="menu-sutta-link" role="treeitem" data-id="' + escapeAttr(meta.id) + '" aria-label="' + escapeAttr(mainText) + '">' +
		'<div class="sutra-label">' +
		'<div class="sutra-label-main">' + escapeHtml(mainText) + '</div>' +
		(subText ? '<div class="sutra-label-sub">' + escapeHtml(subText) + '</div>' : '') +
		'</div>' + starHtml + '</a>';
}

function extractGroupKey(code) {
	var m = String(code || '').match(/^([A-Za-z]+\s*\d+)\s*[–\-]/);
	return m ? m[1].trim() : String(code || '').trim();
}
SA.extractGroupKey = extractGroupKey;

function buildMenuChildren(children, parentId) {
	if (!children || !children.length) return '';
	var allSutta = true;
	for (var k = 0; k < children.length; k++) {
		if (children[k].type !== 'sutta') { allSutta = false; break; }
	}
	if (allSutta) {
		var keys = [], groups = {};
		for (var i = 0; i < children.length; i++) {
			var gk = extractGroupKey(children[i].code);
			if (!groups[gk]) { groups[gk] = []; keys.push(gk); }
			groups[gk].push(children[i]);
		}
		var hasMultiChild = keys.some(function (kk) { return groups[kk].length > 1; });
		if (keys.length >= 2 && hasMultiChild) {
			var html = '';
			for (var j = 0; j < keys.length; j++) {
				var key = keys[j];
				var grpId = safeDomId(parentId + '-' + key.replace(/\s+/g, '_'));
				var subHtml = groups[key].map(buildSuttaLinkHtml).join('');
				html += '<div class="menu-subblock" role="group">' +
					'<button class="menu-toggle nested" type="button" data-target="' + escapeAttr(grpId) + '"' +
					' aria-expanded="false" aria-controls="' + escapeAttr(grpId) + '">' +
					'<span>' + escapeHtml(key) + '</span><span class="chevron" aria-hidden="true">▸</span>' +
					'</button>' +
					'<div id="' + escapeAttr(grpId) + '" class="menu-list collapsed">' + subHtml + '</div>' +
					'</div>';
			}
			return html;
		}
		return children.map(buildSuttaLinkHtml).join('');
	}
	var html2 = '';
	for (var m2 = 0; m2 < children.length; m2++) {
		var child = children[m2];
		if (child.type === 'group') {
			var grpId2 = safeDomId(parentId + '-' + child.key);
			var label = s.uiLang === 'en'
				? child.labelEn || child.labelVi || child.key
				: child.labelVi || child.labelEn || child.key;
			html2 += '<div class="menu-subblock" role="group">' +
				'<button class="menu-toggle nested" type="button" data-target="' + escapeAttr(grpId2) + '"' +
				' aria-expanded="false" aria-controls="' + escapeAttr(grpId2) + '">' +
				'<span>' + escapeHtml(label) + '</span><span class="chevron" aria-hidden="true">▸</span>' +
				'</button>' +
				'<div id="' + escapeAttr(grpId2) + '" class="menu-list collapsed">' + buildMenuChildren(child.children || [], grpId2) + '</div>' +
				'</div>';
		} else if (child.type === 'sutta') {
			html2 += buildSuttaLinkHtml(child);
		}
	}
	return html2;
}

function rebuildGlobalSutraOrder() {
	var index = window.SUTRA_INDEX || [];
	var order = [];
	function walk(children) {
		if (!children || !children.length) return;
		for (var i = 0; i < children.length; i++) {
			var ch = children[i];
			if (ch.type === 'sutta' && ch.id) order.push(ch.id);
			else if (ch.type === 'group') walk(ch.children || []);
		}
	}
	for (var i = 0; i < index.length; i++) walk(index[i].children || []);
	s.SUTRA_ORDER = order;
}

function renderNikayaList(nikKey) {
	if (!sutraMenuList) return;
	var index = window.SUTRA_INDEX || [];
	var nik = null;
	for (var i = 0; i < index.length; i++) {
		if (index[i].key === nikKey) { nik = index[i]; break; }
	}
	if (!nik) { sutraMenuList.innerHTML = ''; return; }
	var secId = safeDomId('sec-' + nik.key);
	sutraMenuList.innerHTML = buildMenuChildren(nik.children || [], secId);
	SA.highlightActiveInMenu();
}

function setActiveNikaya(nikKey, persist) {
	activeNikayaKey = nikKey;
	s.activeNikayaKey = nikKey;
	var tiles = document.querySelectorAll('.nikaya-tile');
	for (var i = 0; i < tiles.length; i++) {
		var isActive = tiles[i].getAttribute('data-nikaya') === nikKey;
		tiles[i].classList.toggle('active', isActive);
		tiles[i].setAttribute('aria-selected', String(isActive));
	}
	if (nikKey === 'BM') renderBookmarksList();
	else renderNikayaList(nikKey);
	if (persist) storage.set(KEY_ACTIVE_NIKAYA, nikKey);
}

function renderBookmarksList() {
	if (!sutraMenuList) return;
	var order = s.SUTRA_ORDER && s.SUTRA_ORDER.length ? s.SUTRA_ORDER : Array.from(SA.BOOKMARKS);
	var ids = order.filter(function (id) { return SA.BOOKMARKS.has(id); });
	if (!ids.length) {
		var bl = SA.bookmarkLabels();
		sutraMenuList.innerHTML = '<li class="menu-empty" style="padding:18px 22px;color:var(--ink-light);font-style:italic;list-style:none">' + escapeHtml(bl.empty) + '</li>';
		return;
	}
	var html = '';
	for (var i = 0; i < ids.length; i++) {
		var meta = SA.findMetaById(ids[i]);
		if (meta) html += buildSuttaLinkHtml(meta);
	}
	sutraMenuList.innerHTML = html;
	SA.highlightActiveInMenu();
}

function updateBookmarksCount() {
	var el = $('bookmarksCount');
	if (!el) return;
	var n = SA.BOOKMARKS.size;
	el.textContent = n ? String(n) : '';
	el.classList.toggle('is-empty', n === 0);
}
SA.updateBookmarksCount = updateBookmarksCount;

function reflectBookmarkState(id, on) {
	var bl = SA.bookmarkLabels();
	var label = on ? bl.on : bl.off;
	if (sutraMenuList) {
		var starsInMenu = sutraMenuList.querySelectorAll('.menu-bookmark-btn[data-id="' + safeCssEscape(id) + '"]');
		for (var i = 0; i < starsInMenu.length; i++) {
			var b = starsInMenu[i];
			b.classList.toggle('is-on', !!on);
			b.setAttribute('aria-pressed', on ? 'true' : 'false');
			b.setAttribute('aria-label', label);
			b.setAttribute('title', label);
		}
	}
	if (id === s.currentSutraId) SA.applyTitleBookmarkState();
	updateBookmarksCount();
}
SA.reflectBookmarkState = reflectBookmarkState;

function applyTitleBookmarkState() {
	var btn = $('btnBookmarkCurrent');
	if (!btn) return;
	if (!s.currentSutraId) { btn.hidden = true; return; }
	btn.hidden = false;
	var on = SA.isBookmarked(s.currentSutraId);
	var bl = SA.bookmarkLabels();
	var label = on ? bl.on : bl.off;
	btn.classList.toggle('is-on', on);
	btn.setAttribute('aria-pressed', on ? 'true' : 'false');
	btn.setAttribute('aria-label', label);
	btn.setAttribute('title', label);
	applyShareBtnVisibility();
}
SA.applyTitleBookmarkState = applyTitleBookmarkState;

function applyShareBtnVisibility() {
	var b = $('btnShareCurrent');
	if (!b) return;
	b.hidden = !s.currentSutraId;
}

function _buildTitleShareUrl() {
	if (!s.currentSutraId) return location.href;
	return location.origin + location.pathname + '#' + s.currentSutraId;
}

function _getShareTitle() {
	var t = ($('title') || {}).textContent || '';
	t = t.trim();
	return t && t !== 'Chưa chọn bài' ? t : 'Sutta Archive';
}

function _showToast(msg) {
	var t = document.getElementById('appToast');
	if (!t) {
		t = document.createElement('div');
		t.id = 'appToast';
		t.className = 'app-toast';
		t.setAttribute('role', 'status');
		t.setAttribute('aria-live', 'polite');
		document.body.appendChild(t);
	}
	t.textContent = msg;
	t.classList.remove('show');
	// eslint-disable-next-line no-unused-expressions
	t.offsetWidth;
	clearTimeout(_showToast._tm);
	clearTimeout(_showToast._tm2);
	requestAnimationFrame(function () { t.classList.add('show'); });
	_showToast._tm = setTimeout(function () { t.classList.remove('show'); }, 1800);
}
SA._showToast = _showToast;

function _closeShareMenu() {
	var menu = $('shareMenu');
	var btn = $('btnShareCurrent');
	if (!menu || !btn) return;
	menu.setAttribute('aria-hidden', 'true');
	setTimeout(function () { if (menu.getAttribute('aria-hidden') === 'true') menu.hidden = true; }, 200);
	btn.setAttribute('aria-expanded', 'false');
}

function _openShareMenu() {
	var menu = $('shareMenu');
	var btn = $('btnShareCurrent');
	if (!menu || !btn) return;
	menu.hidden = false;
	var rect = btn.getBoundingClientRect();
	var menuH = (typeof navigator.share === 'function') ? 280 : 240;
	var spaceBelow = window.innerHeight - rect.bottom;
	var top = spaceBelow > menuH + 16 ? rect.bottom + 8 : Math.max(8, rect.top - menuH - 8);
	var left = Math.max(8, Math.min(rect.left, window.innerWidth - 220));
	menu.style.top = top + 'px';
	menu.style.left = left + 'px';
	var nativeItem = $('shareItemNative');
	if (nativeItem) nativeItem.hidden = !(typeof navigator.share === 'function');
	requestAnimationFrame(function () {
		menu.setAttribute('aria-hidden', 'false');
		btn.setAttribute('aria-expanded', 'true');
	});
}

async function _handleShareAction(action) {
	var url = _buildTitleShareUrl();
	var title = _getShareTitle();
	var text = title + ' — Sutta Archive';
	var en = s.uiLang === 'en';
	if (action === 'native' && typeof navigator.share === 'function') {
		try { await navigator.share({ title: title, url: url }); } catch (_) {}
		_closeShareMenu(); return;
	}
	if (action === 'copy') {
		var msg = en ? 'Link copied' : 'Đã sao chép link';
		try { await navigator.clipboard.writeText(url); _showToast(msg); }
		catch (_) {
			var ta = document.createElement('textarea');
			ta.value = url; ta.style.position = 'fixed'; ta.style.left = '-9999px';
			document.body.appendChild(ta); ta.select();
			try { document.execCommand('copy'); _showToast(msg); } catch (__) {}
			ta.remove();
		}
		_closeShareMenu(); return;
	}
	var pop = 'noopener,noreferrer,width=620,height=520';
	if (action === 'facebook') {
		window.open('https://www.facebook.com/sharer/sharer.php?u=' + encodeURIComponent(url), '_blank', pop);
	} else if (action === 'zalo') {
		window.open('https://zalo.me/share/?u=' + encodeURIComponent(url) + '&t=' + encodeURIComponent(text), '_blank', pop);
	} else if (action === 'twitter') {
		window.open('https://twitter.com/intent/tweet?url=' + encodeURIComponent(url) + '&text=' + encodeURIComponent(text), '_blank', pop);
	} else if (action === 'email') {
		location.href = 'mailto:?subject=' + encodeURIComponent(text) + '&body=' + encodeURIComponent(text + '\n\n' + url);
	}
	_closeShareMenu();
}

(function _wireShareUI() {
	var btn = $('btnShareCurrent');
	var menu = $('shareMenu');
	if (!btn || !menu) return;
	btn.addEventListener('click', function (e) {
		e.stopPropagation();
		var isOpen = menu.getAttribute('aria-hidden') !== 'true' && !menu.hidden;
		if (isOpen) _closeShareMenu(); else _openShareMenu();
	});
	menu.addEventListener('click', function (e) {
		var item = e.target.closest('.share-menu-item');
		if (!item) return;
		e.stopPropagation();
		var action = item.getAttribute('data-share');
		if (action) _handleShareAction(action);
	});
	document.addEventListener('click', function (e) {
		if (menu.hidden || menu.getAttribute('aria-hidden') === 'true') return;
		if (e.target.closest('#shareMenu') || e.target.closest('#btnShareCurrent')) return;
		_closeShareMenu();
	});
	document.addEventListener('keydown', function (e) {
		if (e.key === 'Escape' && !menu.hidden && menu.getAttribute('aria-hidden') !== 'true') {
			_closeShareMenu(); btn.focus();
		}
	});
	window.addEventListener('resize', function () {
		if (!menu.hidden && menu.getAttribute('aria-hidden') !== 'true') _closeShareMenu();
	});
})();

function findNikayaOfSutta(suttaId) {
	var index = window.SUTRA_INDEX || [];
	for (var i = 0; i < index.length; i++) {
		var nik = index[i];
		var found = false;
		(function walk(children) {
			if (!children || found) return;
			for (var j = 0; j < children.length; j++) {
				var ch = children[j];
				if (ch.type === 'sutta' && ch.id === suttaId) { found = true; return; }
				if (ch.type === 'group') walk(ch.children || []);
			}
		})(nik.children || []);
		if (found) return nik.key;
	}
	return null;
}
SA.findNikayaOfSutta = findNikayaOfSutta;

function applyTileLabels() {
	var tiles = document.querySelectorAll('.nikaya-tile .tile-label');
	for (var i = 0; i < tiles.length; i++) {
		var el = tiles[i];
		el.textContent = s.uiLang === 'en' ? (el.getAttribute('data-en') || '') : (el.getAttribute('data-vi') || '');
	}
}

function normStr(str) {
	return String(str || '').normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase();
}

function populateFlatSuttas() {
	s.FLAT_SUTTAS = [];
	var index = window.SUTRA_INDEX || [];
	function walk(children) {
		if (!children || !children.length) return;
		for (var i = 0; i < children.length; i++) {
			var ch = children[i];
			if (ch.type === 'sutta' && ch.id) {
				var codePrefix = ch.code ? ch.code + ' – ' : '';
				var viLabel = ch.titleVi || '', enLabel = ch.titleEn || '', paliLabel = ch.titlePali || '';
				var mainText = s.uiLang === 'en'
					? codePrefix + (enLabel || viLabel || paliLabel || ch.id)
					: codePrefix + (viLabel || enLabel || paliLabel || ch.id);
				var subText = s.uiLang === 'en' ? (paliLabel || viLabel || '') : (paliLabel || enLabel || '');
				s.FLAT_SUTTAS.push({
					id: ch.id, main: mainText, sub: subText,
					flat: normStr(mainText + ' ' + viLabel + ' ' + enLabel + ' ' + paliLabel + ' ' + ch.id)
				});
			}
			else if (ch.type === 'group') walk(ch.children || []);
		}
	}
	for (var i = 0; i < index.length; i++) walk(index[i].children || []);
}

function buildSutraMenuFromIndex() {
	var index = window.SUTRA_INDEX || [];
	if (!Array.isArray(index) || !index.length) {
		if (sutraMenuList) {
			var msg = s.uiLang === 'en' ? 'No sutta index found. Make sure toc.js is loaded.' : 'Chưa có mục lục. Hãy đảm bảo file toc.js đã được tải.';
			sutraMenuList.innerHTML = '<li style="padding:10px;color:var(--ink-light);font-style:italic">' + escapeHtml(msg) + '</li>';
		}
		return;
	}
	rebuildGlobalSutraOrder();
	populateFlatSuttas();
	applyTileLabels();
	if (sutraMenuPanel && !sutraMenuPanel._tileWired) {
		sutraMenuPanel._tileWired = true;
		sutraMenuPanel.addEventListener('click', function (e) {
			var tile = e.target.closest('.nikaya-tile');
			if (!tile || !sutraMenuPanel.contains(tile)) return;
			e.stopPropagation();
			var k = tile.getAttribute('data-nikaya');
			if (k) setActiveNikaya(k, true);
		});
	}
	var initial = null;
	if (s.currentSutraId) initial = findNikayaOfSutta(s.currentSutraId);
	if (!initial) initial = storage.get(KEY_ACTIVE_NIKAYA);
	if (!initial && index[0]) initial = index[0].key;
	setActiveNikaya(initial, false);
}
SA.buildSutraMenuFromIndex = buildSutraMenuFromIndex;

function highlightActiveInMenu() {
	if (!sutraMenuList) return;
	sutraMenuList.querySelectorAll('.menu-sutta-link').forEach(function (a) {
		var isActive = a.getAttribute('data-id') === s.currentSutraId;
		a.classList.toggle('active', isActive);
		a.setAttribute('aria-current', isActive ? 'page' : 'false');
	});
}
SA.highlightActiveInMenu = highlightActiveInMenu;

function syncTileToCurrentSutta() {
	if (!s.currentSutraId) return;
	var nik = findNikayaOfSutta(s.currentSutraId);
	if (nik && nik !== activeNikayaKey) setActiveNikaya(nik, true);
}
SA.syncTileToCurrentSutta = syncTileToCurrentSutta;

function renderSearchResults(matches, q) {
	if (!searchResultsEl) return;
	if (!q) { searchResultsEl.innerHTML = ''; return; }
	if (!matches.length) {
		var msg = s.uiLang === 'en' ? 'No matching sutta found.' : 'Không tìm thấy kinh phù hợp.';
		searchResultsEl.innerHTML = '<div class="search-result-empty" role="status">' + escapeHtml(msg) + '</div>';
		return;
	}
	var html = '';
	for (var i = 0; i < matches.length; i++) {
		var m = matches[i];
		html += '<button class="search-result-item" data-id="' + escapeAttr(m.id) + '" role="option">' +
			'<span class="search-main">' + escapeHtml(m.main) + '</span>' +
			(m.sub ? '<span class="search-sub">' + escapeHtml(m.sub) + '</span>' : '') +
			'</button>';
	}
	searchResultsEl.innerHTML = html;
}

function applySearch(query) {
	var q = normStr((query || '').trim());
	if (!q) return renderSearchResults([], '');
	var matches = s.FLAT_SUTTAS.filter(function (x) { return x.flat.includes(q); }).slice(0, 80);
	renderSearchResults(matches, query);
}

if (searchInput) searchInput.addEventListener('input', debounce(function (e) { applySearch(e.target.value); }, 180));

function _buildShareUrl(keyRaw) {
	return location.origin + location.pathname + '#' + keyRaw;
}

function shareSegment(keyRaw) {
	if (!keyRaw) return;
	var url = _buildShareUrl(keyRaw);
	var titleText = (document.getElementById('title') || {}).textContent || 'Sutta Archive';
	if (navigator.share) {
		navigator.share({ title: titleText, url: url }).catch(function () {});
		return;
	}
	var done = function () { _showToast(s.uiLang === 'en' ? 'Link copied' : 'Đã sao chép link'); };
	var fail = function () { _showToast(s.uiLang === 'en' ? 'Copy failed' : 'Sao chép thất bại'); };
	if (navigator.clipboard && navigator.clipboard.writeText) {
		navigator.clipboard.writeText(url).then(done).catch(function () {
			_legacyCopy(url) ? done() : fail();
		});
	} else {
		_legacyCopy(url) ? done() : fail();
	}
}

function _legacyCopy(text) {
	try {
		var ta = document.createElement('textarea');
		ta.value = text;
		ta.setAttribute('readonly', '');
		ta.setAttribute('aria-hidden', 'true');
		ta.style.position = 'fixed'; ta.style.left = '-9999px';
		ta.style.fontSize = '16px';
		document.body.appendChild(ta);
		ta.select();
		var ok = document.execCommand('copy');
		document.body.removeChild(ta);
		return ok;
	} catch (e) { return false; }
}

function initDelegations() {
	var grid = d.grid;
	if (grid && !grid._shareDel) {
		grid.addEventListener('click', function (ev) {
			var btn = ev.target.closest('.sutra-seg-share');
			if (btn && grid.contains(btn)) {
				ev.preventDefault();
				ev.stopPropagation();
				shareSegment(btn.getAttribute('data-share-key'));
				return;
			}
			var copyBtn = ev.target.closest('.sutra-col-copy');
			if (copyBtn && grid.contains(copyBtn)) {
				ev.preventDefault();
				ev.stopPropagation();
				var col = copyBtn.closest('.sutra-col');
				if (!col) return;
				var body = col.querySelector('.sutra-col-body');
				var text = body ? (body.textContent || '').trim() : '';
				if (!text) return;
				var done = function () { _showToast(s.uiLang === 'en' ? 'Text copied' : 'Đã sao chép'); };
				var fail = function () { _showToast(s.uiLang === 'en' ? 'Copy failed' : 'Sao chép thất bại'); };
				if (navigator.clipboard && navigator.clipboard.writeText) {
					navigator.clipboard.writeText(text).then(done).catch(function () { _legacyCopy(text) ? done() : fail(); });
				} else {
					_legacyCopy(text) ? done() : fail();
				}
			}
		});
		grid._shareDel = true;
	}
	if (sutraMenuList && !sutraMenuList._del) {
		sutraMenuList.addEventListener('click', function (ev) {
			var starBtn = ev.target.closest('.menu-bookmark-btn');
			if (starBtn && sutraMenuList.contains(starBtn)) {
				ev.preventDefault();
				ev.stopPropagation();
				var sid = starBtn.getAttribute('data-id');
				if (sid) {
					var now = SA.toggleBookmark(sid);
					reflectBookmarkState(sid, now);
					if (activeNikayaKey === 'BM' && !now) renderBookmarksList();
				}
				return;
			}
			var btn = ev.target.closest('.menu-toggle');
			if (btn && sutraMenuList.contains(btn)) {
				ev.preventDefault();
				var panel = document.getElementById(btn.dataset.target);
				if (!panel) return;
				var wasCollapsed = panel.classList.contains('collapsed');
				var isNested = btn.classList.contains('nested');
				if (wasCollapsed) {
					var scope = isNested ? btn.closest('.menu-list') : sutraMenuList;
					var sel   = isNested ? '.menu-toggle.nested' : '.menu-toggle:not(.nested)';
					if (scope) scope.querySelectorAll(sel).forEach(function (o) {
						if (o === btn) return;
						var op = document.getElementById(o.dataset.target);
						if (op && !op.classList.contains('collapsed')) {
							op.classList.add('collapsed');
							o.setAttribute('aria-expanded', 'false');
							var c = o.querySelector('.chevron'); if (c) c.textContent = '▸';
							if (!isNested) op.querySelectorAll('.menu-toggle.nested').forEach(function (nb) {
								var np = document.getElementById(nb.dataset.target);
								if (np && !np.classList.contains('collapsed')) np.classList.add('collapsed');
								nb.setAttribute('aria-expanded', 'false');
								var c2 = nb.querySelector('.chevron'); if (c2) c2.textContent = '▸';
							});
						}
					});
				}
				panel.classList.toggle('collapsed', !wasCollapsed);
				var isCollapsedNow = panel.classList.contains('collapsed');
				btn.setAttribute('aria-expanded', isCollapsedNow ? 'false' : 'true');
				var ch = btn.querySelector('.chevron');
				if (ch) ch.textContent = isCollapsedNow ? '▸' : '▾';
				return;
			}
			var a = ev.target.closest('.menu-sutta-link');
			if (a && sutraMenuList.contains(a)) {
				ev.preventDefault();
				var id = a.getAttribute('data-id');
				if (id) {
					SA.openSutra(id);
					SA.closePanels();
					if (searchInput) searchInput.value = '';
					if (searchResultsEl) searchResultsEl.innerHTML = '';
				}
			}
		});
		sutraMenuList._del = true;
	}
	if (searchResultsEl && !searchResultsEl._del) {
		searchResultsEl.addEventListener('click', function (ev) {
			var btn = ev.target.closest('.search-result-item');
			if (btn) {
				var id = btn.getAttribute('data-id');
				if (id) {
					SA.openSutra(id);
					SA.closePanels();
					if (searchInput) searchInput.value = '';
					searchResultsEl.innerHTML = '';
				}
			}
		});
		searchResultsEl._del = true;
	}
}
SA.initDelegations = initDelegations;
SA.renderBookmarksList = renderBookmarksList;

function revealCurrentSuttaInMenu() {
	if (!s.currentSutraId || !sutraMenuPanel || !sutraMenuList) return;
	SA.togglePanel(d.settingsPanel, false);
	var nikOfCurrent = findNikayaOfSutta(s.currentSutraId);
	if (nikOfCurrent && nikOfCurrent !== activeNikayaKey) setActiveNikaya(nikOfCurrent, true);
	SA.togglePanel(sutraMenuPanel, true);
	setTimeout(function () {
		var link = sutraMenuList.querySelector('.menu-sutta-link[data-id="' + safeCssEscape(s.currentSutraId) + '"]');
		if (!link) return;
		var node = link.parentElement;
		while (node && node !== sutraMenuList) {
			if (node.classList && node.classList.contains('menu-list') && node.classList.contains('collapsed')) {
				node.classList.remove('collapsed');
				var panelId = node.id;
				if (panelId) {
					var toggle = sutraMenuList.querySelector('.menu-toggle[data-target="' + panelId + '"]');
					if (toggle) {
						toggle.setAttribute('aria-expanded', 'true');
						var chev = toggle.querySelector('.chevron');
						if (chev) chev.textContent = '▾';
					}
				}
			}
			node = node.parentElement;
		}
		try { link.scrollIntoView({ behavior: 'smooth', block: 'center' }); } catch (e) { link.scrollIntoView(); }
	}, 240);
}
SA.revealCurrentSuttaInMenu = revealCurrentSuttaInMenu;
})();

/* ================================================================
   tts.js — Module [8/8] — Entry point
   Chứa init(): khởi tạo toàn bộ app, wire event listeners.
   TTS (lazy-load module tts.js ở root), print PDF, nav buttons
   (prev/next), dark mode toggle, touch device detection,
   nuclear sticky-hover fix cho mobile.
   ─────────────────────────────────────────────────────────────────
   Exports: SA.openSutra, SA.resetTts, SA.setTtsUiState,
            SA.updateNavButtons, SA.scheduleNextPreload
   Depends: tất cả các module trước (utils → menu)
   ─────────────────────────────────────────────────────────────────
   init() flow:
     initUiLang → loadViewPrefs → sync button states →
     loadBookmarks → applyVisibility → buildSutraMenuFromIndex →
     initDelegations → openSutra(lastId) hoặc renderWelcomeScreen
   ================================================================ */
(function () {
'use strict';
var SA = window.SA;
var $ = SA.$;
var storage = SA.storage;
var s = SA.state;
var d = SA.dom;

function scheduleNextPreload(currentId) {
	try {
		var idx = s.SUTRA_ORDER.indexOf(currentId); if (idx === -1) return;
		var conn = navigator.connection || navigator.mozConnection || navigator.webkitConnection;
		if (conn && conn.saveData) return;
		if (navigator.deviceMemory && navigator.deviceMemory < 2) return;
		var nextId = s.SUTRA_ORDER[idx + 1];
		var prevId = idx > 0 ? s.SUTRA_ORDER[idx - 1] : null;
		var doPreload = function () {
			if (nextId) SA.loadMerged(nextId).catch(function () {});
			if (prevId) SA.loadMerged(prevId).catch(function () {});
		};
		if (!nextId && !prevId) return;
		if ('requestIdleCallback' in window) requestIdleCallback(doPreload, { timeout: 2000 });
		else setTimeout(doPreload, 800);
	} catch(e){}
}
SA.scheduleNextPreload = scheduleNextPreload;

function openSutra(id) {
	if (id) id = SA._resolveSegPrefixToSuttaId(id);
	document.documentElement.classList.remove('is-welcome');
	SA.renderSutra(id);
}
SA.openSutra = openSutra;

(function wireBookmarkCurrent() {
	var btn = $('btnBookmarkCurrent');
	if (!btn) return;
	btn.addEventListener('click', function (e) {
		e.stopPropagation();
		if (!s.currentSutraId) return;
		var now = SA.toggleBookmark(s.currentSutraId);
		SA.reflectBookmarkState(s.currentSutraId, now);
		if (s.activeNikayaKey === 'BM') SA.renderBookmarksList();
	});
})();

function printCurrentSuttaPdf() {
	if (!s.currentSutraId) return;
	var id = s.currentSutraId;
	var merged = SA.MERGED_CACHE.get(id);
	var ensure = merged ? Promise.resolve(merged) : SA.loadMerged(id);
	Promise.resolve(ensure).then(function (m) {
		if (!m || !m.rows || !m.rows.length) return;
		var html = SA.buildSuttaPrintHtml(id, m);
		var iframe = document.createElement('iframe');
		iframe.setAttribute('aria-hidden', 'true');
		iframe.style.cssText = 'position:fixed;right:0;bottom:0;width:0;height:0;border:0;opacity:0;pointer-events:none;';
		document.body.appendChild(iframe);
		var printed = false;
		var cleanedUp = false;
		var cleanup = function () {
			if (cleanedUp) return; cleanedUp = true;
			if (iframe.parentNode) iframe.parentNode.removeChild(iframe);
		};
		var doPrint = function () {
			if (printed) return; printed = true;
			try {
				iframe.contentWindow.focus();
				iframe.contentWindow.onafterprint = function () { setTimeout(cleanup, 1000); };
				window.addEventListener('focus', function onReturn() {
					window.removeEventListener('focus', onReturn);
					setTimeout(cleanup, 1500);
				}, { once: true });
				iframe.contentWindow.print();
			} catch (e) { console.warn(e); cleanup(); }
			setTimeout(cleanup, 180000);
		};
		iframe.onload = function () { setTimeout(doPrint, 400); };
		var doc = iframe.contentWindow.document;
		doc.open(); doc.write(html); doc.close();
		setTimeout(doPrint, 2000);
	}).catch(function (e) { console.warn('[PDF] generation failed', e); });
}

var btnPrintPdf = $('btnPrintPdf');
if (btnPrintPdf) btnPrintPdf.onclick = function () {
	if (!s.currentSutraId) return;
	SA.togglePanel(d.settingsPanel, false);
	printCurrentSuttaPdf();
};

var btnPrev = $('btnPrev');
var btnNext = $('btnNext');

function updateNavButtons() {
	var idx = s.SUTRA_ORDER.indexOf(s.currentSutraId);
	var navTitle = $('navTitle');
	if (!s.currentSutraId || !Array.isArray(s.SUTRA_ORDER) || !s.SUTRA_ORDER.length || idx === -1) {
		if (btnPrev)  btnPrev.disabled  = true;
		if (btnNext)  btnNext.disabled  = true;
		if (navTitle) navTitle.textContent = '—';
		return;
	}
	if (btnPrev) btnPrev.disabled = !(idx > 0);
	if (btnNext) btnNext.disabled = !(idx < s.SUTRA_ORDER.length - 1);
	if (navTitle) {
		var meta  = SA.findMetaById(s.currentSutraId);
		var shortCode = SA.extractGroupKey(meta && meta.code);
		var title = s.uiLang === 'en'
			? (meta && meta.titleEn) || (meta && meta.titleVi) || (meta && meta.titlePali) || s.currentSutraId
			: (meta && meta.titleVi) || (meta && meta.titleEn) || (meta && meta.titlePali) || s.currentSutraId;
		navTitle.textContent = shortCode ? (shortCode + ' · ' + title) : title;
		navTitle.style.cursor = 'pointer';
		navTitle.setAttribute('role', 'button');
		navTitle.setAttribute('title', (meta && meta.code ? meta.code + ' · ' : '') + title);
	}
}
SA.updateNavButtons = updateNavButtons;

var navTitleEl = $('navTitle');
if (navTitleEl) {
	navTitleEl.setAttribute('tabindex', '0');
	navTitleEl.addEventListener('click', function (e) {
		e.stopPropagation();
		if (s.currentSutraId) SA.revealCurrentSuttaInMenu();
	});
	navTitleEl.addEventListener('keydown', function (e) {
		if ((e.key === 'Enter' || e.key === ' ') && s.currentSutraId) {
			e.preventDefault();
			SA.revealCurrentSuttaInMenu();
		}
	});
}

function _blurIfMouse(btn) {
	try { if (!btn.matches(':focus-visible')) btn.blur(); } catch(_){}
}

if (btnPrev) btnPrev.onclick = function () {
	var idx = s.SUTRA_ORDER.indexOf(s.currentSutraId);
	if (idx > 0) openSutra(s.SUTRA_ORDER[idx - 1]);
	_blurIfMouse(btnPrev);
};
if (btnNext) btnNext.onclick = function () {
	var idx = s.SUTRA_ORDER.indexOf(s.currentSutraId);
	if (idx !== -1 && idx < s.SUTRA_ORDER.length - 1) openSutra(s.SUTRA_ORDER[idx + 1]);
	_blurIfMouse(btnNext);
};

var synthSupported = 'speechSynthesis' in window;

function setTtsUiState(state) {
	var btnReadTts  = d.btnReadTts;
	var btnPauseTts = d.btnPauseTts;
	var btnStopTts  = d.btnStopTts;
	if (!btnReadTts || !btnPauseTts || !btnStopTts) return;
	if (!synthSupported || s.isRendering) {
		btnReadTts.disabled = btnPauseTts.disabled = btnStopTts.disabled = true; return;
	}
	if (state === 'idle')    { btnReadTts.disabled=false; btnPauseTts.disabled=true;  btnStopTts.disabled=true; }
	if (state === 'playing') { btnReadTts.disabled=true;  btnPauseTts.disabled=false; btnStopTts.disabled=false; }
	if (state === 'paused')  { btnReadTts.disabled=false; btnPauseTts.disabled=true;  btnStopTts.disabled=false; }
}
SA.setTtsUiState = setTtsUiState;

function clearRowHighlight() {
	if (!d.grid) return;
	var reading = d.grid.querySelectorAll('.sutra-row.reading');
	for (var k = 0; k < reading.length; k++) reading[k].classList.remove('reading');
}

function highlightRowAt(index) {
	clearRowHighlight();
	if (index < 0 || index >= s.virtAllRows.length) return;
	SA.ensureRowRendered(index);
	var row = s.cachedRows[index];
	if (!row) return;
	row.classList.add('reading');
	if (!d.scrollEl) return;
	var rootRect = d.scrollEl.getBoundingClientRect();
	var rowRect  = row.getBoundingClientRect();
	var relativeTop = rowRect.top - rootRect.top + d.scrollEl.scrollTop;
	var viewTop = d.scrollEl.scrollTop, viewBottom = viewTop + d.scrollEl.clientHeight;
	if (relativeTop < viewTop || relativeTop + rowRect.height > viewBottom) {
		d.scrollEl.scrollTo({ top: Math.max(0, relativeTop - 20), behavior: 'auto' });
	}
}

var _ttsModulePromise = null;
var _ttsApi = null;

function loadScript(src) {
	return new Promise(function (resolve, reject) {
		var sc = document.createElement('script');
		sc.src = src; sc.async = true;
		sc.onload = function () { resolve(); };
		sc.onerror = function (e) { reject(e); };
		document.head.appendChild(sc);
	});
}

function ensureTTSLoaded() {
	if (_ttsApi) return Promise.resolve(_ttsApi);
	if (!_ttsModulePromise) {
		_ttsModulePromise = loadScript('tts.js').then(function () {
			if (!window.TTSModule) throw new Error('TTSModule failed to load');
			_ttsApi = window.TTSModule.init({
				getVirtAllRows: function () { return s.virtAllRows; },
				getCurrentSutraId: function () { return s.currentSutraId; },
				getUiLang: function () { return s.uiLang; },
				getIsRendering: function () { return s.isRendering; },
				clearRowHighlight: clearRowHighlight,
				highlightRowAt: highlightRowAt,
				setTtsUiState: setTtsUiState,
				storage: storage,
			});
			return _ttsApi;
		});
	}
	return _ttsModulePromise;
}

function resetTts(clearHighlight, clearStorage) {
	if (synthSupported && window.speechSynthesis) { try { window.speechSynthesis.cancel(); } catch(e){} }
	if (_ttsApi) { try { _ttsApi.reset(clearHighlight, clearStorage); return; } catch(e){} }
	if (clearHighlight) clearRowHighlight();
	if (clearStorage && s.currentSutraId) storage.remove('tts_state_' + s.currentSutraId);
	setTtsUiState('idle');
}
SA.resetTts = resetTts;

if (d.btnReadTts) d.btnReadTts.onclick = async function () {
	try { var api = await ensureTTSLoaded(); api.start(); } catch (e) { console.error('TTS load failed:', e); }
};
if (d.btnPauseTts) d.btnPauseTts.onclick = function () { if (_ttsApi) _ttsApi.pause(); };
if (d.btnStopTts)  d.btnStopTts.onclick  = function () { if (_ttsApi) _ttsApi.stop(); };

function initUiLang() {
	SA.renderUiLangFlag(); SA.applyUiLanguageToSearchUi(); SA.applyUiLanguageToSettingsPanel(); SA.renderGuideDialog();
	var btnUiLang = d.btnUiLang;
	if (!btnUiLang) return;
	btnUiLang.addEventListener('click', function (e) {
		e.stopPropagation();
		s.uiLang = s.uiLang === 'vi' ? 'en' : 'vi';
		storage.set(s.LANG_STORAGE_KEY, s.uiLang); window.SUTRA_UI_LANG = s.uiLang;
		SA.renderUiLangFlag(); SA.applyUiLanguageToSearchUi(); SA.applyUiLanguageToSettingsPanel(); SA.renderGuideDialog();
		SA.buildSutraMenuFromIndex(); SA.highlightActiveInMenu(); updateNavButtons();
		SA.updateBookmarksCount(); SA.applyTitleBookmarkState();
		if (s.currentSutraId) SA.renderSutra(s.currentSutraId); else SA.renderWelcomeScreen();
	});
}

function init() {
	if (!d.grid || !d.titleEl || !d.subtitleEl || !d.card) console.warn('Sutta app: core DOM missing.');
	initUiLang(); SA.loadViewPrefs();
	if (d.btnPali) { d.btnPali.classList.toggle('active', s.showPali); d.btnPali.setAttribute('aria-pressed', String(s.showPali)); }
	if (d.btnEng)  { d.btnEng.classList.toggle('active',  s.showEng);  d.btnEng.setAttribute('aria-pressed',  String(s.showEng)); }
	if (d.btnVie)  { d.btnVie.classList.toggle('active',  s.showVie);  d.btnVie.setAttribute('aria-pressed',  String(s.showVie)); }
	if (d.btnLayout) { d.btnLayout.classList.toggle('active', d.card ? d.card.classList.contains('stack') : false); }
	var _bsk = $('btnSegKey'); if (_bsk) { _bsk.classList.toggle('active', s.showSegKey); _bsk.setAttribute('aria-pressed', String(s.showSegKey)); }
	var _bsh = $('btnSegHdr'); if (_bsh) { _bsh.classList.toggle('active', s.showColHdr); _bsh.setAttribute('aria-pressed', String(s.showColHdr)); }
	SA.syncCmtButtons();
	var _bhp = $('btnHlPli'); if (_bhp) { _bhp.classList.toggle('active', s.hlPli); _bhp.setAttribute('aria-pressed', String(s.hlPli)); }
	var _bhe = $('btnHlEng'); if (_bhe) { _bhe.classList.toggle('active', s.hlEng); _bhe.setAttribute('aria-pressed', String(s.hlEng)); }
	var _bhv = $('btnHlVie'); if (_bhv) { _bhv.classList.toggle('active', s.hlVie); _bhv.setAttribute('aria-pressed', String(s.hlVie)); }
	var _b3c = $('btn3Cols'); if (_b3c) { _b3c.classList.toggle('active', s.show3Cols); _b3c.setAttribute('aria-pressed', String(s.show3Cols)); }
	SA.loadBookmarks();
	SA.applyVisibility(); SA.applySegKeyHdrVis(); SA.loadZoom(); SA.loadLineHeight(); SA.buildSutraMenuFromIndex(); SA.initDelegations();
	SA.updateBookmarksCount();
	var startId = storage.get(SA.KEY_LAST);
	var _bootHash = SA._parseAnchorHash();
	if (_bootHash && _bootHash.sutta) startId = _bootHash.sutta;
	if (startId) openSutra(startId); else SA.renderWelcomeScreen();
	window.addEventListener('hashchange', function () {
		var h = SA._parseAnchorHash();
		if (!h) return;
		if (h.sutta !== s.currentSutraId) openSutra(h.sutta);
		else SA.restoreScrollByAnchor(s.currentSutraId);
	});
	if (!synthSupported) {
		[d.btnReadTts, d.btnPauseTts, d.btnStopTts].forEach(function (b) { if (b) b.disabled = true; });
	} else {
		setTtsUiState('idle');
	}
	SA.updateMenuPanelTop();
	initDebugPanel();
}

function initDebugPanel() {
	var btnDebug = $('btnDebug');
	var debugPanel = $('debugPanel');
	var debugBody = $('debugBody');
	var btnDebugClose = $('btnDebugClose');
	if (!btnDebug || !debugPanel || !debugBody) return;
	if (!s.DEBUG) {
		btnDebug.style.visibility = 'hidden';
		btnDebug.setAttribute('aria-hidden', 'true');
		btnDebug.setAttribute('tabindex', '-1');
		debugPanel.hidden = true;
		return;
	}
	btnDebug.style.visibility = '';
	var visible = false;
	var timer = null;
	var lastFrameT = performance.now();
	var fps = 0;
	(function tickFps() {
		var now = performance.now();
		var dt = now - lastFrameT;
		lastFrameT = now;
		if (dt > 0) fps = Math.round(1000 / dt);
		requestAnimationFrame(tickFps);
	})();
	function fmtBytes(n) {
		if (!Number.isFinite(n)) return '-';
		if (n > 1024*1024) return (n / (1024*1024)).toFixed(1) + ' MB';
		if (n > 1024) return (n / 1024).toFixed(1) + ' KB';
		return n + ' B';
	}
	function update() {
		if (!visible) return;
		var allDom = document.getElementsByTagName('*').length;
		var wraps = d.grid ? d.grid.querySelectorAll('.sutra-row-wrap') : [];
		var sc = d.grid;
		var sh = sc ? sc.scrollHeight : 0;
		var st = sc ? sc.scrollTop : 0;
		var ch = sc ? sc.clientHeight : 0;
		var scrollPct = sh > ch ? Math.round(st / (sh - ch) * 100) : 0;
		var mem = (performance && performance.memory) ? performance.memory : null;
		var lsKey = s.currentSutraId ? ('scroll_anchor_key_' + s.currentSutraId) : '';
		var lsRaw = null;
		try { lsRaw = lsKey ? localStorage.getItem(lsKey) : null; } catch(e) {}
		var anchorKey = s.currentSutraId ? SA.anchorGet(SA.KEY_ANCHOR_K(s.currentSutraId)) : null;
		var allAnchors = [];
		try {
			for (var lsi = 0; lsi < localStorage.length; lsi++) {
				var k = localStorage.key(lsi);
				if (k && k.indexOf('scroll_anchor_key_') === 0) {
					allAnchors.push(k.replace('scroll_anchor_key_', '') + ' → ' + localStorage.getItem(k));
				}
			}
		} catch(e) {}
		var matChunks = 0, totalChunks = s.virtChunks ? s.virtChunks.length : 0;
		if (s.virtChunks) {
			for (var vc = 0; vc < s.virtChunks.length; vc++) {
				if (s.virtChunks[vc].materialized) matChunks++;
			}
		}
		var anchorIdx = -1;
		if (anchorKey && s.virtAllRows) {
			for (var ai = 0; ai < s.virtAllRows.length; ai++) {
				if (String(s.virtAllRows[ai].key || '') === anchorKey) { anchorIdx = ai; break; }
			}
		}
		var lines = [
			'--- MODULAR VERSION ---',
			'Sutta: ' + (s.currentSutraId || '-'),
			'Langs: ' + (s.showPali?'P':'') + (s.showEng?'E':'') + (s.showVie?'V':''),
			'',
			'── DOM ──',
			'Total elements:   ' + allDom,
			'Row wraps:        ' + wraps.length,
			'',
			'── Virtual scroll ──',
			'Total chunks:     ' + totalChunks,
			'Materialized:     ' + matChunks + ' / ' + totalChunks,
			'virtAllRows len:  ' + (s.virtAllRows ? s.virtAllRows.length : 0),
			'',
			'── Anchor ──',
			'localStorage key: ' + (lsKey || '-'),
			'RAW value:        ' + (lsRaw === null ? '(null)' : '"' + lsRaw + '"'),
			'via storage.get:  ' + (anchorKey === null ? '(null)' : '"' + anchorKey + '"'),
			'Match raw?        ' + (lsRaw === anchorKey ? '✓ yes' : '✗ DIFFERENT!'),
			'Current top key:  ' + (s.firstVisibleKey || '-'),
			'Saved idx:        ' + (anchorIdx >= 0 ? anchorIdx : 'not-found in virtAllRows'),
			'Match chunk:      ' + (anchorIdx >= 0 ? Math.floor(anchorIdx / 50) : '-'),
			'',
			'── ALL anchors in storage ──',
		].concat(allAnchors.length ? allAnchors : ['(none)']).concat([
			'',
			'── Scroll ──',
			'scrollTop:        ' + st + ' px',
			'scrollHeight:     ' + sh + ' px',
			'clientHeight:     ' + ch + ' px',
			'Progress:         ' + scrollPct + '%',
			'FPS:              ' + fps,
			'',
			'── Cache ──',
			'Merged suttas:    ' + SA.MERGED_CACHE.size,
			'Loaded packs:     ' + SA.LOADED_PACKS.size,
			'',
			'── TTS ──',
			'Module loaded:    ' + (_ttsApi ? 'yes' : 'no'),
		]);
		if (mem) {
			lines.push('');
			lines.push('── Memory (JS heap) ──');
			lines.push('used:   ' + fmtBytes(mem.usedJSHeapSize));
			lines.push('total:  ' + fmtBytes(mem.totalJSHeapSize));
			lines.push('limit:  ' + fmtBytes(mem.jsHeapSizeLimit));
		}
		debugBody.textContent = lines.join('\n');
	}
	function show() {
		visible = true;
		debugPanel.hidden = false;
		debugPanel.setAttribute('aria-hidden', 'false');
		update();
		if (timer) clearInterval(timer);
		timer = setInterval(update, 500);
	}
	function hide() {
		visible = false;
		debugPanel.hidden = true;
		debugPanel.setAttribute('aria-hidden', 'true');
		if (timer) { clearInterval(timer); timer = null; }
	}
	btnDebug.addEventListener('click', function (e) {
		e.stopPropagation();
		if (visible) hide(); else show();
	});
	if (btnDebugClose) btnDebugClose.addEventListener('click', hide);
	var btnClearStorage = $('btnClearStorage');
	if (btnClearStorage) btnClearStorage.addEventListener('click', function (e) {
		e.stopPropagation();
		var count = 0;
		try { count = localStorage.length; } catch(_) {}
		var msg = 'Xóa TOÀN BỘ ' + count + ' items trong localStorage?\n' +
			'(anchor, settings, last sutta, cached theme…)\n\n' +
			'Trang sẽ reload về trạng thái mặc định.';
		if (!confirm(msg)) return;
		try { localStorage.clear(); } catch(_) {}
		location.replace(location.pathname + location.search);
	});
}

init();

// Dark mode toggle
(function () {
	var btn = document.getElementById('btnDarkMode');
	if (!btn) return;
	var STORAGE_KEY = 'sutra-dark-mode';
	var html = document.documentElement;
	var saved = null;
	try { saved = localStorage.getItem(STORAGE_KEY); } catch(e){}
	if (saved === 'dark') {
		html.setAttribute('data-theme', 'dark');
		btn.textContent = '☀️'; btn.title = 'Chế độ sáng';
	}
	if (!saved && window.matchMedia('(prefers-color-scheme: dark)').matches) {
		html.setAttribute('data-theme', 'dark');
		btn.textContent = '☀️'; btn.title = 'Chế độ sáng';
		try { localStorage.setItem(STORAGE_KEY, 'dark'); } catch(e){}
	}
	btn.addEventListener('click', function () {
		var isDark = html.getAttribute('data-theme') === 'dark';
		if (isDark) {
			html.removeAttribute('data-theme');
			btn.textContent = '🌙'; btn.title = 'Chế độ tối';
			try { localStorage.setItem(STORAGE_KEY, 'light'); } catch(e){}
		} else {
			html.setAttribute('data-theme', 'dark');
			btn.textContent = '☀️'; btn.title = 'Chế độ sáng';
			try { localStorage.setItem(STORAGE_KEY, 'dark'); } catch(e){}
		}
	});
})();

// Touch device detection
(function () {
	try {
		var mql = window.matchMedia('(pointer: coarse)');
		var apply = function (m) {
			var isTouch = !!m.matches || ('ontouchstart' in window) || (navigator.maxTouchPoints > 0);
			document.documentElement.classList.toggle('is-touch', isTouch);
		};
		apply(mql);
		if (mql.addEventListener) mql.addEventListener('change', apply);
		else if (mql.addListener) mql.addListener(apply);
	} catch(e) {}
})();

// Sticky hover/focus fix for mobile
document.addEventListener('click', function(e) {
	var isTouchDevice = ('ontouchstart' in window) || (navigator.maxTouchPoints > 0);
	if (!isTouchDevice) return;
	var clickableTarget = e.target.closest('button, a, [role="button"], [role="tab"], [role="menuitem"], .menu-sutta-link, .nikaya-tile, .pill, .sutra-col-copy, .sutra-seg-share');
	if (clickableTarget) {
		setTimeout(function() {
			try { clickableTarget.blur(); } catch(err) {}
		}, 200);
	}
}, { passive: true });

// Nuclear sticky-hover fix: strip :hover rules on touch devices
(function () {
	function isTouchPrimary() {
		return window.matchMedia('(pointer: coarse)').matches
			|| (('ontouchstart' in window) && navigator.maxTouchPoints > 0);
	}
	function stripHoverRules() {
		if (!isTouchPrimary()) return;
		var REGEX = /:hover\b|:focus(?!-)/;
		function walk(parentRule, rules) {
			if (!rules) return;
			if (parentRule && parentRule.conditionText &&
				/\(hover\s*:\s*hover\)/i.test(parentRule.conditionText)) {
				return;
			}
			for (var i = rules.length - 1; i >= 0; i--) {
				var r = rules[i];
				if (r.cssRules && r.cssRules.length) walk(r, r.cssRules);
				if (r.selectorText && REGEX.test(r.selectorText)) {
					try { (parentRule || r.parentStyleSheet).deleteRule(i); } catch(e) {}
				}
			}
		}
		var sheets = document.styleSheets;
		for (var si = 0; si < sheets.length; si++) {
			try { walk(null, sheets[si].cssRules); } catch(e) {}
		}
	}
	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', stripHoverRules);
	} else {
		stripHoverRules();
	}
	window.addEventListener('load', stripHoverRules);
})();

})();
